# Personal Automation System (PAS)

## Project Overview

A local-first home automation platform where users interact through a single Telegram bot. The infrastructure handles message routing, scheduling, data storage, LLM access, multi-user management, audio output, condition evaluation, and a management interface. Apps are modular plugins that implement specific functionality. Apps can be developed independently and shared between PAS instances as git repos.

**Documentation maintenance rules are in the `pas-documentation-maintenance` skill. Testing patterns and standards are in the `testing-standards` skill. URS workflow and traceability matrix are in the `pas-urs-workflow` skill.**

## Architecture Decisions

### Runtime & Monorepo
- **Node.js 22 LTS + TypeScript 5.x**, ESM only, `strict: true`
- **pnpm workspaces** — `core/` is infrastructure, `apps/*` are plugins. Path aliases: `@core/*` → `core/src/*`
- **Single process** — all apps in one Node.js process (target: Mac Mini with 32GB RAM)
- **Developer commands**: `pnpm dev`, `pnpm build`, `pnpm lint`, `pnpm test`, `pnpm scaffold-app --name=<app>`, `pnpm install-app <git-url>`

### Data & Storage
- **Markdown files on filesystem** — no database. Scoped paths: `data/users/<user_id>/<app_id>/`, `data/users/shared/<app_id>/`, `data/spaces/<spaceId>/<appId>/`, `data/system/`
- Atomic writes via temp file + rename (with Windows retry for EPERM). History never deleted — archive operations preserve content
- **YAML frontmatter** on all generated .md files for Obsidian compatibility. Use `stripFrontmatter()` before processing for LLM/eval. `appendWithFrontmatter()` for atomic create-or-append
- **Shared data spaces** — named membership groups (`/space` command). `DataStore.forSpace(spaceId, userId)` checks membership
- **Per-user Obsidian vaults** — symlinks at `data/vaults/<userId>/` to canonical data. Windows junctions, Unix symlinks. VaultService rebuilds at startup

### LLM Architecture
- **Multi-provider**: Anthropic (native), Google Gemini (native), OpenAI-compatible (covers OpenAI, Groq, Together, etc.), Ollama (optional local)
- **Tier-based routing** — apps request `fast`, `standard`, or `reasoning` tier; infrastructure maps to provider+model
- **Security boundary** — apps must NOT import LLM SDKs directly (`@anthropic-ai/sdk`, `openai`, `@google/genai`, `ollama` are banned). All LLM access via `CoreServices.llm`
- **Per-app safeguards** — `LLMGuard` enforces rate limits + monthly cost caps. `SystemLLMGuard` for infrastructure calls
- **Per-user cost tracking** — `AsyncLocalStorage` propagates userId transparently; 8-column usage log
- **Runtime model switching** — `ModelSelector` persists to YAML, changeable via GUI. `ModelCatalog` fetches available models (1-hour cache)
- Ollama optional — when `OLLAMA_URL` is empty, classification uses Claude fast model

### App System
- **Manifests** — YAML (`manifest.yaml`), validated against JSON Schema. Declare identity, capabilities (intents, commands, photos, schedules, rules, events), requirements, user config
- **Distribution** — apps are standalone git repos. `pas install <git-url>` clones, validates, scans for banned imports, checks `pas_core_version` compatibility
- **Trust model** — install-time static analysis catches accidental violations. No runtime sandbox (honest about this). DI enforces undeclared services are `undefined`, scoped data prevents path traversal
- **Message routing priority**: 1) `/command` exact match → 2) Photo classification → 3) Free text LLM classification (with "none" escape) → 4) Chatbot fallback
- **Route verification** — enabled by default. Grey-zone classifications (confidence 0.4–0.7) trigger a second LLM call (standard tier) with app descriptions for verification. On disagreement, inline Telegram buttons let the user choose. Disable via `routing.verification.enabled: false` in pas.yaml

### Chatbot & App Awareness
- **Chatbot app** — full conversational AI fallback. Per-user conversation history (20 turns), context store integration, graceful LLM failure degradation
- **`/ask` command** — app-aware system prompt with AppMetadataService + AppKnowledgeBase + SystemInfoService
- **Model journal** — per-model markdown files at `data/model-journal/{model-slug}.md`, `<model-journal>` tag extraction from LLM responses

### Reports, Alerts & Automation
- **Reports** — user-defined recurring reports: 4 section types (changes, app-data, context, custom), optional LLM summary, Telegram delivery
- **Alerts** — condition evaluation (deterministic + fuzzy/LLM) against data files. 6 action types: telegram_message, run_report, webhook, write_data, audio, dispatch_message. Scheduled or event-triggered
- **Template variables** — `{data}`, `{summary}`, `{alert_name}`, `{date}` in alert action fields

### External APIs & n8n
- **REST API** — `POST/GET /api/data`, `POST /api/messages`, `GET /api/schedules`, plus report/alert/changes/LLM/telegram APIs. Bearer token auth, 100 req/60s
- **n8n dispatch** — `n8n.dispatch_url` in pas.yaml; cron triggers POST to n8n instead of internal execution, with automatic fallback
- **Outbound webhooks** — config-driven EventBus subscribers, HMAC-SHA256 signing, fire-and-forget

### Frontend (Management GUI)
- **Server-rendered HTML** via Fastify + Eta templates, **htmx** for interactivity, **Pico CSS**
- Auth: Per-user password login (Telegram user id + password). Cookie: `{userId, sessionVersion, issuedAt}`, signed, 24h sliding session. Legacy `GUI_AUTH_TOKEN` accepted only when exactly one `isAdmin` user exists. CSRF double-submit cookie on all POSTs. Admin routes require `isPlatformAdmin`; non-admin users see only own data and joined spaces.

## Code Conventions

### File Naming & Imports
- Lowercase with hyphens: `scoped-store.ts`. Tests: `__tests__/<name>.test.ts`
- ESM imports with `.js` extension. Apps import types from `@core/types`, receive services via `CoreServices` in `init()`

### Error Handling & Logging
- App failures caught and logged — never crash the system
- Structured logging via Pino (JSON in production, pretty in dev)

### Data Files
- `## Active` / `## Archive` sections for list-type data. YAML frontmatter. Lowercase filenames with hyphens

### Testing
- **Vitest** for all tests. Mock `CoreServices` for app unit tests. Real filesystem (temp dirs) for DataStore tests
- **Zero failing tests policy** — the full test suite must pass with zero failures at all times. "Pre-existing failure" is not an excuse to leave tests broken. If you encounter a failing test, fix it — either fix the code or fix the test. Never skip, ignore, or dismiss test failures as someone else's problem.
- **Time-sensitive tests** — never hardcode dates in tests that compare against "today". Use relative dates (e.g., `new Date(Date.now() - 86400000)`) so tests don't rot as time passes
- **Testing patterns and standards are in the `testing-standards` skill. URS workflow and traceability matrix are in the `pas-urs-workflow` skill.**

### Deferred Work Tracking
- **`docs/open-items.md` is the single source of truth for all deferred, out-of-scope, and follow-up work.** Every spec, plan, and findings doc that explicitly defers something must have a corresponding entry in `docs/open-items.md` before the session ends.
- If a task is described as "deferred", "out of scope", "future phase", "follow-up", or "to be done in a later session", it must be added to `docs/open-items.md` under the appropriate section: Confirmed Phases, Deferred Infrastructure Work, Unfinished Corrections, Food App Enhancements, Proposals, or Accepted Risks.
- A `PostToolUse` hook (`check-deferred-work.sh`) automatically reminds you when you write to a spec or plan doc that contains deferred-work language. Do not dismiss this reminder without acting on it.

### Post-Phase Simplify Pass
After a phase merges, a brief cleanup commit is allowed before starting the next phase. Rules:
- **Phase footprint only** — touch only files that were part of the completed phase. No opportunistic cleanup of unrelated code.
- **No restructuring** — rename, extract, or reorganize only if the change is obviously correct and risk-free (e.g., a duplicated helper that is clearly safe to DRY). Do not refactor logic.
- **Separate commit** — the simplify pass must be its own commit, clearly labeled (e.g., `refactor(hermes-p7): post-merge simplify pass`), so it is easy to revert if it causes regressions.
- **Zero failing tests** — run `pnpm test` before committing the simplify pass. If any test is red, fix it first.
- **Triggered by** — dead code found during review, duplicated helpers within the phase, spurious comments, or a reviewer explicitly noting a cleanup opportunity. Not every phase needs one.

## Key File Paths

| Path | Purpose |
|------|---------|
| `core/src/bootstrap.ts` | Main composition root |
| `core/src/types/app-module.ts` | AppModule + CoreServices interfaces |
| `core/src/types/llm.ts` | LLM type system (providers, tiers, models) |
| `core/src/types/config.ts` | SystemConfig + LLMConfig types |
| `core/src/schemas/app-manifest.schema.json` | Manifest JSON Schema |
| `core/src/services/router/index.ts` | Message routing dispatch |
| `core/src/services/data-store/scoped-store.ts` | Per-user/per-app data access |
| `core/src/services/data-store/paths.ts` | Scope normalization (POSIX traversal rejection, null-byte guard) |
| `core/src/services/file-index/index.ts` | FileIndexService — in-memory file metadata index, EventBus refresh |
| `core/src/services/llm/index.ts` | LLM service (multi-provider routing) |
| `core/src/services/llm/llm-guard.ts` | Per-app rate limit + cost cap |
| `core/src/services/context/request-context.ts` | Unified AsyncLocalStorage — `{userId?, householdId?}` propagated through every dispatch point |
| `core/src/services/config/app-config-service.ts` | Per-user config overrides (reads userId from requestContext) |
| `core/src/services/reports/index.ts` | ReportService: CRUD, run, cron lifecycle |
| `core/src/services/alerts/index.ts` | AlertService: CRUD, evaluate, cron lifecycle |
| `core/src/services/spaces/index.ts` | SpaceService: CRUD, membership |
| `core/src/services/vault/index.ts` | VaultService: per-user Obsidian vault symlinks |
| `core/src/services/household/index.ts` | HouseholdService — YAML persistence, userId→householdId, boundary assertions |
| `core/src/services/app-installer/index.ts` | App install orchestrator |
| `core/src/services/invite/index.ts` | Invite code generation, validation, redemption |
| `core/src/services/credentials/index.ts` | CredentialService — scrypt hashing, sessionVersion, credentials.yaml |
| `core/src/services/api-keys/index.ts` | ApiKeyService — per-user API key store, verify/revoke, debounced lastUsedAt |
| `core/src/gui/index.ts` | GUI route registration |
| `core/src/gui/auth.ts` | GUI token auth + cookie middleware |
| `core/src/gui/csrf.ts` | CSRF protection |
| `core/src/api/index.ts` | External API plugin registration |
| `core/src/api/auth.ts` | API Bearer token auth |
| `core/src/utils/frontmatter.ts` | Obsidian frontmatter utilities |
| `core/src/utils/file-mutex.ts` | FileMutex — `withFileLock`/`withMultiFileLock` for atomic RMW operations |
| `config/pas.yaml` | System configuration |
| `core/src/services/conversation/` | ConversationService + all conversation helpers (fallback, /ask, /edit, /notes) |
| `apps/food/` | Food management app (household, recipes, grocery, pantry) |
| `docs/urs.md` | User Requirements Specification |
| `docs/CREATING_AN_APP.md` | App developer guide |
| `docs/MANIFEST_REFERENCE.md` | Manifest field reference |
| `docs/implementation-phases.md` | Detailed phase guide (read before starting new phases) |

## Security

Security patterns and posture are in the `pas-security-posture` skill. Invoke when touching auth, cookies, LLM prompts, templates, path handling, or API endpoints.

## Implementation Status

All infrastructure (phases 0–30), food app (H1–H12b), security remediation (R1–R7, CR6, CR8, CR9), and deployment readiness (D1–D6 incl. D5a, D5b) phases are complete. LLM enhancement items #1 (route metadata) and #2 Chunks A–D complete. D5c Chunks 0–E complete. **Hermes P1 complete (Chunks A–D, all sub-chunks).** **Hermes P2 complete (Chunks A–E).** **Hermes P3 complete — session persistence, REQ-CONV-SESSION-001..014.** Post-merge: simplify pass (resolveOrDefaultSessionKey, buildFrontmatter DRY, .legacy-checked sentinel, dead export removal) + Codex P2/P3 fixes (upgrade-compat scan, stale module imports). **Hermes P4 complete (2026-04-28) — durable-memory snapshot + fenced recall, REQ-CONV-MEMORY-001..012.** Post-merge: simplify pass (byte-identical test bug fixed, task-reference comments removed) + Codex corrections (ensureActiveSession fail-open, empty-session rollback on LLM failure, budget enforcement ≤4000, entryCount = included count, sanitizer zero-width + case-insensitive). **Hermes P5 complete (2026-04-28) — SQLite + FTS5 transcript search, REQ-CONV-SEARCH-001..014.** Derived index (`data/system/chat-state.db`), user-scoped FTS5 search, recall classifier (fast-tier LLM), Layer 5 fenced injection (`<memory-context label="recalled-session">`), rebuild CLI, opt-in retention/prune. Post-merge Codex corrections: stale rebuild reconciliation (delete DB before re-index), path-derived ownership authority (frontmatter mismatch = warn + skip), Layer 5 before history in both prompt builders, user-scoped `Map<userId,Set<sessionId>>` prune sweep, lint gate clean. ~8593 tests / ~376 files. **Hermes P7 complete (2026-04-28) — session auto-titling (Chunk A) + NL /newchat classifier (Chunk B), 16 URS requirements REQ-CONV-TITLE-001..008 + REQ-CONV-NEWCHAT-001..008.** **Hermes P9 complete (2026-04-29) — Photo Memory Bridge + Receipt Integrity, 7 URS requirements REQ-CONV-PHOTO-001..005 + REQ-FOOD-RECEIPT-001..002.** Photo handlers return `{photoSummary}`; `dispatchPhoto` resolves session before dispatch, binds `expectedSessionId`, runs handler in ALS, appends turn pair; `formatConversationHistory` exact-string whitelist (4 headers, 2000-char cap, spoof-resistant); `PHOTO_SUMMARY_GUIDANCE` in both prompt builders; `sanitizePhotoField` strips control chars + bidi + XML fence tags. Receipt integrity: `isValidReceiptDate` (calendar-strict, 90-day window, `MAX_RECEIPT_AGE_DAYS`), today's date (timezone-aware) injected into LLM prompt, `rawExtractedDate` audit trail, `capturedAt` as filename/frontmatter/price-store sort authority. LLM Enhancement #3.A/B/C complete. Post-merge Codex corrections (2026-04-29): P1 — receipt frontmatter now has `date: parsed.date` (display) + `capturedAt` (sort authority); P2 — date rejection warns include `userId` from requestContext, non-string dates also logged; P3 — bidi sanitizer extends to U+2069 (LRI/RLI/FSI/PDI) with explicit `\uXXXX` notation. **Hermes P8a Chunk A complete (2026-05-04) — idle auto-reset, `last_activity_at` frontmatter, `idle-detector.ts`, `runIdleResetHook` with full fail-open, Router placement (routeMessage + routePhoto), NL double-message guard, `auto_reset_idle_minutes` config. 10 URS requirements REQ-CONV-IDLE-001..010. Post-merge Codex corrections (2026-05-04): P2-1 — endActive race guard (`endedSessionId: null` → `status='none'`); P2-2 — literal /newchat and /reset commands suppressed when idle reset fires; P2-3 — `last_activity_at` not bumped on already-ended sessions; P2-4 — test assertion added for `handleMessage` call; P3-1 — `formatDuration` non-integer hours (90 min → "1 hour 30 minutes"); P3-2 — open-items.md updated (accepted risk + P8b/P8c deferred phases); P3-3 — real-store integration test added; P3-4 — `IdleResetState` moved to `types/conversation-session.ts`. Second Codex review corrections (2026-05-04): R2-P1 — `endActive` CAS fix (index lock across getActive+verify+clearActiveUnlocked prevents wrong-session closure); R2-P2 — routePhoto idle-reset hook moved before "no photo apps" early return; R2-P3 — `formatDuration` rewritten using day/hour/minute parts array (1470 min was "25 hours", now "1 day 30 minutes"). Third Codex review corrections (2026-05-04): R3-P2 — `endActive` safe ordering fix: index cleared only after transcript `ended_at` write succeeds (clearIndex flag pattern; write failure leaves index intact + test added); R3-P1 — `parentTitle` JSDoc reserved-for-P8b comment; R3-P3 — Biome lint: _userId in session-index, vi.mocked() in hook tests, biome-ignore for sentinel values, optional chaining, import ordering; deleted fix-regex.js.** **Hermes P8b complete (2026-05-04) — Memory flush on idle reset. Per-user opt-in toggle (`flush_memory_on_idle_reset`, default OFF) via `CONVERSATION_USER_CONFIG` + GUI + `<config-set>` LLM tag. `SessionSummarizer` (fast tier, `maxTokens=400`, `temperature=0`, tail 60 turns, 12k-char cap, JSON envelope, sanitization). `flushMemoryToContextStore` writes to ContextStore under rolling key `recent-session-summary` with defense-in-depth re-sanitization. CAS-first ordering (endActive before summarize); 8-second `Promise.race`/`AbortController` timeout; full fail-open. Disable cleanup: toggle-off deletes prior summary (both `<config-set>` and GUI). `buildMemorySnapshot` extended with `pinnedKeys` (default `['recent-session-summary']`) guaranteeing inclusion in Layer 2 despite budget truncation. Bypass symbol (`CONTEXT_INTERNAL_BYPASS`) captured in compose-runtime closures; never imported by hook/helpers. 12 URS requirements REQ-CONV-FLUSH-001..012. Post-merge Codex corrections (2026-05-04): Codex-P1 — `buildSnapshot` callback in handle-ask/handle-message gates pinning on `flush_memory_on_idle_reset` setting via `resolveUserBool`; GUI config cleanup changed from fire-and-forget to awaited with error-level logging; Codex-P2 — removed `please` from `MEMORY_FLUSH_INTENT_REGEX` action-verb group (false-positive on "please explain session memory"); 3 regression-guard negative tests added; Codex-P3a — `ConversationRetrievalService` interface now exposes `opts?` on `buildMemorySnapshot`; compile-time interface test added; Codex-P3b — `BIDI_ZERO_WIDTH_RE` rewritten with `\u` escape sequences; control-char regex similarly; `runFlushWithTimeout` destructures deps to eliminate non-null assertions.** 409 test files / 9136 tests passing (full suite, including food app). **Food receipt/price TDD Batches 0–6 + post-TDD simplify pass complete (2026-05-05)** — see Current Priority section below. **Hermes P5 carry-forwards complete (2026-05-05) — `/recall` command + `<session-search>` pseudo-tool, 21 URS requirements REQ-CONV-RECALL-001..009 + REQ-CONV-TOOL-SEARCH-001..012.** `/recall` Router built-in (bypasses AppToggleStore); `handleRecall` with term-aware FTS5 highlight stripping, per-hit 1500-char cap, full 23-char session id display, `sendSplitResponse`. `<session-search query="..."/>` pseudo-tool: `SESSION_SEARCH_INSTRUCTION_BLOCK` injected when intent regex matches AND `session_search_tool_enabled` ON AND `hasSessionSearch()` true; `extractSessionSearchTag`/`stripSessionSearchTags` at every exit path; single re-prompt loop with `buildToolContinuationPrompt` (all inputs fenced via `buildMemoryContextBlock`); second LLM response flows through existing post-processors; recursion cap; `sessionSearchAllowed` gate prevents execution when intent didn't match. `session_search_tool_enabled` user config. Codex corrections: P1 — execution gate reuses `sessionSearchAllowed` (same as instruction injection gate); continuation inputs fenced; P2 — `searchSessions` try/catch narrowed; empty-`beforeTag` fallback; integration test T2 deterministic; P3 — `const retrieval` captured before `Promise.all` in both handlers; Minor — session-id backtick escape. 416 test files / 9306 tests passing. **Unified Settings Chunks A + E + F complete (2026-05-05).** `SettingsRegistry` composes from chatbot virtual manifest + installed app manifests; entries keyed by `(appId, key)`. New `SettingsWriter` routes coerced writes per appId; `<config-set>` allowlist + intent gates derive from `SettingsRegistry.getNlSafeQualifiedKeys()` (REQ-SETTINGS-007). Chatbot prompt receives a per-turn `<memory-context label="settings-catalog">` block + a separate trusted `<config-set>` instruction block via new `'settings'` source category — gated by PAS classifier `settingsCandidate` flag in free-text mode, default-on in `/ask`. `adminOnly`/`dangerous`/`hidden` settings filtered from non-admin catalogs. Live per-turn reads via per-app `AppConfigService.getOverrides`. Fail-fast on invalid `nlIntentRegex`. 6 URS requirements: REQ-SETTINGS-001, 006, 007, 008, 011, 012. System-config-derived settings (chat.sessions.*) deferred to Chunk C. Chunks B/D/G (GUI page, /settings command, /gui/credentials) deferred to follow-on phases. **Unified Settings Chunk B complete (2026-05-06) — `/gui/settings` page with accordion per category; per-row Reset (htmx outerHTML swap); single-form Save with PRG redirect; `SettingsWriter.validate` + `writeBatch` + `registerPostWriteHook`; `AppConfigServiceImpl.removeOverride`; `categories.ts` shared module; layout nav link; CSRF + auth + XSS escaping; concurrency-safe via `withFileLock`. 11 URS requirements: REQ-SETTINGS-002, 003, 004, 005, 014, 015, 016, 017, 018, 019, 020. Chunks D/G (/settings Telegram command, /gui/credentials) deferred. **Open-Items Cleanup Batch 2 complete (2026-05-07) — `isPasRelevant()` deletion + URS cleanup; MODEL_SWITCH_INTENT_REGEX descoped to own phase.** **Open-Items Cleanup Batch 3 complete (2026-05-07) — `/flushmemory` + `/flush-memory` Router built-ins (REQ-CONV-FLUSH-013..018); `SessionControlLogger` telemetry + `pnpm analyze-session-control-log` CLI (REQ-CONV-NEWCHAT-009..012); `chat.recall.max_window_days` config key (REQ-CONV-TEMPORAL-013..015). Post-merge Codex corrections (P2-1..P2-3, P3-4..P3-8): stale-callback telemetry; `outcome='failed'` on handleNewChat throw; grey-zone confirmation rate scoped to grey-zone entryIds; Message field JSON-parsed in log parser; opening `<script>`/`<style>` tags stripped in safeForLog; URS narrowed to successful invocations; maxWindowDays NL test added. 467 test files / 10,324 tests passing. Remaining cleanup batch: 5 (test-only additions). **Open-Items Cleanup Batch 1 complete (2026-05-07, `codex/batch1-gui-cleanup`) — `app-detail.eta` renders `select`-typed user_config entries as `<select>` (REQ-CONV-FLUSH-011); `flush_memory_on_idle_reset` toggle-off write migrated through `SettingsWriter` post-write hook (was legacy direct write). Codex corrections: type safety on `user_config` map, data-loss bug (missing `removeOverride` call on toggle-off), oracle quality (GUI routes test). URS traceability matrix updated (REQ-CONV-FLUSH-011 row: +2 test files, +2 happy, +5 edge). 467 test files / 10,324 tests passing.** **Open-Items Cleanup Batch 4 complete (2026-05-07, `codex/batch4-food-micro-fixes`) — `formatCheapestPriceAnswer` wording changed to "Lowest saved package price" (REQ-FOOD-PRICE-002), 11 unit tests + 4 NL persona tests; Energy/mood fields compile-time regression guard (REQ-FOOD-HEALTH-NEG-001). 2 new URS requirements.** **Open-Items Cleanup Batch 5 complete (2026-05-07, `codex/batch5-test-coverage`) — P4 freeze integration test (F1–F9, real `composeRuntime` + `RecordingStubProvider`, 9 scenarios in `memory-snapshot-freeze.integration.test.ts`); `interactionContext` snapshot fetch removed (public `getRecentInteractions()` path retained); 2 new regression-guard tests; URS REQ-CONV-MEMORY-{001, 002, 005, 006, 007, 009, 010, 011} + REQ-CONV-RETRIEVAL-014 extended with integration test references. 470 test files / 10,368 tests passing.** **Persona Regression Suite Chunk A.1 — substrate complete (2026-05-09).** New top-level `regression/` pnpm workspace excluded from root `pnpm test` (REQ-REG-001). Shipped: `shared/types.ts` (PersonaCase, RunResult, TierModelSnapshot, OracleKind, Verdict), `shared/cache-key.ts` (git-blob-hash + tier-model snapshot, repo-relative paths only, `Promise.all` parallel hashing), `shared/git-hash.ts` (git ls-files --error-unmatch + `--no-filters`, ENOENT propagation), `shared/validate-case.ts` (PersonaCase shape + Chunk-A scope guard rejecting `oracle: 'rubric'` / `oracle: 'judge'` per REQ-REG-014), `runner/cache.ts` (schema-validated reads, atomic tmp+rename writes with PID+nonce, history retained per REQ-REG-010), `runner/budget.ts` (CaseBudget + RunBudget with NaN/Infinity rejection, REQ-REG-008), `runner/seed.ts` (`<hash>  <relpath>` manifest verifier with traversal/absolute rejection, repo-relative paths in failures, REQ-REG-006), `oracles/structural.ts` (AJV strict + set-equality + keyed-scalar tolerance + calendar-strict dates via shared `@core/utils/temporal.isCalendarStrict`; non-parseable JSON → `verdict: 'error'` per spec line 180; defensive misconfig guard on min/maxIso), `runner/case-runners/receipt-runner.ts` (calls REAL `parseReceiptFromPhoto` from `apps/food/src/services/receipt-parser.ts` — does NOT duplicate the prompt; pre-charge budget gate; `expectRejection: true` mode validates parser overwrites date to today + preserves `rawExtractedDate`; verdict precedence preserves prior fail/error). Config: `regression.maxRunBudgetUsd` in `pas.yaml.example` with zod schema (REQ-REG-009). Pre-Task-1 baseline fixes (4 tests on `main`): edit realpath assertion, alert-executor backslash path runtime guard, GUI/API auth ALS seeded before awaited rehydration, file-mutex on `appendWithFrontmatter`. Simplify pass landed in `63548fe` (isCalendarStrict reuse, 3× `Promise.all` parallelism, verdict-precedence fix, NaN test, comment hygiene). 8 implemented URS REQs (REQ-REG-001/002/004/006/008/009/010/014); 6 deferred to Chunks A.2/B/C/D. 489 test files / 10,986 tests passing across root (10,889) + regression workspace (97). **Chunk A.2 (5 receipt cases) is blocked on operator photo delivery** — see `docs/open-items.md` Persona Regression Suite Chunk A.2 carry-forward for the 5 specific photo filenames + downstream task list. The orchestrator + CLI originally scoped for A.2 shipped early in Chunk B.1. **Persona Regression Suite Chunk B.1 complete (2026-05-11) — routing bucket + orchestrator + CLI + REQ-REG-011.** `core/src/types/regression.ts` (shared types, breaks `core → regression` cycle); `LoadedCase` case-loader supporting `.case.ts` + `index.ts buildCases()` modules; routing case-runner dispatching to three real production classifiers via metered adapters (`FoodShadowClassifier.classify`, `detectSessionControl`, `classifyPASMessage`); CostTracker-delta cost metering; orchestrator with **hard-abort RunBudget** (Codex C-9: remaining cases marked `budget-exceeded` without dispatch + synthesized `error` oracle verdicts so REQ-REG-011 counts them); **per-input REQ-REG-011 accuracy gate** where `fail` AND `error` both count against denominator (Codex C-2; floor = 20 food-shadow inputs; threshold = 0.95; CLI exits 1 below threshold); single `pnpm test:regression` script with `--bucket --rerun --dry-run --json --help` flags. 36 routing cases: 27 generated from `FOOD_PERSONAS` via single-source `buildCases()` (Codex C-12; ~108 accept-phrase inputs), 3 strict per-intent session-control cases, 6 PAS-classifier cases (positive + negative for `pasRelated`/`dataQueryCandidate`/`settingsCandidate`). Codex corrections baked in: C-1 dot-path expectations (not JSON-Pointer); C-3 food-shadow adapter returns raw output on `parse-failed` instead of throwing (oracle judges via schema fail = real regression signal); C-4 subprocess design eliminates `core → regression` cycle; C-7 local `_stub-provider.ts` instead of importing the non-exported `RecordingStubProvider`; C-10 `deps.caseBudgetUsd` single source of truth; C-11 happy paths for all three targets; C-13 strict session-control intents (no lenient `unclear`); C-14 PAS `DATA_QUERY_PREFILTER` zero-cost coverage; C-15 `LoadedCase` shape from the start; C-16 `fileURLToPath(import.meta.url)` everywhere; C-19 single CLI script; C-21 temp-git-repo cache invalidation test (no real-source pollution). 9 implemented URS REQs (REQ-REG-001/002/004/006/008/009/010/011/014); 5 still deferred to Chunks B.2/C/D. 211 unit + integration tests pass; root suite (488 files / 10,890 tests) green. Chunk B.2 (`/gui/regression` admin page + SSE subprocess + REQ-REG-007 + REQ-REG-013) is the next phase. **Post-merge Codex correction (2026-05-11):** `buildProductionDeps()` was rewired to compose the same `ProviderRegistry → ModelSelector → LLMServiceImpl` stack as `compose-runtime.ts` (was a throwing stub); `buildDryRunDeps()` added for env-free dry-runs; `cli-main.ts` peeks at argv to dispatch between the two. Dry-run now renders an estimate-focused summary (cases, total inputs, upper-bound cost) instead of fake pass/fail counts. Borderline session-control fixture "how do I start a new chat" removed (genuinely ambiguous to LLM). PAS adapter intentionally omits `appMetadata` (documented; tests the base no-metadata classifier path so cases don't drift with apps installed/uninstalled). REQ-REG-013 cost field correctly metered via `CostTracker` delta; **per-call token counts remain 0 — `LLMService.complete()` returns only the string**. Operator verification (real-LLM, accuracy gate) is the remaining carry-forward — see open-items.md. **Persona Regression Suite Chunk B.2 complete (2026-05-11) — `/gui/regression` admin page, REQ-REG-007 + REQ-REG-013 + REQ-REG-003 (promoted from Chunk D) + REQ-REG-015 (history) + REQ-REG-016 (cancel) + REQ-REG-017 (estimate).** Five read routes (page, drilldown, case-row, history, estimate) + three write routes (POST runs, GET SSE events, POST cancel), all admin-only. New `--list` CLI flag + `buildMetadataDeps()` resolves real tier IDs without composing providers (Codex C1 cache-key parity). Six backend services under `core/src/gui/services/regression/`: `case-discovery` (fresh per page load, fail-closed terminator check), `cache-reader` (current-key-first selection + strict RunResult validator), `estimator` (per-bucket constants), `run-registry` (single-active-run, terminal-state inference, cancel + GC + shutdown), `subprocess` (process.execPath + tsx loader, `gate-failed` distinct from `failed`, Pino-on-stdout tolerance), `sse-helper` (Fastify hijack-pattern, keep-alive, terminal close). SSE `case-completed` payload carries only `{caseId, runId}`; client fetches `/cases/:caseId/row` partial via htmx (Codex I7 XSS defense). `forceFresh` checkbox expands the visible filter into `--rerun=<id>` args (Codex I2 — no new CLI flag). Codex corrections baked in: C1 (cache-key parity), C2 (newer-stale doesn't mask older current), C3 (gate-failed vs failed), C4 (no TTL on currentCacheKey), C5 (never-run drilldown), I1 (both --rerun forms), I2 (forceFresh expansion), I3 (Pino→stderr in --json), I4 (list-mode fail-closed), I5 (strict cache validator), I6 (302 redirect for unauth POST), I7 (SSE row via htmx fetch), I8 (concrete hijack-pattern), I9 (fake-CLI fixture + real --list smoke test), I10 (accepted-risk entries in open-items.md), M2 (pnpm --filter @pas/regression), M3 (pnpm build for typecheck), M4 (process.execPath, no PATH), M5 (behavioural estimator tests). 7 URS entries: REQ-REG-003/007/013/015/016/017 implemented (5 new — REQ-REG-014 unchanged); REQ-REG-015/016/017 are net-new. Token counts displayed as `—` with a documented carry-forward (`LLMService.complete()` usage plumbing). 6 implementation batches across the plan (CLI flag → backend services → read routes → write routes → integration tests → URS + docs). Root core suite: 6921 tests passing (+145 from B.2 across services + routes + integration). Regression workspace: 243 tests passing (+26 from B.2 CLI changes). Three accepted risks logged in `docs/open-items.md`: in-memory run registry, single-active-run serialization, estimator constants are approximations. **Post-merge Codex review (2026-05-11):** P1 corrections — (1) wired the GUI end-to-end to SSE via `partials/regression-live.eta` (EventSource bootstrap, estimate confirm, per-row htmx fetch on `case-completed`, cancel button, terminal banners); (2) `spawnRegression` schedules a 5-second SIGKILL after each SIGTERM in both the AbortSignal and `handle.cancel()` paths, timer cleared on exit; (3) `createRun()` wraps `runFactory` in try/catch — on failure it clears `activeRunId`, resolves `whenComplete`, marks state `failed`, rethrows — preventing the phantom-active wedge. P2 corrections — (4) `readOne` passes the filename cacheKey to `looksLikeRunResult` so filename/content mismatch is rejected; (5) `/cases/:caseId/history` now requires the case in `caseDiscovery.discover()` (matches drilldown/row); (6) `openSseStream` accepts an `onClose` callback so client disconnects detach the registry listener. P3 corrections — (7) `process.env.DOTENV_CONFIG_QUIET = 'true'` set in `cli-main.ts` so `--list --json` stdout is strictly NDJSON; (8) URS matrix updated with `codex-corrections.test.ts` rows. Incidental: fixed a pre-existing bug in run-registry's no-handle path (`checkDone` read `state.status` before `dispatchEvent` updated it). **Persona Regression Suite Chunk C complete (2026-05-11) — recall bucket + rubric oracle + chatbot bucket.** `regression/src/oracles/rubric.ts` (REQ-REG-005): judge LLM with standard tier; rubric input fenced through production `buildMemoryContextBlock` + `sanitizeContextContent` (strips zero-width / bidi controls, collapses 3+ backticks, escapes role-like tags); score-≥-4 pass threshold; NaN/Infinity/out-of-range → `verdict: 'error'` (prevents misbehaving judge from flipping real failures to pass). 25 recall cases under `regression/src/cases/recall/` (single `buildCases()` index pinning `today: '2026-05-11'` for deterministic temporal assertions; 13 true / 10 false / 2 observational); graded by structural oracle against `classifyRecallIntent`; pre-filter cases zero-meter. 10 chatbot cases under `regression/src/cases/chatbot/` migrated from `scripts/iterate-prompts.ts` v0 corpus, rewritten as self-contained prompts (Codex C3), each declaring `expectedHandler` (Codex I6) and a 3–4-criterion rubric. `chatbot-environment.ts` (REQ-REG-006 + REQ-REG-012): per-run `composeRuntime` against an isolated `mkdtemp` data dir; `verifyFixtureIntegrity` runs BEFORE any seed write (REQ-REG-006 enforcement point); full try/catch wraps post-mkdtemp path with `rm(tmpRoot)` on failure (Codex I4). Orchestrator lazy-builds the chatbot env on the first chatbot case and disposes after the last via try/finally; env-factory failure marks ALL remaining chatbot cases as `verdict: 'error'` without retrying (Codex I3). `buildProductionDeps` wires recall adapter + chatbot env factory + judge LLM + `applyModelMatrixOverride` / `applyJudgeModelOverride` helpers (Codex C1: `--model-matrix=<list>` and `--judge-model=<ref>` CLI flags). Recall adapter accepts per-call `today` override for deterministic temporal expectations (Codex C4). 3 new URS REQs (REQ-REG-005/006/012). 27 test files / 329 tests passing in the regression workspace. Carry-forwards: chatbot env `captureHandler`/`endActiveSession` are placeholder stubs (Router instrumentation not yet plumbed through `RuntimeServices`); Task 14 local-model verification template ready at `docs/superpowers/plans/findings/2026-05-11-chunk-c-local-model-verification.md` (operator-driven, requires local ollama).

Spec: `docs/superpowers/specs/2026-04-13-deployment-readiness-roadmap-design.md`. See `docs/implementation-phases.md` for detailed phase history.

### Current Priority: Post-Chunk-C follow-ups (Chunk C implementation complete 2026-05-11; live Gemma verification recorded 2026-05-12; chatbot framework wiring fully exercised end-to-end with 5/10 case-quality pass on real Gemma)
**Next phase work is operator-driven prompt hardening informed by the Chunk C verification findings:** food-shadow + recall + PAS classifier prompts need `format: json` / explicit JSON-only instructions for local Gemma compatibility; the rubric oracle's judge prompt needs softening for local judges that misread fenced-input conventions; food app's price-lookup intent should accept "cheapest X" patterns. See `docs/superpowers/plans/findings/2026-05-11-chunk-c-local-model-verification.md` for per-case detail and `docs/open-items.md` for the full follow-up backlog. Re-introduction of `expectedHandler` on chatbot fixtures is gated on Router instrumentation surfacing per-dispatch handler ids through `RuntimeServices`. Chunk A.2 (5 hand-curated receipt fixtures) still blocked on operator photo delivery.
**Hermes P1 is complete** (D.4 merged 2026-04-27). ConversationService is now a first-class core service; all legacy `fallback`/`_legacyKeys` surface is removed. **Hermes P2 is complete** (Chunks A–E merged 2026-04-27). ConversationRetrievalService provides broad, policy-governed data visibility for `handleMessage` and `handleAsk`; 16 new URS requirements (REQ-CONV-RETRIEVAL-001 through 016) document the implementation. **Hermes P3 is complete** (2026-04-27). Session persistence — manual `/newchat` and `/reset`; `ChatSessionStore` with per-session markdown transcripts, `active-sessions.yaml` index, legacy `history.json` migration, `expectedSessionId` in-flight race guard; 14 URS requirements (REQ-CONV-SESSION-001 through 014). **Hermes P4 is complete** (2026-04-28). Durable-memory snapshot frozen at session-mint via `ensureActiveSession`; persisted in session frontmatter; injected as Layer 2 (`<memory-context label="durable-memory">` block); per-turn ContextStore re-injection removed; Layer 4 recalled data wrapped in `<memory-context label="recalled-data">`; 12 URS requirements (REQ-CONV-MEMORY-001 through 012). Plan: `docs/superpowers/plans/can-you-start-on-shimmying-mountain.md`. **Hermes P5 is complete** (2026-04-28). SQLite + FTS5 derived transcript index (`data/system/chat-state.db`); user-scoped search; two-stage recall pipeline (pre-filter + fast-tier LLM classifier); Layer 5 fenced injection (`<memory-context label="recalled-session">`); rebuild CLI (`pnpm chat-index-rebuild`); opt-in retention + prune (`auto_prune`, `retention_days`); 14 URS requirements (REQ-CONV-SEARCH-001 through 014). Spec: `docs/superpowers/specs/2026-04-28-hermes-p5-transcript-search-design.md`. **Hermes P7 is complete** (2026-04-28). Session auto-titling (Chunk A): `TitleService` + `TitleGenerator` + `AutoTitleHook`; auto-title after first exchange (fire-and-forget); `/title` command (display/set); `skipIfTitled` guard; Markdown-canonical + SQLite-derived writes; title sanitization (control chars, whitespace collapse, 80-char truncation); 8 URS requirements (REQ-CONV-TITLE-001 through 008). NL /newchat classifier (Chunk B): `SessionControlClassifier` keyword pre-filter (16 phrases) + fast-tier LLM; `PendingSessionControlStore` TTL store (5-minute expiry); grey-zone inline Telegram buttons (`sc:yes`/`sc:no`); opt-in at Router level; 8 URS requirements (REQ-CONV-NEWCHAT-001 through 008). **LLM Enhancement #2 Chunks A–D** are complete: Chunk A (route-first dispatch, 9-intent allowlist, ~74 new tests), Chunk B (shadow classifier infrastructure B.1 + B.2 + FOOD_PERSONAS, ~239 tests), Chunk C (shadow pipeline wired into `handleMessage` — `computeVerdict`, `finalizeShadow`, `regexWinner` per branch, skipped-* gates, `shadow_sample_rate` config; +26 tests), Chunk D (shadow-primary machinery — `routing_primary` flag, `SHADOW_HANDLERS` 25-entry table, `shadow_min_confidence` threshold, result reuse preventing double-classify, telemetry CLI `pnpm analyze-shadow-log`; +64 tests). Production flip (`routing_primary: shadow`) is a config decision gated on ≥95% telemetry from `pnpm analyze-shadow-log`. **Hermes P9 is complete** (2026-04-29, 9dba662). Photo Memory Bridge + Receipt Integrity — see Implementation Status above. **Hermes P8a + P8b are complete** (2026-05-04) — see Implementation Status above. **Hermes P8c complete (2026-05-04) — parent-session lineage, REQ-CONV-LINEAGE-001..006.** When idle reset fires, successor minted with `parent_session_id` pointing to ended session; wired through `ensureActiveSession` + `appendExchange` cold-mint; validated via `SESSION_ID_RE`; persisted in Markdown frontmatter and SQLite index (schema v2, btree index `sessions_parent_session`); rebuild propagates from frontmatter. Post-merge Codex corrections (2026-05-04): P1 — photo post-idle-reset mint records `parent_session_id` (`routePhoto` captures hook return, `dispatchPhoto` accepts + forwards `parentSessionId`); P2 — fail-open `appendExchange` path in `handleMessage` and `handleAsk` now forwards `parentSessionId` (was silently dropped when `ensureActiveSession` rejects); P3 — `upsertSession` set-once invariant at SQLite layer (`INSERT ... ON CONFLICT DO UPDATE` excluding `parent_session_id` from SET clause, REQ-CONV-LINEAGE-007) + FTS orphan defence. **Food receipt/price TDD Batches 0–6 + post-TDD simplify pass complete (2026-05-05).** Batches 0–6 closed the receipt follow-up bug: receipt-query service isolation, `formatReceiptDetails` 4096-char guard, `isValidPriceEntry` warning on malformed price entries, receipt date validator, receipt summary integrity (hallucination counter-instruction), `HandlerResult` void contract so food `handleMessage` yields `{handled:false}` to chatbot fallback, regex tightening (EXPLICIT_RECEIPT_RE, PRICE_LOOKUP_EXCLUDE_RE, STORE_SPENDING_RE, ITEM_STOPWORDS) + DataQuery verb expansion (RC1–RC6), chatbot+DataQuery generalization for receipt/price/spending questions via PAS classifier pre-filter + `formatDataAnswer` shared utility (RC7). Post-TDD simplify pass: `HandlerResult = void | {handled:boolean}` build fix (unblocked echo + notes app compilation), stale RED/GREEN/RC# comment strip from 7 test files, 4 household-guard DRY sites collapsed to `requireHouseholdOrMessage`, gate/executor split for 3 receipt handlers (`force` param removed; callers use `isXIntent` + `executeX`), `writeSeedData` household-scope path fix (`data/households/<id>/shared/food/`), `TestCaseMeta` corpus annotation on `iterate-prompts.ts` `TEST_CASES` (182–424), Codex corrections (corpus spec drift, route-dispatch stale comment, `CaseResult` carries `meta`). 409 test files / 9136 tests passing. Next: production flip of shadow classifier (needs ≥95% telemetry from `pnpm analyze-shadow-log`). **Hermes P5 carry-forwards complete (2026-05-05)** — `/recall` command + `<session-search>` pseudo-tool. 21 URS requirements (REQ-CONV-RECALL-001..009, REQ-CONV-TOOL-SEARCH-001..012). Codex corrections applied. 416 test files / 9306 tests passing. **Hermes P6 complete (2026-05-05) — typed memory + temporal recall, 11 URS requirements REQ-CONV-KIND-001..005 + REQ-CONV-TEMPORAL-001..006.** `ContextEntryKind` enum (5 durable + `untyped`), `.kinds.yaml` sidecar, `listDurableForUser` (system+user merge, user wins, filtered to DURABLE_KINDS), `strict_durable_kinds` config flag, `threat-scan.ts` (`ContextStoreThreatError`), `<memory-kind-set>` LLM control tag, `TimeAnchor` discriminated union (`null`/`absolute`/`window`), `parseRecallVerdict` with future-date rejection, `isCalendarStrict` calendar validation, `localDayToUtcRange` DST-correct UTC range (Intl.DateTimeFormat binary search), `timeAnchorToFilters` pipeline translation (null→no window, legacy 14d removed), `<session-search>` any-order attr parser (`TAG_OUTER + ATTR_RE`; `after`/`before` temporal attrs; unknown/duplicate/invalid → `rejectAll`), continuation-prompt result fence label surfaces applied filters, `formatTurnTimestamp` + per-turn italic timestamp in `formatRecallReply`. 432 test files / 9590 tests passing. **Hermes P6.next complete (2026-05-05) — NL temporal precision broadening + mid-session snapshot rebuild, 16 URS requirements REQ-CONV-TEMPORAL-007..012 + REQ-CONV-MEMORY-013..022.** Chunk A: `recall-classifier.ts` extended with `<phrasing reference>` block of 10 NL relative-date forms and computed dates (`findLastWeekday`, `firstOfMonth`, `firstOfPriorMonth`); prompt-only, LLM authority unchanged, 365d cap unchanged. Chunk B: `NoActiveSessionError` + `SessionCasMismatchError` in `errors.ts`; `rebuildMemorySnapshot(ctx, opts)` on `ChatSessionStore` with double-CAS using `withMultiFileLock([index, transcript])` (alphabetical order, deadlock-safe vs `endActive`); `handleRefreshMemory` handler reuses `buildSnapshot` callback pattern from `handle-message.ts`/`handle-ask.ts`, gates `pinnedKeys` on `flush_memory_on_idle_reset`; `/refreshmemory` + `/refresh-memory` Router built-ins; always-persist policy (REQ-CONV-MEMORY-020); REQ-CONV-MEMORY-012 amended by REQ-CONV-MEMORY-022 (byte-stability holds between `/refreshmemory` events). 440 test files / 9758 tests passing. **Open-Items Cleanup Batch 2 complete (2026-05-07, `codex/batch2-chatbot-cleanup`)** — `isPasRelevant()` + tests + URS entries deleted; MODEL_SWITCH_INTENT_REGEX descoped. **Open-Items Cleanup Batch 3 complete (2026-05-07, `codex/batch3-router-recall-cleanup`)** — `/flushmemory`+`/flush-memory` Router built-ins, `SessionControlLogger` telemetry, `chat.recall.max_window_days`. 13 new URS: REQ-CONV-FLUSH-013..018, REQ-CONV-NEWCHAT-009..012, REQ-CONV-TEMPORAL-013..015. Post-merge Codex corrections applied (P2-1..P2-3, P3-4..P3-8). 467 test files / 10,324 tests passing.

Plan: `docs/superpowers/plans/2026-04-15-llm-enhancement-opportunities.md`

D5c plan: `docs/superpowers/plans/2026-04-20-d5c-per-household-governance.md`

| Chunk | Status | What it does |
|---|---|---|
| 0 | ✓ Complete | Semantics decisions + URS entries + open-items.md fix |
| A | ✓ Complete | Fix 3 ALS dispatch gaps (bootstrap:816/835/924, context.ts:60/115/163/191) + regression guard |
| B | ✓ Complete | CostTracker household dimension + reservations |
| C | ✓ Complete | HouseholdLLMLimiter + RateLimiter peek/commit + config surface + error types |
| D | ✓ Complete | Ops dashboard (/gui/llm Per-Household section + live metrics) |
| E | ✓ Complete | composeRuntime() bootstrap refactor + load-test harness |

### Open Items
See `docs/open-items.md` for all deferred phases, unfinished corrections, proposals, and accepted risks.

## AI Assistant Directives

- You are not constrained by human development timelines. **Within the scoped phase, implement the complete vertical slice; do not defer required safety, tests, or docs.**
