# Open Items

All outstanding work, deferred phases, corrections, and proposals in one place.
User manual actions are tracked separately in `user_actions.md`.

---

## Phase Sequence (ordered — read the Master Execution Order first)

### ⚠️ Master Execution Order — all pending work (2026-07-07)

**This is the canonical ordering for everything pending in this project. Read it before picking any next phase.** The numbered table below covers only the chatbot-primary / T7 / HTML tracks; the full pending set spans four tracks (audit remediation PP-1..PP-7 + UX Hardening live in `docs/implementation-phases.md` "Planned Phases" sections; SR/AG under Confirmed Phases below). Tracks may interleave; **within a track the order is binding, and the hard gates must never be reordered past.**

**Hard gates (violating any of these creates rework or an unreviewable design — do not skip):**

1. **SR-1 design pass before T2a (#6).** Tier A capability names, T2a `capabilities.tools[]`, and the AG-3 metadata share one manifest surface — built once or built twice.
2. **AG-3 rider lands inside T2a itself** (see the amended #6 row): per-tool risk class, `agentAllowed`, confirmation requirement, cost hint. Retrofitting risk classes onto shipped tools is the expensive path.
3. **SR-2 interface before T5.notes (#12)** so each T5 app slice migrates once (fallback: fold into T6b, accepted only if T5 starts before SR-2 is planned).
4. **AG-2 only after T3 (#9)** — shadow-mode telemetry is the evidence the loop substrate behaves. **AG-5 only after AG-2 + ≥1 month of real session traces** — the traces are the design input for the routine format; earlier is guessing.
5. **PP-1, PP-2, PP-3 complete before the repo goes public** (SEC-1's history decision is irreversible once published). **SR-3 only after PP-1..PP-7.**
6. **SR-1 Tier C (process isolation) before any public app registry ships.**
7. **PP-7 batch 4 only while the T-track remains distant** — its targets (route verification, per-purpose classifier overrides) are deleted in T6b.

**Recommended sequence by track:**

- **Track A — reliability & UX (independent; start anytime, highest near-term leverage):** UX Hardening Batches 1 → 3 → 4 → 5 (Batch 2 already shipped in the GUI UX Redesign) interleaved with **PP-1 → PP-2 → PP-3** (the publication gate trio, containing INST-1 / SEC-1 / DEP-1/2) → **PP-5** (BKP-1 backups) → **PP-4 → PP-6 → PP-7** (PP-7 batches 1–2 are free wins, anytime; batch 4 per gate 7).
- **Track B — chatbot-primary + agentic (the numbered table, with confirmed insertions):** **SR-1 design pass** → #4 T1a → #5 T1 → **#6 T2a (+AG-3 rider)** → #7 T2b → #8 T2c → #9 T3 → **AG-2 (+AG-6 GUI timeline, +AG-7 agent bucket; admin-only)** → #10 T3b → #11 T4 → **SR-2 interface** → #12–17 T5.* → #18 T6a → #19 T6b → **AG-5 routines** (per gate 4).
- **Track C — publication (after Track A completes):** **SR-3** (absorbs PP-6/DOC remainder; ships `APP_TRUST_MODEL.md` regardless of SR-1 tier progress) → **SR-4** (harness extraction — lowest urgency).
- **Track D — non-blocking, additive:** #20–25 T7 slices (activate on post-T6b recall telemetry only); #26–28 HTML-A/B/C.

*Already done from the confirmed SR/AG set: AG-1 — doctrine adopted at `docs/agentic-autonomy-doctrine.md` (includes the AG-8 standing decision: no resident light-harness agent in core).*

---

| # | Phase | Sessions | What it delivers |
|---|---|---|---|
| 1 | ~~**Settings Chunk D**~~ ✓ Complete (2026-05-08) | 1 | `/settings` Telegram command — list and set tunables inline. `handleSettings` + Router built-in + `PendingSettingsConfirmStore` + integration + persona tests. REQ-SETTINGS-009 + REQ-SETTINGS-010. |
| 2a | ~~**Persona Regression Suite Chunk A.1 — substrate**~~ ✓ Complete (2026-05-09) | 1 | `regression/` workspace, types, cache (git-blob + tier snapshot), schema-validated reads + history, budget guards (CaseBudget + RunBudget), `regression.maxRunBudgetUsd` config (REQ-REG-009), structural oracle (error-on-non-parse + calendar-strict), PersonaCase validator, fixture integrity verifier (REQ-REG-006), receipt runner against production `parseReceiptFromPhoto`. 97 unit tests. REQ-REG-001/002/004/006/008/009/010/014. |
| 2b | ~~**Persona Regression Suite Chunk A.2 — receipt cases**~~ ✓ Complete (2026-05-15, commit `1e92580`). | 1 | 5 fixtures + `buildCases()` index + `multisetRows` oracle + receipt-bucket cache salt + orchestrator dispatch arm. Operator real-LLM verification remains — see Chunk A.2 carry-forward below. |
| 3a | ~~**Persona Regression Suite Chunk B.1 — routing bucket + orchestrator + CLI**~~ ✓ Complete (2026-05-11) | 1 | LoadedCase loader, routing case-runner (food-shadow + session-control + pas), metered classifier adapters (cost via CostTracker delta), orchestrator with hard-abort RunBudget, REQ-REG-011 accuracy gate (per-input; fail+error count), CLI (`pnpm test:regression --bucket=routing --dry-run --rerun --json --help`), 36 routing cases (27 FOOD_PERSONAS generated + 3 session-control + 6 PAS), URS REQ-REG-001/002/004/006/008/009/010/011/014. 211 unit tests; full root suite (10,890) green. |
| 3b | ~~**Persona Regression Suite Chunk B.2 — GUI scaffolding**~~ ✓ Complete (2026-05-11) | 1 | `/gui/regression` admin-only page (REQ-REG-007, REQ-REG-013), per-case model IDs / cost / timestamp; token counts as `—` (carry-forward — see below); CLI `--list` flag + `buildMetadataDeps()` for cache-key parity (Codex C1); current-key-first cache selection (Codex C2); `gate-failed` distinct from `failed` terminal state (Codex C3); fresh case discovery per page load (Codex C4); never-run drilldown with inputs+expected from `ListedCase` (Codex C5); Force fresh = expanded rerun (Codex I2); Pino-to-stderr (Codex I3); fail-closed list-mode parser (Codex I4); strict cache-validator mirror (Codex I5); 302-redirect on unauth (Codex I6); SSE → server-rendered row via htmx fetch (Codex I7); Fastify hijack-pattern SSE helper (Codex I8); fake-CLI integration + real --list smoke (Codex I9); coverage-changed banner (REQ-REG-003 promoted); history view (REQ-REG-015 new); cancel-run (REQ-REG-016 new); estimate (REQ-REG-017 new). |
| 3c | ~~**Persona Regression Suite Chunk C — recall bucket + rubric oracle + chatbot bucket**~~ ✓ Implementation + correction phase + Codex P0/P1 follow-ups complete (2026-05-12) | 1 | 25 recall cases (REQ-REG-005), rubric oracle (`oracles/rubric.ts`), 10 chatbot cases migrated from v0 corpus, chatbot env factory (REQ-REG-006/012), `--model-matrix` + `--judge-model` CLI flags actually swap model in LLM service (correction Batch 0). **Latest post-correction evidence (Codex 2026-05-12, `--no-cache` against local Gemma): routing 32/36 = 89.62% accuracy (food-shadow 18 pass + 9 honest label-mismatch; 0 `parse-failed`), recall 24/25 (1 residual `parse-failed` on `recall-true-yesterday`), chatbot 3/10 pass + 7 error (Gemma 26b judge token-repetition; framework correctly refuses to grade malformed JSON). `modelIds.standard: gemma4:26b` verified end-to-end on 10/10 chatbot cache files. Regression workspace ~346 tests.** See `docs/superpowers/plans/findings/2026-05-11-chunk-c-local-model-verification.md`. Open follow-ups (each tracked separately in Proposals below): recall `recall-true-yesterday` parse-failed residual; chatbot Gemma 26b token-repetition; chatbot `chatbot-costco-21-items` handler-defect (only returns "1 updated, 1 new" diff view, not full 21-item list); 9 food-shadow label-mismatch failures under Gemma 4 e4b; chatbot env `captureHandler`/`endActiveSession` Router instrumentation. |
| 4 | **T1a — Dependency spike** | 1 | Lock Vercel AI SDK v6 + provider versions; per-model tool-call support matrix |
| 5 | **T1 — LLMService substrate** | 1 | `completeWithTools` + owned loop wrapper + per-step cost reservation + capability flags |
| 6 | **T2a — Tool foundation** | 1 | `types/tool.ts`, manifest `capabilities.tools[]`, ToolRegistry, ToolPolicy, install/load validation. **AG-3 rider (confirmed 2026-07-07):** per-tool agent metadata — risk class (`read`/`write`/`external-effect`), `agentAllowed`, confirmation requirement, cost hint — designed into the schema now even though agent mode ships later; capability naming co-designed with SR-1 Tier A (SR-1 design pass precedes this phase). **Implementation plan (Fable-authored + Codex-reviewed 2026-07-08): `docs/superpowers/specs/2026-07-08-t2a-tool-registry-plan.md`** — 10-task TDD breakdown over SR-1's ToolDef schema + AG-3 rider, ToolPolicy (`requiresCapabilities ⊆ declared`, reject async schemas, confirmation-not-loosenable on write/external-effect), install+load enforcement, and the SR-1 §8-Q1 services↔capabilities consistency validator; legacy apps stay byte-identical. |
| 7 | **T2b — Security surface** | 1 | PendingToolConfirmationStore, `tc:yes`/`tc:no` callbacks in compose-runtime, AttachmentStore |
| 8 | **T2c — Execution surface** | 1 | tool-trace NDJSON, tool-result-fence, PII redactor, preselection, end-to-end round-trip |
| 9 | **T3 — Shadow mode** | 1–2 | Chatbot dry-run shadow, persona corpus, sample-rate + cost cap + kill switch |
| 10 | **T3b — Per-user beta** | 1 | `routing.primary: chatbot` for opt-in users after ≥1 month T3 telemetry |
| 11 | **T4 — Pseudo-tool migration** | 1 | `<config-set>`, `<memory-kind-set>`, `<session-search>`, `<switch-model>`, `<model-journal>` → native tools |
| 12 | **T5.notes** | 1 | Notes app NL intents → tools |
| 13 | **T5.food.recipes** | 1 | Recipe intents → tools |
| 14 | **T5.food.grocery** | 1 | Grocery intents → tools |
| 15 | **T5.food.pantry** | 1 | Pantry intents → tools |
| 16 | **T5.food.health** | 1 | Health intents → tools |
| 17 | **T5.food.receipts** | 1 | Receipt intents → tools (highest blast radius, last) |
| 18 | **T6a — Canary flip** | 1 | Default `routing.primary: chatbot` for all users; ≥2 week canary + SLOs |
| 19 | **T6b — Cleanup** | 1 | Delete IntentClassifier, RouteVerifier, grey-zone, shadow-classifier internals |
| 20 | **T7.1** | 1 | EmbeddingService + sqlite-vec + platform spike (non-blocking, activate when telemetry justifies) |
| 21 | **T7.2** | 1 | Hybrid FTS5 + vector recall via RRF |
| 22 | **T7.3** | 1 | Optional LLM rerank stage |
| 23 | **T7.4** | 1 | Optional HyDE query expansion |
| 24 | **T7.5** | 1 | ContextStore supersession + governance |
| 25 | **T7.6** | 1 | PII redaction for embeddings + tool traces |
| 26 | **HTML-A — HTML artifact convention** | 1 | New `docs/artifacts/` tree (committed `templates/` + policy `README.md`; git-ignored `generated/`+`experimental/`) and one canonical self-contained HTML template. `pas-html-artifacts` plugin skill deferred within HTML-A until the convention is in use. Proposed URS area `ARTIFACT`. No deps. |
| 27 | **HTML-B — URS/status dashboard generator** | 1–2 | `pnpm docs:dashboard` parses the `### REQ-*` blocks in `urs.md` (+ `implementation-phases.md`, `open-items.md`) → one self-contained interactive HTML requirements table in `docs/artifacts/generated/`. Provenance banner; `escapeHtml`/`safeJsonForScript` HTML-safety; parse-diagnostics report; read-only; no CI gate. Proposed URS area `DASHBOARD`. Depends on HTML-A. |
| 28 | **HTML-C — User-facing static doc site** | 1 | `pnpm docs:site` renders `README.md` + `docs/USER_GUIDE.md` (user-facing) → self-contained static HTML site; `docs/OPERATIONS.md`/`DEPLOYMENT.md`/`INSTALL_LLAMA_CPP.md` only via explicit opt-in + content-sensitivity review. Reuses HTML-B's HTML shell; raw-HTML policy decided at impl time. Proposed URS area `DOCSITE`. Depends on HTML-B. |

*T7 slices (#20–25) are a non-blocking epic — begin when post-T6b recall telemetry shows FTS5 alone is insufficient.*
*HTML phases (open-items sequence #26–28) are a non-blocking additive track, independent of the chatbot-primary / T7 work. Each is purely additive — no existing `.md` converted, no existing runtime-coupled file or skill touched, no CI gate added. The `#` is an open-items sequence position, unrelated to the historical "Phase 26/27/28" entries in `docs/implementation-phases.md`.*

*SR/AG insertion points (confirmed 2026-07-07, not renumbered into the table — see the SR-1..SR-4 and AG entries under Confirmed Phases):* **SR-1** design pass before #6 (T2a); **AG-1** ✓ done (doctrine adopted, `docs/agentic-autonomy-doctrine.md`) and **AG-3** rides #6 itself (see the amended row); **SR-2** interface before #12 (T5.notes); **AG-2/AG-6/AG-7** (bounded agent sessions + GUI timeline + `agent` regression bucket) after #9 (T3), admin-only first; **AG-5** (routine distillation) after AG-2 + ≥1 month of session traces; **SR-3** after PP-1..PP-7 (tracked in `docs/implementation-phases.md` planned section, not this table); **SR-4** after SR-3.*

---

## Confirmed Phases (need planning before implementation)

These are greenlit but not yet planned. Each needs a spec/plan before coding.

- **SR-1 — App isolation & shared-app trust model (confirmed 2026-07-07, from Fable strategic review)** — Turn the SEC-4 "container isolation" one-liner into a decided, documented, enforced trust model. Tier A: capability-scoped service injection (reply-scoped messenger default with `telegram:any-user` as an explicit manifest capability, per-app outbound send rate limits, least-privilege inventory of every `CoreServices` member — today `telegram.send(anyUserId)` lets any app message any user). Tier B: runtime ESM loader-hook import enforcement (replaces the regex analyzer's enforcement role; analyzer stays as install-time UX) + `process.env` scrubbing before app load. Tier C: per-app process isolation — **required before a public app registry ships**. Deliverable in all cases: `docs/APP_TRUST_MODEL.md` promoted from `app-sharing-vision.md`'s "What PAS Does NOT Enforce" section. Detailed design: `docs/superpowers/plans/2026-07-07-fable-strategic-review.md` §SR-1. Planned-phase prose: `docs/implementation-phases.md` "Planned Phases — Strategic Review & Agentic Autonomy". **Sequencing: design pass before T2a** (shared manifest capability surface — capability names co-designed with AG-3). Proposed URS area `REQ-ISO-*`.
- **SR-2 — Channel abstraction seam (confirmed 2026-07-07, from Fable strategic review)** — `ChannelAdapter` interface (send/sendRich/edit + capability descriptor: `supportsButtons`, `maxMessageLength`, markup dialect) + channel-neutral inbound context with a `channel: { id, native }` escape hatch; `TelegramChannelAdapter` as the only implementation, byte-identical behavior verified by the existing suite; `MessageContext` kept as a compatibility alias; 4000-char split + Markdown escaping move behind the capability descriptor (BufferingTelegramProxy becomes channel-generic with a Telegram policy object). **Non-goals: no second channel, no GUI-chat channel, no change to Telegram-id-based GUI identity.** Detailed design: strategic-review doc §SR-2. **Sequencing: interface lands before T5.notes** (phase-sequence #12) so each T5 slice migrates its app once; fallback is folding into T6b. Proposed URS area `REQ-CHANNEL-*`.
- **SR-3 — Open-source publication cut (confirmed 2026-07-07, from Fable strategic review)** — Everything between "PP-1..PP-7 done" and a public repo a stranger succeeds with: git-history/secret audit (gitleaks + manual fixture review; decide squash-republish vs. full history), license decision (PUB-2, operator), README pitch + quickstart **verified on a machine that has never run PAS** and turned into a scripted fresh-install test CI runs, SECURITY.md, CONTRIBUTING.md, CoreServices API stability statement (after SR-1 Tier A changes service surfaces), internal-docs hygiene (superpowers plans, "Hermes" codename never public-facing), demo recording, `docs/APP_TRUST_MODEL.md` shipped regardless of SR-1 tier progress. Absorbs the unimplemented remainder of DOC-1..10 (overlaps PP-6 — reconcile at planning time). Detailed design: strategic-review doc §SR-3. **Sequencing: after PP-1..PP-7.** Proposed URS area `REQ-PUB-*`.
- **SR-4 — Regression harness extraction (confirmed 2026-07-07, from Fable strategic review)** — Extract the persona-regression core (case schema, budget guards, cache, oracles, CLI) into a workspace package with zero `@core/*` imports (enforced like the LLM boundary), behind a `CaseRunner` + `CacheKeyContributor` seam; PAS buckets become the first adapter; repo split only when an external consumer exists. **Shares its seam with the "Regression Suite v2 — generic per-app test discovery" proposal and AG-7's `agent` bucket — design the seam once for all three.** Detailed design: strategic-review doc §SR-4. **Sequencing: after SR-3, lowest urgency.** Proposed URS area `REQ-HARNESS-*`.
- **AG — Graduated agentic autonomy (confirmed 2026-07-07, from Fable agentic-harness deep dive)** — Adopted doctrine: `docs/agentic-autonomy-doctrine.md` (AG-1; includes the AG-8 standing decision — no OpenClaw-style resident agent in core). Work items: **AG-3** (rides T2a planning — per-tool agent metadata: risk class `read`/`write`/`external-effect`, `agentAllowed`, confirmation requirement, cost hint); **AG-2** (bounded interactive agent sessions — `/agent` + escalation offer, same ToolRegistry tools, step cap + per-session budget reservation + confirmation gates + kill switch, reasoning-tier floor, admin-only first; gates on T3 shadow evidence); **AG-6** (agent-session timeline on the GUI Activity surface, folded into AG-2); **AG-7** (`agent` regression bucket asserting final data state + budget compliance, never tool-call sequence; folded into AG-2); **AG-4** (tier-gated autonomy ladder codified in config + refusal UX + model-matrix coverage); **AG-5** (routine distillation — frontier agent session's trace distilled into a reviewed markdown+YAML routine executed deterministically or on fast tier; review queue mandatory; gates on AG-2 + ≥1 month of real session traces). Background proposal agents (2026-04-15 plan) unchanged, unblocked by T2. Full analysis: `docs/superpowers/plans/2026-07-07-agentic-harness-deep-dive.md`. Proposed URS areas `REQ-AGENT-*`, `REQ-ROUTINE-*`.

- ~~**GUI UX Redesign for Nontechnical Users (2026-07-06)**~~ ✓ Complete (2026-07-06) — Task-oriented restructure of the management GUI for nontechnical users (both personas). Seven batches shipped: nav regroup + cross-cutting patterns; new Home (attention banners, plain-language metrics, Chart.js charts); report wizard; alert wizard; Household & sharing hub (guided invites); new surfaces (Conversations `/gui/sessions`, Backups, Activity, AI-usage charts); URS/docs sweep (this batch). **Absorbed UX Hardening Batch 2 (I4–I8, M3, M4, M7 — see the updated note on that entry below) and closed the D2 conversation-sessions open item (see the updated entry below).** No backend contract changes; stack unchanged (Fastify + Eta + htmx + Pico + vendored Chart.js 4.4.9); charts are declaratively registered in one file (`core/src/gui/charts/registry.ts`); zero failing tests throughout. Spec: `docs/superpowers/specs/2026-07-06-gui-ux-redesign-design.md`. Plan: `docs/superpowers/plans/2026-07-06-gui-ux-redesign.md`. Full batch-by-batch write-up: `docs/implementation-phases.md` (`## GUI UX Redesign for Nontechnical Users (2026-07-06)`). 26 new URS entries (REQ-GUI-UX-001..006, REQ-GUI-HOME-001..004, REQ-GUI-CHARTS-001..002, REQ-GUI-WIZARD-001..007, REQ-GUI-HOUSEHOLD-001..003, REQ-GUI-SURFACE-001..004). Creation of reports/alerts stayed admin-only by operator decision during the Codex plan review — see the "Member self-service report/alert creation" Proposal below.
- **UX Hardening Phase (from 2026-06-11 UX review; expanded by 2026-07-06 second pass)** — Fix 19 verified UX issues across the three user-facing surfaces, identified in a full UX review, **plus 11 second-pass findings (2026-07-06) covering onboarding/first contact, notification hygiene, and command discoverability** (findings + per-issue fix recommendations for both passes: `docs/superpowers/plans/2026-06-11-ux-review-findings-and-fix-plan.md`). Five batches, each a complete vertical slice (code + tests per `pas-testing-standards` + URS entries per `pas-urs-workflow` + traceability matrix):
  - **Batch 1 — Telegram/Router** (C1, C2, I1, I2, I3, M1, M2): silent reply loss on multi-intent buffer-flush failure; swallowed preamble failure; raw unescaped `appId` in denial messages; no typing indicator during LLM operations (overlaps Hermes P7 carry-forward UX-polish item 2 — implement once, here); vague per-segment failure apology; empty-LLM-response edge in `splitTelegramMessage`; silent photo-download failure.
  - ~~**Batch 2 — Management GUI**~~ ✓ Shipped (2026-07-06, absorbed by and delivered inside the GUI UX Redesign phase) (I4, I5, I6, I7, I8, M3, M4, M7): **absorbed by the GUI UX Redesign phase (2026-07-06, spec: `docs/superpowers/specs/2026-07-06-gui-ux-redesign-design.md`) — do not plan separately.** Original scope: session-expiry reasons unexplained on login page; unstyled plain-text htmx errors; no spinner/disabled state on long actions; rate-limit message without recovery guidance; JS-synced hidden inputs losing form state on validation re-render; raw enum labels; missing Reset confirmations; missing aria-labels. **All eight items (I4–I8, M3, M4, M7) shipped in GUI UX Redesign Batch 1** (`docs/implementation-phases.md`, commits `f0a7feb`..`df149d3`) — login reasons (I4/I7), styled error fragments (I5), loading indicators (I6), nav regroup + confirmations + aria (M3/M4/M7); I8 (lost form state) retired by the report/alert wizard hidden-field echo pattern (Batches 3–4).
  - **Batch 3 — Conversation** (I9, I10, M5, M6): proactive `source: 'app'` turns consuming the 20-turn history budget; session-search re-prompt failure discarding a valid first response; `/ask` capability discoverability in `/help`; `household-cost-cap` wording.
  - **Batch 1 amendment (D1, 2026-07-06)**: publish the per-user command catalog to Telegram's native command menu via `setMyCommands` (per-chat scopes for admin-only commands), driven by `getEffectiveCommandCatalog`.
  - **Batch 4 — Onboarding & first contact (2026-07-06 second pass: B1, B2, O1, O2 layers 1–2, O3)**: onboarding digest opt-in is write-only (`onboarding.yaml` `digestPreference` has no reader — either wire it to a per-user morning Report or drop the question); welcome message promises "set reminders" with no reminder feature; unregistered `/start` (no args) dead-ends at "not authorized" with no invite-code hint (+ deep-link in `/invite` output); food-app household guard tells the second household member to *create* a household instead of join (+ auto-join the food household on invite redemption); no self-service GUI password bootstrap (Telegram-delivered one-time login code).
  - **Batch 5 — Notification hygiene & polish (2026-07-06 second pass: N1 layer 1, N2, B3, S1)**: per-user toggles for the un-toggleable daily proactive food jobs (perishable/leftover/defrost/rating/freezer/cuisine-diversity), giving the Settings "Notifications" placeholder section its content; one-time "voting closes soon" reminder to non-voters at ~75% of the voting window; `location` manifest default "Raleigh, NC" → empty + one-time setup hint; expired multi-step-flow lookups reply with a timeout notice instead of silently falling through to the chatbot.
- **Chatbot-Primary + Native Tool Calls (T0–T7)** — Transition PAS from "deterministic router-first, chatbot-fallback" to "chatbot-primary with native LLM tool calls." Plan: `plans/how-much-does-the-curious-lake.md`. Router is thinned, not deleted — auth, invites, slash-commands, photo handoff, idle-reset hook all stay.
  - **T0** (doc-only) — README Hermes section, CLAUDE.md directive, this file updates. *(Complete, this session.)*
  - **T1a** — Dependency spike: lock Vercel AI SDK v6 exact versions, confirm tool-call API per provider, Ollama per-model support. Findings populate "Per-Model Tool-Call Support Matrix" section below.
  - **T1** — `LLMService.completeWithTools` substrate: owned loop wrapper around `ToolLoopAgent`; per-step cost reservation via `prepareStep`; `ModelCatalog` capability flags; banned-import extension.
  - **T2** — ToolRegistry + ToolPolicy + PendingToolConfirmationStore + AttachmentStore + tool trace (NDJSON sidecar) + tool-result fencing + preselection + minimal PII redaction + Telegram `tc:yes`/`tc:no` callback wiring.
  - **T3** — Chatbot shadow (dry-run only): `routing.primary: shadow_chatbot`; `chatbot_shadow_sample_rate`, daily/monthly cost cap, kill switch; labeled persona corpus.
  - **T3b** — Per-user beta: `routing.primary: chatbot` for opt-in users after ≥1 month T3 telemetry showing tool-precision ≥90%.
  - **T4** — Pseudo-tool migration: `<config-set>`, `<memory-kind-set>`, `<session-search>`, `<switch-model>`, `<model-journal>` → native tool calls in the chatbot virtual app.
  - **T5** — App migration (one sub-phase per session): T5.notes → T5.food.recipes → T5.food.grocery → T5.food.pantry → T5.food.health → T5.food.receipts.
  - **T6a** — Default flip with canary: `routing.primary` defaults to `chatbot`; ≥2 week canary + SLOs.
  - **T6b** — Cleanup after stable canary: delete IntentClassifier, RouteVerifier, grey-zone, shadow-classifier internals, parallel-mode plumbing.
  - **T7** — Non-blocking backlog epic: see "T7 — Hybrid Memory" entry below.

  **Obsoleted by this phase** (full rationale in plan §6; items marked below in their original sections):
  - Route-first allowlist expansion A.1 + A.2 → canceled in T5 (ROUTE_HANDLERS disappears)
  - Per-handler `is*Intent` classifiers → canceled in T6b
  - Route verification / grey-zone / RouteVerifier / pending-verification-store / verification-logger → canceled in T6b
  - Context-promotion → canceled in T6b
  - MODEL_SWITCH route-first conversion (7-component plan) → canceled; `<switch-model>` becomes a native tool in T4
  - Food deterministic-filter chain inversion → subsumed in T5
  - Shadow classifier production flip (`routing_primary: shadow`) → repurposed as chatbot-vs-regex shadow telemetry harness through T3–T5, retires in T6b

- **T7 — Hybrid Memory: Vector Retrieval + RRF + Governance** *(non-blocking confirmed epic; activate slices individually when post-T6 recall telemetry justifies them)*
  - **T7.1** — `EmbeddingService` (Ollama default / OpenAI-compat fallback / Transformers.js last-resort); `chat-state.db` embedding metadata table `(model_name, embedding_dim, created_at)`; `messages_vec` sqlite-vec virtual table (platform spike on Mac Mini + Windows gates schema bump).
  - **T7.2** — Hybrid recall (FTS5 + vector) via Reciprocal Rank Fusion; `chat.recall.vector_enabled` telemetry flag; `pnpm chat-recall-eval` offline corpus tool.
  - **T7.3** — Optional fast-tier LLM rerank stage (off by default, telemetry-gated).
  - **T7.4** — Optional HyDE hypothetical-document embedding for query (off by default, telemetry-gated).
  - **T7.5** — ContextStore supersession: `deprecated_at`, `superseded_by`, `superseded_at` fields; supersession audit log; `<memory-kind-set supersedes="...">` tag extension; deprecated entries excluded from `listForUser` and Layer 2 snapshot.
  - **T7.6** — PII redaction: `redactPii(text, policy)` regex + opt-in fast-tier LLM pass. Default-on for embedding inputs and tool traces; default-off for transcripts. Config: `chat.embedding.privacy_mode`, `chat.tool_trace.privacy_mode`.

- ~~**Hermes P1 Chunk C**~~ ✓ Complete (2026-04-26) — `/ask`, `/edit`, `/notes` Router built-ins; daily-notes opt-in (`chat.log_to_notes`, default OFF); `<config-set>` LLM tag (allowlist + intent-regex); `coerceUserConfigValue` shared coercion; `/help` dedup. REQ-CONV-006/007/008/009/010/016/019/020 implemented.
- ~~**Hermes P2 Chunks B–E**~~ ✓ Complete (2026-04-27) — ConversationRetrievalService full implementation. Chunk B: `ReportService.listForUser` + `AlertService.listForUser` scoped APIs. Chunk C: compose all readers + `buildContextSnapshot` with partial-failure tolerance. Chunk D: wired into `handleMessage`/`handleAsk` + persona tests. Chunk E: URS finalization + docs. REQ-CONV-RETRIEVAL-001 through 016 implemented. Spec: `docs/superpowers/specs/2026-04-27-hermes-p2-conversation-retrieval-design.md`.
- ~~**Hermes P3**~~ ✓ Complete (2026-04-27) — Session persistence: manual `/newchat` and `/reset`. `ChatSessionStore` with per-session markdown transcripts (`YYYYMMDD_HHMMSS_<8hex>`), `active-sessions.yaml` index under `withFileLock`, legacy `history.json` migration (one `source: legacy-import` session), `expectedSessionId` in-flight race guard. REQ-CONV-SESSION-001 through 014 implemented. Plan: `docs/superpowers/plans/can-you-start-on-wondrous-bentley.md`.
- ~~**Hermes P4**~~ ✓ Complete (2026-04-28) — Durable-memory snapshot + fenced recall. `MemorySnapshot` frozen at session-mint time via `ensureActiveSession` (before prompt assembly); persisted in session frontmatter (`memory_snapshot:`). `buildMemoryContextBlock` / `sanitizeContextContent` fence utility. Layer 2 snapshot in prompt before per-turn context; Layer 4 fenced wrapper for recalled `searchData` results. Per-turn ContextStore re-injection removed from `gatherContext`. REQ-CONV-MEMORY-001 through 012 implemented. Spec: `docs/superpowers/specs/2026-04-28-hermes-p4-memory-snapshot-design.md`. Plan: `docs/superpowers/plans/can-you-start-on-shimmying-mountain.md`.
- ~~**Hermes P5 — SQLite + FTS5 transcript search**~~ ✓ Complete (2026-04-28) — Full-text search across session transcripts via derived SQLite + FTS5 index. LLM recall classifier + auto-injection into Layer 5 fenced context (`<memory-context label="recalled-session">`). Rebuild CLI + opt-in retention. REQ-CONV-SEARCH-001..014. Spec: `docs/superpowers/specs/2026-04-28-hermes-p5-transcript-search-design.md`. Tests: `transcript-recall.persona.test.ts` (16 scenarios S1–S16), `transcript-recall.integration.test.ts` (5 test cases across 3 describe blocks T1–T3).
- **Hermes P5 carry-forward — Cross-user household search** — P5 search is strictly user-scoped; user A cannot see user B's transcripts even in the same household. Cross-household search (opt-in per-household visibility) is a future phase. Accepted risk for P5.
- ~~**Hermes P5/P6 carry-forward — NL temporal precision**~~ ✓ Complete (2026-05-05, Hermes P6.next Chunk A) — Recall classifier prompt now includes a `<phrasing reference>` block with 10 NL relative-date forms and computed example dates (`findLastWeekday`, `firstOfMonth`, `firstOfPriorMonth`). LLM-authority interpretation unchanged; 365d cap unchanged. REQ-CONV-TEMPORAL-007..012. Remaining deferred: 365d cap relaxation (REQ line 19), deterministic backstop (REQ line 20).
- ~~**Hermes P6.next carry-forward — 365d recall cap relaxation**~~ ✓ Complete (2026-05-07, Batch 3 Item 3) — `chat.recall.max_window_days` config key (zod-validated, `[1, 3650]`, default 365) threads through `RecallPipelineDeps` → `ClassifyRecallDeps` → `buildClassifierPrompt(today, maxWindowDays)` → `parseRecallVerdict`. Default 365 preserved for all unconfigured installs. REQ-CONV-TEMPORAL-013..015.
- **Hermes P6.next carry-forward — Deterministic NL backstop** — P6.next extends the recall classifier prompt with ~10 NL phrase examples (prompt-only). A server-side resolver that deterministically maps phrase patterns → `TimeAnchor` without LLM would eliminate classifier variance. Requires arbitration design when resolver and LLM disagree; deferred until there is evidence the LLM-only path is insufficient.
- **Hermes P6.next carry-forward — GUI rebuild button on `/gui/context`** — `/refreshmemory` is Telegram-only. A rebuild button on the GUI ContextStore page would let non-Telegram paths trigger a snapshot rebuild. Deferred: the Telegram workflow is the primary user path.
- **Hermes P6.next carry-forward — `<rebuild-memory/>` LLM-self-issued tag** — Would allow the LLM to trigger a mid-session snapshot rebuild when it detects the user has changed preferences. Requires a trust model for LLM-initiated expensive operations; deferred.
- **Hermes P5 carry-forward — FTS5 snippet tuning** — The `snippet()` call uses 10-token window and `[`/`]` highlight markers. Window size and markers can be made configurable in a future phase.
- ~~**Hermes P5 carry-forward — LLM-visible session_search tool**~~ ✓ Complete (this phase) — `<session-search query="..."/>` pseudo-tool implemented; re-prompt driver in `handle-message.ts` + `handle-ask.ts`; gated by `session_search_tool_enabled` (default ON). REQ-CONV-TOOL-SEARCH-001..012.
- ~~**Hermes P5 carry-forward — `/recall` slash command**~~ ✓ Complete (this phase) — `/recall <query>` dispatched as Router built-in; `handleRecall` + `formatRecallReply`; term-aware FTS5 highlight stripping; current session included. REQ-CONV-RECALL-001..009.
- ~~**Hermes P7 — Auto-titling + natural-language /newchat intent**~~ ✓ Complete (2026-04-28) — Session auto-titling (Chunk A): `TitleService`, `TitleGenerator`, `AutoTitleHook`, `/title` command, `skipIfTitled` guard, Markdown-canonical + SQLite-derived writes, title sanitization. REQ-CONV-TITLE-001..008. NL /newchat classifier (Chunk B): `SessionControlClassifier` keyword pre-filter + fast-tier LLM, `PendingSessionControlStore` TTL store (5-minute expiry), grey-zone inline Telegram buttons (`sc:yes`/`sc:no`), opt-in at Router level. REQ-CONV-NEWCHAT-001..008.
- **Hermes P7 carry-forward — UX polish bucket** — Deferred from original P7 scope. Four items: (1) Streaming responses via Telegram edit-message pattern (`StreamingConfig`: 1 edit/sec, 40-char buffer, 429 fallback). (2) Typing indicator during LLM calls. (3) UTF-16-aware message truncation. (4) Clarify tool with structured choice schema. Implement as a follow-on UX phase.
- **Hermes P8 — Auto-reset, idle timeout, active-work protection** — Auto-reset after N hours idle; daily reset; pre-reset memory-save turn; active-work protection warning. Deferred from P3 per `feedback_no_autoreset_before_recall.md` (reset without recall makes chat feel more forgetful). Implement after P5 FTS5 recall is available.
- ~~**Hermes P8b — Memory flush on idle reset**~~ ✓ Complete (2026-05-04) — Per-user opt-in toggle (`flush_memory_on_idle_reset`, default OFF). On idle reset, fast-tier LLM summarizer produces a structured recap of the dying session (tail 60 turns, 12k-char transcript cap, JSON envelope, sanitization); `flushMemoryToContextStore` writes it to ContextStore under rolling key `recent-session-summary`. CAS-first ordering (endActive before summarize); 8-second `Promise.race` timeout via `AbortController`; full fail-open; disable cleanup (toggle-off deletes prior summary via both chat `<config-set>` tag and GUI). `buildMemorySnapshot` extended with `pinnedKeys` (default `['recent-session-summary']`) so the entry survives the 4,000-char budget. 12 URS requirements REQ-CONV-FLUSH-001..012. Plan: `C:\Users\matth\.claude\plans\can-you-start-planning-sorted-ullman.md`.
- **Hermes P8b carry-forward — Date-stamped multi-summary retention** — Currently a rolling single key (`recent-session-summary`) overwrites prior summaries. Strategy A (date-stamped keys) would retain N most-recent summaries across sessions.
- **Hermes P8b carry-forward — Group-chat session-key handling** — Deferred to Hermes P3 group-chat work (S8 in the persona test suite, marked `it.todo`).
- **Hermes P8b carry-forward — Summarizer telemetry** — Counters for `written`/`failed`/`skipped`/`disabled`/`timeout` outcomes + latency percentiles.
- ~~**Hermes P8b carry-forward — Mid-session `/flushmemory` slash command**~~ ✓ Complete (2026-05-07, Batch 3 Item 1) — `/flushmemory` and `/flush-memory` Router built-ins; `handleFlushMemory` with 8-second timeout + late-resolve guard; `flushMemoryToContextStore` return type widened to `{ status: 'written'; persistedLength: number } | { status: 'failed' }` for accurate reply count. REQ-CONV-FLUSH-013..018.
- **Hermes P8b carry-forward — Conditional idle-reset notice upgrade** — When `summaryStatus === 'written'`, extend P8a's inactivity notice with a "I've saved a summary for our next chat." addendum.
- **Hermes P8b carry-forward — Plumb `AbortSignal` into `LLMService.complete`** — The 8-second timeout aborts at the hook level but in-flight provider HTTP requests may keep running and bill. Requires threading `signal` through `LLMService.complete` and the provider adapters.
- ~~**Hermes P8c — Parent-session lineage**~~ ✓ Complete (2026-05-04) — When idle reset fires, successor session minted with `parent_session_id` pointing to ended session. Wired through `ensureActiveSession` + `appendExchange` cold-mint; validated via `SESSION_ID_RE`; persisted in Markdown frontmatter and SQLite index (schema v1→v2, idempotent PRAGMA-checked ALTER, btree index `sessions_parent_session`); rebuild propagates from frontmatter. REQ-CONV-LINEAGE-001..006. Post-merge Codex corrections (2026-05-04): P1 — photo post-idle-reset lineage; P2 — fail-open `appendExchange` path in `handleMessage`/`handleAsk` now forwards `parentSessionId`; P3 — `upsertSession` set-once invariant at SQLite layer (REQ-CONV-LINEAGE-007) + FTS orphan defence. Deferred carry-forwards: cross-user household lineage queries, graph-walk API, lineage-aware recall (all in Proposals section below).
- ~~**Hermes P9 — Photo Memory Bridge + Receipt Integrity**~~ ✓ Complete (2026-04-29, 9dba662; Codex corrections aab0a8f) — Photo handlers return `photoSummary`; router appends to session transcript; `formatConversationHistory` whitelist exempts photo-summary turns (2000-char, spoof-resistant); `PHOTO_SUMMARY_GUIDANCE` in chatbot prompt; `sanitizePhotoField` strips control chars + bidi (U+200B–U+2069 incl. LRI/RLI/FSI/PDI, explicit `\uXXXX` notation) + XML fence. Receipt: `isValidReceiptDate` (calendar-strict, 90-day window); date rejection warns include `userId` from requestContext; non-string dates explicitly warned; frontmatter has `date: parsed.date` (display) + `capturedAt` (sort authority); `rawExtractedDate` audit trail; price-store `updatedAt` from `capturedAt`. REQ-CONV-PHOTO-001..005, REQ-FOOD-RECEIPT-001..002. LLM Enhancement #3.A/B/C complete.
- **LLM Enhancement #3** — Entity/slot extraction: grocery items, quantities, stores, days, portions via `llm.extractStructured`. Extends Enhancement #2's classifier-only approach to structured extraction across Food handlers. Plan: `docs/superpowers/plans/2026-04-15-llm-enhancement-opportunities.md`.
- **LLM Enhancements #4–#7** — DataQuery keyword gate removal, chatbot system-data categorization, knowledge reranker, photo caption confidence routing. Same plan.
- ~~**LLM Enhancement #8 — Native tool-call infrastructure**~~ — **Promoted to blocking prerequisite T1** of the Chatbot-Primary phase (see confirmed phase entry above). The Vercel AI SDK v6 (`ai` + provider packages) is the selected substrate. `LLMService.completeWithTools` with an owned loop wrapper around `ToolLoopAgent`, per-step cost reservation via `prepareStep`, and `ModelCatalog` capability flags replace the original "add a new tag" framing entirely. No separate planning needed — T1a spike then T1 implementation are the execution path. The tag-based limitations listed in the original entry (escape edge cases, reliability ceiling, no parallel calls, prompt-section bloat) are the primary motivation for the migration.
- **Phase H12c** — Alcohol + Meal Quality Signals. Deferred pending H12a stabilization.
- **Phase 27C** — CrossAppDataService + LinkResolver: read-only cross-app file access. Deferred until a concrete use case requires it.
- **Persona Regression Suite** — Separate opt-in LLM-calling test suite (`regression/` workspace) that catches accuracy, quality, and safety regressions when swapping LLM tier models. GUI-runnable (`/gui/regression`, admin-only), cache-keyed on (test definition + coverage file hashes + model IDs), result history retained per (case × model). v1 buckets: receipt extraction (structural oracle), chatbot fallback (rubric judge), recall classifier (structural), routing/intent (structural, ≥0.95 accuracy gate). Spec: `docs/superpowers/specs/2026-05-04-persona-regression-suite-design.md`.

- ~~**Persona Regression Suite Chunk A.2 — receipt cases**~~ ✓ Complete (2026-05-15, commit `1e92580`) — see CLAUDE.md Implementation Status. 5 receipt fixtures (4 real-photographed + 1 synthetic expired-90d) emitted via `regression/src/cases/receipt/index.ts buildCases()`; structural-oracle `multisetRows` operative for row-level multi-field correlation preserving duplicate counts; receipt-runner asserts `rawExtractedDate` for rejection sidecars; receipt-bucket cache-key date+timezone salt via shared `bucketCacheSalt(bucket, tz)` (used by both `runSuite()` and `emitCaseList()` so the GUI's `currentCacheKey` agrees with the real dispatch cache file); orchestrator dispatch wired with full test coverage. Two Codex review rounds + a simplify pass applied in-session. 476 regression-workspace tests + 11,538 root tests pass; typecheck + build clean.

  **Deferred follow-on items from Chunk A.2:**
  - **Operator real-LLM verification** — the receipt bucket has been verified via dry-run (5 cases discovered, summary green) and unit-test-mocked LLMs (605 regression-workspace tests pass, of which 175 exercise the receipt bucket across both the `structural` oracle over `.expected.json` and the `transcription` oracle over `.transcription.yaml` ground truth). Live verification against the production LLMService is still pending. With production env set (same as `pnpm dev`): run `pnpm --filter @pas/regression test:regression -- --bucket=receipt`. Expected: 5 dispatches, all `verdict: 'pass'`, exit 0, run cost ~$0.15. Then re-run immediately and confirm every result has `source: 'cached'` and zero LLM calls. Finally, edit any sidecar (e.g. bump `costco-long.expected.json`'s `total` by $0.01) and confirm only that case re-dispatches; revert. **Editing a `.transcription.yaml` fixture requires regenerating the matching `.sha256` sidecar** (`shasum -a 256 <slug>.transcription.yaml | awk '{print $1}' > <slug>.transcription.sha256`) — otherwise the loader fails closed with `verdict: 'error'`. Editing the SHA file alone is the documented test path for verifying the fail-closed branch. If step 1 yields any `verdict: 'fail'`, investigate `actuals[]` vs the `.true.md` / `.transcription.yaml` ground truth — the 4 real-receipt sidecars were hand-verified by the operator on 2026-05-15, so a mismatch points at a parser regression or a sidecar correction, not a sloppy ground truth.
  - **Future-dated receipt fixture (`receipt-future-date.jpg`)** — operator deferred 2026-05-15 (real-world risk negligible). The expired-90d branch of `isValidReceiptDate` is covered by the synthetic fixture; the future-date branch is symmetric (same rejection path, opposite side of the inequality). Revisit only if real-world receipts ever exhibit clock-skew bugs.
  - **Cropped-fixture re-photographing** — `trader-joes-long.jpg` and `trader-joes-short.jpg` are cropped above the printed date line in the operator's view. Sidecars omit `date` so the parser's actual extraction is accepted without assertion. Operator may re-photograph with the date visible to enable date assertions on these two fixtures.
  - ~~**`AppLogger` JSDoc comment cleanup**~~ ✓ Closed (2026-05-21) — the stale "Codex 2026-05-15 #2:" session-noise prefix was removed from the `core/src/types/app-module.ts` AppLogger JSDoc in the Phase 4 micro-cleanups (commit `b8d56db`); the durable rationale for the pino object-first overload is retained.

  **Deferred minors carried from the simplify pass** (`refactor(regression-A): post-Chunk-A simplify pass`, commit `63548fe`):
  - ~~Stringly-typed verdict literals across regression tests~~ ✓ Closed (2026-05-21) — the regression **test-file** verdict literals were migrated to `VERDICT.*` constants in the Phase 4 micro-cleanups (commit `b822a60`). Note: the production-oracle follow-up is also now closed — see entry below.
  - ~~**Regression oracle verdict literals — bare strings in production code**~~ ✓ Closed (2026-05-21) — `structural.ts`, `rubric.ts`, `markdown-report.ts`, the three `core/src/gui/services/regression` aggregators, and `run-history-store.ts` (new `VERDICT_VALUES` export) migrated to `VERDICT.*`. AST guards added per workspace — the root vitest config omits `regression`, so each guards its own files: `regression/src/__tests__/verdict-literal-guard.test.ts` and `core/src/gui/services/regression/__tests__/verdict-literal-guard.test.ts`, sharing the scanner `core/src/testing/verdict-literal-scan.ts`.
  - ~~**`RunResult.tokenCounts` hardcoded to `{input: 0, output: 0}`**~~ ✓ Closed (2026-05-22) — Resolved via `CostTracker.getTokenUsageTotals()` (REQ-REG-018). All four runners (routing, recall, receipt, chatbot) now propagate real token counts: routing + recall via `MeteredError` catch, chatbot + receipt via `try/finally` deltas. `LLMService.complete()` was NOT changed — the fix belonged in `CostTracker`.
  - AJV validator memoization in `regression/src/oracles/structural.ts` — hoist receipt schema to module scope when bucket count grows beyond 1 or profiling shows compile time dominates
  - ~~`RunResult.source` set to `'cached'` after cache hit~~ ✓ Resolved in Chunk B.1 — `runSuite` overrides `source: 'cached'` on cache-hit RunResults.

- **Persona Regression Suite Chunk B.1 carry-forward — operator real-LLM verification** — Chunk B.1 shipped 2026-05-11 with `buildProductionDeps()` wired through to the same `ProviderRegistry → ModelSelector → LLMServiceImpl` stack `compose-runtime.ts` uses, and `buildDryRunDeps()` for env-free dry-runs. The CLI is functional end-to-end. **Operator verification remaining:**
  - With production env set (same as `pnpm dev`), run `pnpm test:regression -- --bucket=routing` and confirm ≥ 0.95 accuracy across food-shadow inputs (exit 0). First run populates the cache.
  - Re-run immediately and confirm every result has `source: 'cached'` and zero LLM calls.
  - Edit any covered file (e.g. add a comment to `apps/food/src/routing/shadow-classifier.ts`), re-run, and confirm only the affected cases re-dispatch.
  - If accuracy < 0.95, investigate `verdict: 'fail'` results' `actuals` to determine whether the case's expected label is wrong (fix it) or the classifier regressed (file here). Do NOT lower the threshold.
- ~~**Unified Settings Surface Chunks A + E + F**~~ ✓ Complete (2026-05-05) — `SettingsRegistry` composes from chatbot virtual manifest + installed app manifests; entries keyed by `(appId, key)`. `SettingsWriter` routes coerced writes per appId; `<config-set>` allowlist + intent gates derive from `SettingsRegistry.getNlSafeQualifiedKeys()` (REQ-SETTINGS-007). Chatbot prompt receives a per-turn `<memory-context label="settings-catalog">` block + a separate trusted `<config-set>` instruction block via new `'settings'` source category — gated by PAS classifier `settingsCandidate` flag in free-text mode, default-on in `/ask`. `adminOnly`/`dangerous`/`hidden` settings filtered from non-admin catalogs. Live per-turn reads via per-app `AppConfigService.getOverrides`. Fail-fast on invalid `nlIntentRegex`. 6 URS requirements: REQ-SETTINGS-001, 006, 007, 008, 011, 012. Spec: `docs/superpowers/specs/2026-05-04-unified-settings-surface-design.md`. Deferred chunks: see Deferred Infrastructure Work section.
- ~~**Unified Settings Surface Chunk B — GUI settings page**~~ ✓ Complete (2026-05-06) — Single `/gui/settings` page with accordion per category; per-row Reset (htmx outerHTML swap); single-form Save with PRG redirect; `SettingsWriter.validate` + `writeBatch` + `registerPostWriteHook`; `AppConfigServiceImpl.removeOverride`; `categories.ts` shared module; layout nav link; CSRF + auth + XSS escaping; concurrency-safe via `withFileLock`. 11 URS requirements: REQ-SETTINGS-002..005, 014..020. Plan: `C:\Users\matth\.claude\plans\lexical-discovering-origami.md`.
- ~~**Unified Settings Surface Chunk C — system-config-derived settings**~~ ✓ Complete (2026-05-08) — `SystemConfigWriter` + `mutatePasYaml` for atomic YAML writes with in-memory propagation; `SYSTEM_SETTING_DEFS` + `SYSTEM_KEY_RUNTIME_PATH` in `settings-metadata.ts`; system-scope defs registered via `buildSettingsRegistry`; `SettingsWriter` three-source policy (`nl`/`gui`/`admin-confirmed`); `SettingsReader` system reads + effective-default resolver (`systemConfigBackingKey`); GUI admin visibility + restart badge + dangerous-tampering check; confirm flow (`GET/POST /confirm`, `matchesDangerConfirmPhrase`); `Router.setIdleMinutes` hot-update hook; food manifest pseudo-field cleanup (`guest_profiles_info`, `schedule_overrides_info`). 16 URS requirements: REQ-SETTINGS-021..036. Plan: `can-you-start-unified-cozy-clarke.md`.
- **Unified Settings Surface Chunk C.next — additional system settings** — Surface `defaults.timezone`, `defaults.log_level`, `backup.{enabled,path,schedule,retentionCount}`, `llm.safeguards.*`. These reuse the SystemConfigWriter + GUI infrastructure from Chunk C and only add new `SYSTEM_SETTING_DEFS` entries plus per-key restart-required flags. Deferred from Chunk C.
- **Unified Settings Surface Chunk D — /settings Telegram command** — Telegram `/settings` command that lists and sets tunables inline, without requiring the GUI. Deferred from Chunks A+E+F; implement as a follow-on session.
- ~~**Deterministic-filter chain inversion (food handleMessage) — standalone phase**~~ — Subsumed by chatbot-primary T5. When food's NL intents are migrated to native tools (T5.food.*), the regex-gate cascade in `handleMessage` is retired entirely. The motivating problem (too-eager regex matching, canned-reply black holes) evaporates when `handleMessage` returns `{handled: false}` for all NL intents and the chatbot tool loop takes over.
- **User Identity Clarity + Chatbot Command Awareness (2026-05-18)** — **Merged; W1/W2 traceability backfill closed 2026-05-21.** W1 merged 2026-05-18 (PR #35), W2 merged 2026-05-18 (PR #34); post-merge Codex review corrections applied on branch `worktree-w2-codex-review-fixes` (not yet merged). Two-workstream phase. W1 delivered a single `getEffectiveCommandCatalog(userId)` source of truth feeding `/help`, sandboxed system-prompt catalog (per-entry + total length caps, control-char + fence-escape sanitization, service-availability gating), build-failing doc-coverage test with structured allowlist (missing-fields / orphan / expired / malformed-root gates), and boot-time soft warning; backfilled help docs for every conversation command, `/invite`, echo, and notes; resolved `/notes` shadowing (Notes app `/notes` → `/listnotes`); fixed `auto_detect_pas` resolver to honor the manifest default derived from `CONVERSATION_USER_CONFIG`. W2 surfaces `user.name` in the operator GUI with numeric id only in `<small>` on admin debug tables; login accepts display name OR numeric Telegram id with resolve-then-rate-limit on canonical id and IP-spray throttling preserved; globally-unique names enforced via a shared `DISPLAY_NAME_LOCK_KEY` between `createInvite` and `registerUser` with active-only invite semantics, clock injection for tests, `scanForDuplicateNames` boot helper that flips `loginByNameAllowed`. Spec: `docs/superpowers/specs/2026-05-18-user-identity-and-invite-discoverability-design.md`. Plan: `docs/superpowers/plans/2026-05-18-user-identity-and-invite-discoverability.md`. **Post-merge traceability backfill complete (2026-05-21):** W1 `implementation-phases.md` section added; `REQ-CHATBOT-CATALOG-001..007` URS entries + matrix rows added; W2 `REQ-USER-009..012` confirmed canonical (no `REQ-USER-IDENTITY-*` namespace). See the closed entry under "Unfinished Corrections" below.
- **HTML complementary documentation layer (HTML-A/B/C — open-items sequence #26–28)** — Adopt HTML as a generated/derived OUTPUT format alongside Markdown. Markdown stays the source of truth; the existing `.md` corpus is NOT converted. Three additive phases; none modify existing runtime-coupled files (existing skills, AppKnowledgeBase-indexed docs, and the `check-deferred-work.sh` hook are all untouched — HTML-A *creates a new* plugin skill, which is permitted). No CI/build gate is added; generators run on-demand only. All generated HTML and templates must be self-contained (inline CSS/JS, no CDN), carry a provenance banner (generation time + source files/hashes + a "Markdown is the source of truth" notice), be HTML-safe (`escapeHtml` + `safeJsonForScript` from `core/src/utils/escape-html.ts`, a CSP `<meta>`, XSS tests), and be keyboard-accessible + offline-readable. Origin: Claude blog article on HTML artifacts. URS area codes are *proposed* — registered into `docs/urs.md` only by the implementing phase. Each phase needs its own spec/plan before coding.
  - **HTML-A — HTML artifact convention** — new `docs/artifacts/` directory: committed `templates/` subdir + a policy `README.md`; git-ignored `generated/` and `experimental/` subdirs; one canonical self-contained HTML template. A `pas-html-artifacts` plugin skill is a deferred follow-on within HTML-A, added once the convention is in real use. Proposed URS area `ARTIFACT` (documentation-only). No dependencies.
  - **HTML-B — URS / status dashboard generator** — `pnpm docs:dashboard` parses the `### REQ-*` requirement blocks in `docs/urs.md` (Phase treated as an opaque string, Status as an enum-with-fallback), cross-referenced against `implementation-phases.md` and `open-items.md`, and emits one self-contained interactive HTML requirements table into `docs/artifacts/generated/`. The traceability-matrix table is a secondary cross-check only; the parser emits a diagnostics report listing any REQ block it could not fully parse (no silent failure). Read-only — the first programmatic URS reader; no CI gate. Prefer zero new deps; a small justified dev dep only if it materially reduces parsing/escaping risk. Proposed URS area `DASHBOARD`. Depends on HTML-A for the artifact home.
  - **HTML-C — user-facing static HTML site** — `pnpm docs:site` renders the user-facing docs (`README.md`, `docs/USER_GUIDE.md`) into a self-contained static site under `docs/artifacts/generated/`. Operator docs (`docs/OPERATIONS.md`, `docs/DEPLOYMENT.md`, `docs/INSTALL_LLAMA_CPP.md`) are included only via an explicit opt-in flag plus a pre-share content-sensitivity review — they may contain operational detail not intended for broad sharing. Reuses HTML-B's HTML shell; the raw-embedded-HTML policy (escape / strip / allowlist) is decided and recorded at implementation time. Proposed URS area `DOCSITE`. Depends on HTML-B.

---

## Planned Review Work

Cross-cutting review work that should happen in staged sessions before any cleanup or backfill implementation begins.

- **Staged test/spec coverage review** — Review-only effort to assess whether repo-local tests cover intended behavior, use meaningful assertions, avoid brittle or over-mocked patterns, and identify obsolete or superseded tests. Run this across multiple sessions by stage using `docs/test-review-roadmap.md`.
- **Pre-open-source audit roadmap — COMPLETE (2026-07-06)** — All six areas of the roadmap in `docs/superpowers/plans/2026-06-11-ux-review-findings-and-fix-plan.md` ("Audit Roadmap" section) are audited: area 1 (security & trust boundary) — SEC-1..SEC-5 (Fourth-Pass Review); area 2 (fresh-install reality check) — INST-1..INST-8 (Fifth-Pass Review; a true clean-VM run remains a worthwhile one-hour follow-up after INST-1..INST-3 land, since the pass traced code rather than machine state); area 3 (privacy/data-flow) — PRIV-1..PRIV-4 (Sixth-Pass Review, includes the full verified data-flow map ready to lift into `docs/PRIVACY.md`); area 4 (backup/restore drill) — BKP-1..BKP-6 (Seventh-Pass Review; restore rehearsed end-to-end successfully, incl. worst-case derived-index rebuild); area 5 (long-horizon resources) — RES-1..RES-7 (Eighth-Pass Review; measured 97-day growth rates — re-measure the inventory table after a year of multi-user operation); area 6 (dependency/supply-chain) — DEP-1..DEP-5 (Ninth-Pass Review). All actionables tracked below. Highest-leverage fixes across the whole series: INST-1 (fresh-install boot crash), SEC-1 (PII scrub + history decision), DEP-1/DEP-2 (dependency update + CI), BKP-1 (enable backups in production).

---

## Deferred Infrastructure Work

Confirmed gaps that need to be addressed; timing depends on which phase picks them up.

- **Receipt bucket — 4 of 5 cases fail on line-item extraction (found 2026-09-01, needs an owner)** — `receipt-costco-long`, `receipt-trader-joes-correction`, `receipt-trader-joes-long` and `receipt-trader-joes-short` all fail with `Missing rows at lineItems` against the operator-authored `.transcription.yaml` ground truth, running `claude-sonnet-4-6` at standard tier (5 cases: 1 pass / 4 fail / 0 error). **This is pre-existing and unrelated to the 2026-09-01 Regression Run Unblock phase**, on three independent lines of evidence: (a) `trader-joes-long` and `trader-joes-short` have no `date:` field in their sidecars, so `a37462d`'s `buildExpectation` date-window change cannot reach them, yet they fail identically to the two date-bearing cases; (b) `receipt-expired-90d` — the case that directly exercises the date-rot fix — passes, and the expected `Receipt date failed sanity check; falling back to today` warning fires; (c) it is not the truncation defect class either, since `apps/food/src/services/receipt-parser.ts:250,269` already uses `maxTokens: 8192` with a `finishReason === 'length'` continuation call from Receipt Parser Robustness PR1. The likely reading is a genuine extraction-quality gap against ground truth of 23–26 line items — exactly what the PR2 transcription oracle was built to surface. Next step is to diff the model's `lineItems` against the sidecar for one case and decide whether the gap is model capability, prompt, or image resolution.
- **Settings Chunk G — `/gui/credentials` page** — Secrets that don't belong on the Settings page (USDA FDC API key, n8n dispatch URL, webhook secrets + HMAC signing keys) need a dedicated `/gui/credentials` admin-only page with masked values and per-credential revoke/update. Deferred from the Unified Settings Surface phase; implement as its own focused session after Chunks A–F ship. (Note: Chunks A+E+F shipped 2026-05-05; Chunks B/C complete; Chunk D complete 2026-05-08.)
- **Rename `WriteSource = 'gui'` → `'authorized-form'`** — The `'gui'` write source is also used for Telegram `/settings` non-dangerous writes (Settings Chunk D). The name `'gui'` is misleading now that two surfaces use it. A future cleanup should rename it to `'authorized-form'` across `SettingsWriter`, `handle-settings.ts`, `/gui/settings` route, and all tests. Low urgency; purely cosmetic correctness.
- **Telegram `/settings`: rate-limit confirm attempts** — The dangerous-setting confirm flow is currently only TTL-limited (60s window, single-use on success). A brute-force attacker could attempt many wrong phrases within the 60s window. A future improvement should add a per-user attempt counter (e.g. max 5 tries per pending entry) so the entry is invalidated after repeated mismatches. Accepted risk for now given the requirement for admin isAdmin status and the 60s TTL.
- **Scheduler cron editing** — `PUT /gui/scheduler/:appId/:jobId` endpoint needed before the food manifest's "Edit cron via GUI Scheduler" claim is true. The Unified Settings Surface phase corrects the manifest text only; building the actual edit endpoint is deferred. Scope: `/gui/scheduler` route + eta view update + AppConfig cron-override persistence.
- **D5a §1 — `forShared(scope)` path segment** — `forShared()` ignores its scope argument. Fixing requires migrating ~30+ food callsites + existing shared files. Target: D5c or later. Search `forShared(` in `core/` and `apps/`.
- **D5a §4 — Collaboration space UX** — `kind: 'collaboration'` is fully modeled but no production code creates one. Add `SpaceService.createCollaboration()`, admin command `/collab create`, GUI screen. Do NOT weaken isolation tests. Target: post-D5c.
- **One-off task user scope** — `OneOffTask` has no `user_scope` field; all tasks run as `userScope: 'system'`. Apps needing per-user one-off tasks need the schema extended + bootstrap wiring.
- **Open-source security pre-publication work (2026-07-06 fourth-pass review, SEC-1..SEC-5)** — Actionables from the security & trust-boundary audit; full findings in `docs/superpowers/plans/2026-06-11-ux-review-findings-and-fix-plan.md` (Fourth-Pass Review). Verified clean: `data/`/`.env`/`config/pas.yaml` never committed across all 1,241 commits; no token-shaped strings anywhere in history; all tested posture claims (API/GUI auth, webhook secret, callback gating, path handling) hold. Remaining work, in sequence: (SEC-1a, now) scrub the operator's real Telegram ID from the 8 tracked files (4 tests: `auth-set-password`, `migration-backup`, `invite-name-validation`, `user-manager`; 4 spec/plan docs) and sweep `docs/superpowers/` for real household details; (SEC-5, now, docs-only) DEPLOYMENT.md tunnel section routes the whole service publicly — document path-scoped ingress (`/webhook/*` only) as the default with full exposure + Cloudflare Access as explicit opt-in; (SEC-1b, blocking publication, operator decision) history strategy — fresh-history public repo (recommended) vs `git filter-repo` vs accept-ID-in-history; (SEC-2, blocking publication) gitleaks-style secret-scan gate in the pre-push hook chain + CI; (SEC-3, blocking publication) `install-app` CLI confirm step gets a plain-language trust warning — the permission summary reads as an enforced sandbox but in-process apps have full `process.env`/fs/network access regardless of declarations; mirror the warning in the install docs; (SEC-4, before promoting third-party installs) re-scope the Container isolation proposal trigger — see amended entry under Proposals.
- **Fresh-install / first-run fixes (2026-07-06 fifth-pass review, INST-1..INST-8)** — Actionables from the fresh-install reality check; full findings in `docs/superpowers/plans/2026-06-11-ux-review-findings-and-fix-plan.md` (Fifth-Pass Review). **(INST-1, bug, highest-leverage pre-publication item — do first):** fresh install crashes at first boot (reproduced with verbatim example config) — the household migration's fresh-install branch (`core/src/services/household/migration.ts:194-202`) writes only the marker, never adds `household_id` to `pas.yaml` nor creates `households.yaml`, so the Phase-3 strict config load throws `ConfigValidationError` and its "run the migration" advice is marker-blocked; fix the fresh branch to do the full bootstrap (default household + config rewrite), add `household_id: default` to `pas.yaml.example` + DEPLOYMENT.md minimal config, and add a first-boot integration test from example config + empty data dir. **(INST-2):** documented native entrypoint `node dist/core/src/main.js` doesn't exist (DEPLOYMENT.md:96, launchd plist DEPLOYMENT.md:230 — which also names a stale repo dir `personal-assistant` — and OPERATIONS.md:121; real entrypoint `core/dist/bootstrap.js`); native production is broken anyway because `pnpm build` doesn't copy `gui/views`/`gui/public` into dist (Dockerfile hand-copies them) — add a build asset-copy step or declare native-prod unsupported and make launchd wrap `pnpm dev`. **(INST-3, docs):** DEPLOYMENT.md:201 sets `WEBHOOK_URL` without the `/webhook/telegram` path → webhook registers at `/`, every update 404s, bot silently dead; fix alongside the SEC-5 rewrite of the same section. **(INST-4):** README's Docker+Cloudflare-Tunnel path can't work — no cloudflared service in compose and `CLOUDFLARE_TUNNEL_TOKEN` is parsed into config but consumed by nothing; either add a tunnel service that uses it or rewrite the section and remove the dead env var. **(INST-5):** compose hard-couples every install to Ollama (`depends_on` + unconditional `OLLAMA_URL` override) and ships the model volume empty with no pull step documented — profile-gate ollama, respect `.env`, document `ollama pull`. **(INST-6, docs):** DEPLOYMENT.md step 5 says GUI is at `http://localhost:3000` but all routes are under `/gui` (no root redirect); optionally add `GET /` → `/gui`. **(INST-7, docs):** DEPLOYMENT.md says "pnpm 9+" vs engines `>=10`; also labels the hard-required `GUI_AUTH_TOKEN` "Legacy". **(INST-8, minor):** `prepare` runs bash unconditionally — likely breaks `pnpm install` on Windows without Git Bash; guard or document. Suggested batching: INST-1 alone (code+test); INST-2/3/6/7 as one DEPLOYMENT/OPERATIONS editing session combined with SEC-5; INST-4/5 before publicizing the Docker path.
- **Privacy / data-flow statement + template cleanup (2026-07-06 sixth-pass review, PRIV-1..PRIV-4)** — Actionables from the privacy/data-flow audit; full findings incl. the complete verified data-flow map in `docs/superpowers/plans/2026-06-11-ux-review-findings-and-fix-plan.md` (Sixth-Pass Review). Verified clean: the only hardcoded external endpoint in the codebase is `api.telegram.org`; no telemetry/update-checks/analytics; webhook + n8n payloads are metadata-only; fully-local LLM mode (Ollama/llama.cpp, no cloud keys) is genuinely supported. Remaining work: **(PRIV-1, docs-only, pre-publication)** publish the audit's data-flow map as `docs/PRIVACY.md` (or a README "What leaves your machine" section) — must plainly state that anything stored in PAS data files can be recalled into LLM prompts, name the alert `webhook` action's raw-file-contents flow (up to 1MB), describe the fully-local recipe, and note Telegram as the one unavoidable cloud dependency; **(PRIV-2, docs + optional one-liner)** document that tier→provider auto-assignment decides which vendor receives conversation text (`pickFastTier` prefers Google > OpenAI > Anthropic, so adding `GOOGLE_AI_API_KEY` silently reroutes fast-tier classifier traffic incl. recalled transcript excerpts) and link each provider's data-use terms; optionally log the resolved tier→provider map at startup; **(PRIV-3, cross-ref)** the secret-redaction proposal below is the code half — until it ships, PRIV-1's statement is the mitigation; **(PRIV-4, template cleanup)** `.env.example`'s `GOOGLE_CALENDAR_*`/`OPENWEATHERMAP_API_KEY` and `pas.yaml.example`'s `food.usda_fdc_api_key` have zero consuming code (no manifest declares `external_apis`, nothing calls `services.secrets.get`) — remove or mark "reserved — not yet implemented" so the data-flow statement can be honestly exhaustive. Suggested batching: PRIV-1 + PRIV-2 docs + PRIV-4 in one writing session.
- **Dependency/supply-chain hardening (2026-07-06 ninth-pass review, DEP-1..DEP-5)** — Actionables from the dependency audit; full findings incl. the advisory table and major-version ledger in `docs/superpowers/plans/2026-06-11-ux-review-findings-and-fix-plan.md` (Ninth-Pass Review). Verified strong: single lockfile, no stray npm/yarn artifacts, no git/URL deps, 34 direct deps, pinned `packageManager` + engines, Docker `--frozen-lockfile`, pnpm 10 install-script allowlist limited to three vetted native builds. Remaining work: **(DEP-1, pre-publication, one session)** 35 known vulns as of 2026-07-06 (2 critical / 13 high / 19 moderate / 1 low) — `pnpm update -r` clears nearly all within existing caret ranges (incl. the fastify body-validation-bypass high and both `@fastify/static` GUI-surface moderates); protobufjs critical additionally needs `@google/genai` 2.x or a `pnpm.overrides` pin; finish with a full test run + clean `pnpm audit`; **(DEP-2, pre-publication, pairs with SEC-2)** no CI exists — add a GitHub Actions workflow (frozen-lockfile install, build, lint, test, `pnpm audit --prod --audit-level=high`, gitleaks); **(DEP-3, same week)** set pnpm `minimumReleaseAge` (4–7 days) and add a Renovate config (grouped weekly, majors separated) so advisory drift can't silently re-accumulate; **(DEP-4, scheduled)** major-version ledger — `@anthropic-ai/sdk` 0.78→0.110 and `@google/genai` 1→2 first, then zod 4 / better-sqlite3 12 / @fastify/view 12 / emittery 2 and the dev-toolchain majors (vitest 4, Biome 2, TS 6) as standalone sessions; **(DEP-5, no new action)** dev-tree advisories are effectively production advisories until INST-2 lands, because tsx is the current production runtime.
- **Long-horizon retention & rotation (2026-07-06 eighth-pass review, RES-1..RES-7)** — Actionables from the resource audit; full findings + the measured 97-day growth inventory in `docs/superpowers/plans/2026-06-11-ux-review-findings-and-fix-plan.md` (Eighth-Pass Review). Verified fine: no photos are ever persisted to disk; FTS index derived/pruneable; FileIndexService scoped away from `data/system`; model-journal monthly rotation works (and is the reusable pattern); P5 transcript prune + backup retention exist. Remaining work: **(RES-1, highest leverage)** production `pas.log` (`data/system/logs/`, pino file transport) has no rotation and sits inside the backup scope so every nightly tar re-archives the whole log history — add `pino-roll`-style rotation or move logs out of `data/`; **(RES-2/RES-5, one pattern)** `llm-usage.md` (~1MB/yr, read whole at boot-rebuild + per GUI view) and `change-log.jsonl` (~1MB/yr, read whole daily by the diff collector) need monthly rotation via the model-journal pattern; **(RES-4)** shared size-triggered rotate-to-archive helper for the three telemetry logs (shadow-classifier hit 6.1MB in 8 weeks while enabled; its one archive was manual) — closes the class before the next telemetry campaign; **(RES-3, post-launch)** report/alert history writes one file per run forever (~8.7k files/yr for an hourly alert) — keep-last-N with optional monthly digest; **(RES-6, low)** regression cache never evicts abandoned models' entries — age sweep or GUI clear button; **(RES-7, docs)** write the "Data growth & retention" section into OPERATIONS.md (the audit's inventory table + what's safe to prune) — fold into the same session as BKP-2.
- **Backup/restore hardening (2026-07-06 seventh-pass review, BKP-1..BKP-6)** — Actionables from the backup/restore drill; full findings + executed drill results in `docs/superpowers/plans/2026-06-11-ux-review-findings-and-fix-plan.md` (Seventh-Pass Review). Verified working end-to-end: archive → extract → symlink fidelity → SQLite integrity → worst-case derived-index rebuild from restored transcripts; vault symlinks self-heal at boot; retention rotation and partial-archive cleanup correct. Remaining work: **(BKP-1, operator, config-only, today)** the live `pas.yaml` has no `backup:` section — BackupService has never run in production; add the four-line block, and consider default-on for the open-source release; **(BKP-2, docs)** OPERATIONS.md restore procedure is incomplete — stale filename pattern, missing staging mkdir, no "what's NOT in the archive" list (`.env`, codebase, third-party apps installed into `apps/`), no post-restore `pnpm chat-index-rebuild` step, no vault-symlink note; batch with the INST-2/3/6/7 DEPLOYMENT/OPERATIONS session; **(BKP-3, small code)** `chat-index-rebuild --data <x>` without `--db` silently writes the live DB — derive the db default from `--data` or warn on mismatched roots; **(BKP-4, small code)** exempt `system-backup` from the job-failure notifier's consecutive-failure auto-disable (or surface "days since last successful backup"), and document the WAL torn-capture + GNU-tar file-changed consistency assumptions; **(BKP-5, small code)** reject/warn when `backup.path` resolves inside `dataDir` (recursive self-inclusion); **(BKP-6, verify-once)** busybox tar multi-`-C` invocation in the Alpine container — fold into the INST-5 Docker work. — Confirmed gaps blocking the stated goal of open-sourcing PAS as a base infrastructure layer with third-party apps. Full findings + per-item fixes in `docs/superpowers/plans/2026-06-11-ux-review-findings-and-fix-plan.md` (Third-Pass Review, Part A). Blockers: `@pas/core` is `private: true`/unpublished so the documented standalone-app path is impossible as written (DOC-1 — decide: publish to npm vs document fork-and-clone as the only path); no LICENSE or CONTRIBUTING.md at repo root (DOC-2); `pnpm install-app` never runs `pnpm install`/`pnpm build` but the CLI claims "dependencies installed", docs omit the post-install steps, and an app without committed `dist/` works in dev (tsx `.ts` fallback) but breaks in production (DOC-3 — includes adding the missing end-to-end install smoke test); `MANIFEST_REFERENCE.md` Complete Example + `CREATING_AN_APP.md` help example still declare `/notes`, a reserved built-in, stale since the W1 `/listnotes` rename (DOC-4). Important: `user_config.category` schema enum hard-codes `food`/`notes` so third-party apps have no legitimate category (DOC-5, schema change — cheaper before external manifests exist); no canonical out-of-tree example app repo (DOC-6); no doc-drift guards mirroring the W1 doc-coverage gate pattern (DOC-7). Minor: `splitTelegramMessage` not exported so docs teach a hand-rolled splitter (DOC-8); `subscribes[].handler` documented with two contradictory shapes (DOC-9); README doesn't route the app-developer audience above the fold (DOC-10). DOC-4/DOC-9 are pure doc fixes doable in any session; DOC-1/DOC-2 need operator decisions (license choice, publish strategy) before a phase is planned.
- **Latency quick wins — local LLM serving (2026-07-06 third-pass review, L1/L2/L4)** — Zero-quality-impact latency fixes; full analysis in `docs/superpowers/plans/2026-06-11-ux-review-findings-and-fix-plan.md` (Third-Pass Review, Part B). (L1) Ollama provider passes no `keep_alive`, so the 5-minute default unloads gemma4:31b and the first message after any lull pays a full model reload — add a `keep_alive` request option + `pas.yaml` knob; verify diagnosis first via `ollama ps` after idle. (L2) `handle-message.ts:187-206` runs the recall classifier and PAS classifier sequentially though they are independent — parallelize with `Promise.all`; confirm the serving layer executes them concurrently. (L4) Serve the fast-tier model via the existing llama.cpp provider (persistent server, prefix caching, `--parallel` slots) instead of Ollama — integration + install doc already exist. Trigger-gated follow-ons (regression evidence required, and both components die in T6b so pursue only if T-track remains distant): fast-tier route verification instead of `tier: 'standard'` (L5); per-purpose model overrides for non-routing classifiers, overlapping the existing cascading-models proposal (L3).
- **Food household ← platform household unification (2026-07-06 UX second pass, O2 layer 3)** — The food app keeps its own membership roster (`data/users/shared/food/household.yaml` + join codes via `/household`) parallel to the platform `HouseholdService` (`data/households/`, assigned at invite redemption). The two rosters can drift (e.g., a user removed from the platform household remains a food-household member with shared-scope data access) — a security-hygiene gap as well as onboarding friction. Design needed: derive food membership from `HouseholdService` (single source) vs sync-on-mutation; migration for existing `household.yaml` files. **Sequence before chatbot-primary T5.food.*** so the tool migration doesn't re-encode the parallel-roster design. Batch 4 of the UX Hardening Phase ships the tactical layers (guard-message fix + auto-join on invite redemption); this entry is the strategic unification.
- **Per-space scheduled jobs** — Space-scoped apps have no way to register scheduled jobs that run per-space. Needs scheduler support. (From `docs/superpowers/specs/2026-04-14-space-aware-food-data-design.md`.)
- **Review Phase 7 residual — broader active-space food migration** — Phase 7 only made recipe/receipt/grocery photo writes and interaction records space-aware. Follow `docs/superpowers/specs/2026-04-14-space-aware-food-data-design.md` to extend active-space behavior to pantry photos, callback-space plumbing, the major shared-data interactive message/callback flows in `apps/food/src/index.ts`, and cross-scope read/write reconciliation. Keep scheduled jobs shared-only until per-space scheduler support exists. This follow-up should add higher-level regressions proving those flows write to `spaces/<spaceId>/food/...` when a space is active and still fall back to shared when none is active. Depends in part on the separate `forShared(scope)` selector bug above.
- ~~**Route-first allowlist: 'save a recipe'**~~ — Canceled (superseded by chatbot-primary T5; the entire ROUTE_HANDLERS allowlist disappears when food NL intents migrate to native tools).
- ~~**Route-first allowlist: 'search for a recipe'**~~ — Canceled (same reason as above).
- ~~**Route-first allowlist expansion (A.2)**~~ — Canceled (superseded by chatbot-primary T5).
- ~~**Per-handler `is*Intent` classifiers**~~ — Canceled by T6b (replaced by tool descriptions and per-turn preselection intent gates during chatbot-primary T5).
- ~~**`MODEL_SWITCH_INTENT_REGEX` route-first conversion**~~ — Canceled (superseded by chatbot-primary T4). `<switch-model>` becomes a native tool call with `ToolPolicy { adminOnly: true, dangerous: true, confirmationRequired: true }` registered by the chatbot virtual app. The 7-component router plumbing (route intent signal, processModelSwitchTags in handleMessage, prompt-builder route-awareness, constant+manifest declaration, RouteInfo threading, tests, persona NL tests) is replaced by the tool declaration + ToolRegistry enforcement. *Original framing as a "straightforward follow-up" was incorrect. Codex review (2026-05-06) revealed five blockers; chatbot-primary T4 is now the cleaner path.*

  **Current behavior:**
  - `<switch-model>` tags are processed only on `/ask` (`handle-ask.ts:380` calls `processModelSwitchTags`).
  - `handle-message.ts:375` strips switch tags silently in free-text mode (no admin gate, no processing).
  - The gate inside `processModelSwitchTags` checks `MODEL_SWITCH_INTENT_REGEX` against `userMessage` only — no route-intent signal.
  - The virtual chatbot manifest declares `capabilities.messages.intents: []` (`virtual-app.ts:31`), so `ctx.route.intent` is `'ask'` for `/ask` (from `routeForCommand`) or `'chatbot'` for fallback (from `routeForFallback`) — never a model-switch intent.

  **Real design surface for end-to-end route-first acceptance:**
  1. **Router classifier-branch chatbot special case** (`router/index.ts:425`) — high-confidence chatbot match currently dispatches to the virtual app's `handleMessage` tripwire. Add: `if (match.appId === 'chatbot' && conversationService) { dispatchConversation(ctx, routeFromClassifier(...)); return; }` mirroring the verifier-confirmed branch at line 410.
  2. **Process tags in `handleMessage`** (`handle-message.ts:375`) — replace `replace(SWITCH_MODEL_TAG_REGEX, '')` with a `processModelSwitchTags` invocation; admin guard still applies.
  3. **Prompt-builder route-awareness** (`prompt-builder.ts:355-368`) — switch instructions only emit when `categories.has('llm')` (keyword categorization in `system-data.ts:19-39`). Thread `route?.intent` and append switch instructions when `route.intent === MODEL_SWITCH_INTENT`, so the LLM is even told to emit `<switch-model>` for route-only phrasings like "pick a snappier brain please."
  4. **`MODEL_SWITCH_INTENT` constant + manifest declaration** — define once (e.g. `core/src/services/conversation/control-tags.ts`), import into `virtual-app.ts capabilities.messages.intents`. Strict parity assertion in `manifest-parity.test.ts` (currently doesn't assert intents at all — see line 9) using the imported constant, so the two never drift.
  5. **Pass full `RouteInfo`, not just `routeIntent`** — match Food's `dispatchByRoute` pattern (`apps/food/src/routing/dispatch.ts:34`): accept route-first only when `appId === 'chatbot'` AND `intent === MODEL_SWITCH_INTENT` AND `source` is in TRUSTED_SOURCES AND `confidence >= MIN_INTENT_CONFIDENCE` (≥0.75). Defends against degraded/untrusted routes.
  6. **Tests at three levels:**
     - Manifest/cache test: `MODEL_SWITCH_INTENT` is in `getManifestCache().getIntents('chatbot')`.
     - Router test: a free-text message classified as chatbot+MODEL_SWITCH_INTENT dispatches via `dispatchConversation` (not `dispatchMessage`), and `ctx.route.intent` reaches the handler.
     - Handler test: a route-only phrasing produces a `<switch-model>` tag via the system prompt, that tag is processed (not stripped), and `setTierModel` is called. **Negative cases must assert `setTierModel.not.toHaveBeenCalled()`**, not just "no confirmation."
  7. **Persona NL tests** (`__tests__/natural-language.test.ts`) — only meaningful after items 1-3 are wired. Should-not-match cases must assert no side effect on `setTierModel`.

  **Decision needed before planning:** is enabling free-text model switching desired at all, or should the system stay `/ask`-only? If `/ask`-only, the right fix is to leave `MODEL_SWITCH_INTENT_REGEX` as-is (the silent-strip in `handleMessage` is already correct for that scope) and document the "/ask is the explicit channel for admin actions" invariant in URS instead of converting to route-first.

  **Size:** L (multi-component, 7+ files) once a direction is chosen.
- **Hermes P1 Chunk C residual — `/edit` LLM rate-limit parity** — `EditServiceImpl` uses `systemLlm` (system-tier guard), not `conversationLLMGuard`. Free-text and `/ask` go through the per-app conversation guard; `/edit`'s LLM proposals use the system guard instead. Not a bug, but the discrepancy could surprise operator cost-cap tuning. Rewiring `EditServiceImpl` to accept a conversation-scoped guard is a separate refactor; revisit if the discrepancy surfaces in real cost-cap behavior.
- **Enhancement #2 production flip** — *(Repurposed by chatbot-primary phase; supersedence note 2026-05-13.)* Originally: flip `routing_primary` to `shadow` in food manifest after ≥95% telemetry agreement from `pnpm analyze-shadow-log`. **Now governed by Persona Regression Suite REQ-REG-011 (≥0.95 per-input routing accuracy)** — cleared by Gemma 4 31B at 0.9811 in the 2026-05-13 sweep. The `pnpm analyze-shadow-log` telemetry is retained as a supplementary signal (cross-checks shadow vs. real user traffic when both are running), but is no longer the production-flip gate. **New path:** the food shadow classifier infrastructure (`SHADOW_HANDLERS`, `routing_primary` flag, `analyze-shadow-log` CLI) is repurposed as the chatbot-vs-regex telemetry harness during T3–T5 of the chatbot-primary phase. The food shadow log baseline (fresh since 2026-05-05 test-isolation fix) feeds into T3 persona corpus calibration. The shadow classifier infrastructure retires in T6b when the chatbot-primary canary has been stable for ≥2 weeks.
- **D2a non-target food write sites** — FileIndex enrichment was intentionally scoped to the primary food stores. The following are indexed by path/title only and will not surface well in NL queries: freezer, leftovers, waste log, budget history, quick meals, guests, family profiles, ingredient cache. Enrich in a follow-up phase if D2b NL querying reveals gaps. (See `docs/superpowers/specs/2026-04-13-d2a-file-index-foundation-design.md` line 163)
- **ContextStore writes — report LLM summaries (design follow-up)** — `ReportService.run()` produces an LLM summary at `core/src/services/reports/index.ts:366` (`this.summarize()`), saved to report-history files and sent to Telegram. After `/newchat`, transcript ageout, or in any future session, the chatbot has no durable recall of recent reports. Needs design before code: a report can have multiple recipients (configurable subscribers), so the durable entry's scope (owner only? all recipients? household?) is not obvious from `summarize()`. New writes are visible on next session or after `/refreshmemory` (via `buildMemorySnapshot`).
- **ContextStore writes — daily-diff LLM summary (design follow-up)** — `DailyDiffService.run()` at `core/src/services/daily-diff/index.ts:46-85` calls `summarizeChanges` (`:61`) and writes only to `data/system/daily-diff/{date}.md`. Same recall-gap shape as the reports case. Needs design before code: `DailyDiffService` is system-scope and `run()` takes no `userId` — decision needed on whether durable writes are per-admin (one entry per admin), per-user (each user gets their own), or system-wide via `kind: 'environment-fact'`.
- **Biome warning backlog (~2,400 warnings, 2026-05-20)** — The 2026-05-20 lint-error cleanup drove `pnpm lint` to zero errors and added enforcement gates (pre-push, pre-merge-commit, PostToolUse). ~2,400 Biome warnings remain — `noNonNullAssertion` (~1,582), `noExplicitAny` (~805), and `noUnusedVariables` (~19). All are configured `warn` by design and are non-blocking by policy. Clearing the backlog is deferred as a large standalone cleanup; it should not be mixed into feature branches.
- **Numeric recalibration of regression estimator constants (2026-05-22)** — Recompute `PER_CASE_USD_BY_BUCKET` in `core/src/gui/services/regression/estimator.ts` from observed `RunResult.tokenCounts` after a fresh full-suite run. The mechanism is ready as of 2026-05-22 (REQ-REG-018): `tokenCounts.input` and `tokenCounts.output` are populated for all four buckets, so a real sweep produces the per-case averages needed to replace the current hand-calibrated constants (routing 0.005, receipt 0.06, chatbot 0.04, recall 0.01). Only the numeric calibration is deferred; no code changes are needed until the sweep is run.
- **Per-user `routing.multi_intent_split` override (2026-05-22)** — The 2026-05-22 Chatbot Context & Routing Fixes phase shipped `routing.multi_intent_split` as a system-scoped boolean (default `true`, kill-switch via `pas.yaml`). A future enhancement would let individual users opt out of multi-intent splitting via a per-user config key (e.g., for users whose phrasing reliably trips false-positive splits). Requires extending `CONVERSATION_USER_CONFIG` + the routing call site to read the per-user override before the system default. Trigger: a user requests it OR telemetry surfaces a false-positive split rate worth tuning per-user.
- ~~**Stricter call-graph-based proactive-send guard (2026-05-22)** — The 2026-05-22 phase ships an entrypoint-scoped AST scanner (Strategy A — `apps/food/src/testing/proactive-send-scan.ts`) that flags raw `telegram.send*` inside a named set of proactive entrypoint functions plus their direct helpers. A future enhancement (Strategy B) would build a transitive call graph from each entrypoint and flag any reachable `telegram.send*`, eliminating the need to maintain the explicit helper list. Trigger: the entrypoint list grows brittle OR a real regression sneaks through a helper not listed in the explicit set.~~ ✓ **Closed (2026-05-24, Classifier + Reply-Collector + Call-Graph Guard phase)** — Strategy B `findReachableSends` (`apps/food/src/testing/proactive-send-call-graph.ts`) builds a `ts.Program`-backed function-level call graph and BFS-walks from each of the 13 `PROACTIVE_ENTRYPOINTS`, flagging any reachable `telegram.send*` (send / sendWithButtons / sendPhoto / sendOptions) outside `utils/proactive-message.ts`. Cross-file symbol resolution uses the TypeScript type checker (with alias dereferencing for ES-module imports); cycle-safe via a visited Set; the explicit "direct helpers" list is gone. `scanFoodProactiveSends` now delegates to Strategy B; `scanFoodProactiveSendsFromSources` is retained as a thin in-memory Strategy-A scanner for the guard self-test only (Codex Round 1 #16). The real sweep against `apps/food/src` returns zero hits — Strategy A's helper list was already accurate. 4 URS entries: REQ-FOOD-PROACTIVE-BRIDGE-008..011; 14 new tests across `proactive-send-call-graph.test.ts` (12) and `proactive-send-guard.test.ts` (2 rewritten).
- ~~**Reply-collector "combine multi-intent responses into one message" enhancement (2026-05-22)** — `tryMultiIntentSplit` currently dispatches each segment sequentially through `routeOneTextRequest`, so each segment's reply goes out as a separate Telegram message after a single preamble. Option B from the Task 4.3 plan would collect each segment's reply in memory and emit one combined Telegram message at the end, reducing notification noise. Requires a reply-collector seam in the router/handler boundary that all dispatch paths cooperate with (today the handlers call `telegram.send` directly). Trigger: operator finds the per-segment message stream noisy in practice.~~ ✓ **Closed (2026-05-24, Classifier + Reply-Collector + Call-Graph Guard phase)** — `BufferingTelegramProxy` wraps the inner real `TelegramService` during `tryMultiIntentSplit`; AsyncLocalStorage scope via `requestContext.replyBuffer` routes handler `services.telegram.send` through the buffer via the `ContextAwareTelegramService` wrapper; rich sends (`sendPhoto`/`sendWithButtons`/`sendOptions`) flush the buffer and pass through; `editMessage` bypasses (REQ-ROUTE-019b); final flush emits one combined message (or auto-splits at segment boundaries when total exceeds 4000 chars). Three nested `requestContext.run` sites in Router were patched to spread the outer store so the buffer survives every dispatch hop (Codex Round 1 #2). 7 URS entries: REQ-ROUTE-017, REQ-ROUTE-018, REQ-ROUTE-019, REQ-ROUTE-019b, REQ-ROUTE-020, REQ-ROUTE-021, REQ-ROUTE-022. 7 new test files; ~42 new tests across reply-buffer.test.ts, request-context-reply-buffer.test.ts, router-context-merge.test.ts, router-multi-intent-reply-buffer.test.ts, multi-intent-natural-language.persona.test.ts; plus an in-place rewrite of router-multi-intent.test.ts (Codex Round 1 #5: handlers now actually call `services.telegram.send` so combined-reply assertions are no longer vacuous).
- **Multi-intent splitting for the photo path (2026-05-22)** — `tryMultiIntentSplit` is wired only into the free-text route path (`routeMessage`). Photo messages with captions that contain multiple intents bypass the splitter entirely. Photos were explicitly out of scope for the 2026-05-22 phase. Adding photo support requires segmenting the caption while keeping the attached photo bound to (one or more) segments — non-trivial UX/contract decisions. Trigger: a multi-intent photo caption surfaces a real user complaint.
- ~~**Regression-suite end-to-end pass for the new `invite-platform` routing case (deferred from Task 3.4, 2026-05-22)**~~ ✓ **Ran 2026-05-24** via `pnpm test:regression -- --rerun=pas-invite-platform-positive --no-manifest`. Result: full routing bucket gate **PASSED** at 98.11% routing accuracy (REQ-REG-011 floor 95%) — 77 cases, 59 pass / 18 fail / 0 error, $0.14 cost, 22m wall. The new `pas-invite-platform-positive` case itself reported `verdict: fail` for all 7 inputs — the LLM-classifier's `pasRelated` output came back `false` for the platform-invite phrasings (the case's oracle asserts `pasRelated: true`). See the new accepted-risk entry below for analysis: **the deterministic router-level redirect IS proven** by `core/src/services/router/__tests__/router-verification.test.ts:'invite-platform routing — deterministic gate (Codex R2 F3)'` (commit `d83a517`); the LLM-classifier-level recognition of these phrasings as platform questions is a separate, deeper gap.

- **5-series Anthropic models have no `MODEL_PRICING` entries (2026-09-01, Regression Run Unblock)** — `core/src/services/llm/model-pricing.ts` lists only `claude-opus-4-6`, `claude-sonnet-4-6`, `claude-sonnet-4-20250514` and `claude-haiku-4-5-20251001`. `claude-opus-5`, `claude-sonnet-5`, `claude-fable-5(-1)`, `claude-opus-4-8` and `claude-opus-4-7` are all absent, so real runs log "Unknown model pricing — using conservative fallback estimate" and bill at `DEFAULT_REMOTE_PRICING`. Every cost figure reported for a 5-series run (regression `costUsd`, per-household cost caps, the AI-usage GUI) is therefore an approximation of unknown sign. Fix: add real per-1M input/output prices for the 5-series ids, alongside the `MODEL_CAPABILITIES` entries added in the same phase. Trigger: before any cost-cap tuning or budget decision that relies on 5-series spend.
- **The built-in Anthropic default model no longer exists (2026-09-01, Regression Run Unblock)** — `core/src/services/config/default-providers.ts` sets `defaultModel: 'claude-sonnet-4-20250514'`, which the 2026-09-01 live catalog probe found **404s** (as do `claude-opus-4-20250514`, `claude-3-5-sonnet-20241022`, `claude-haiku-3-5-20241022`, `claude-sonnet-4-7`). A fresh install that sets `ANTHROPIC_API_KEY` and takes the defaults therefore points at a dead model. Belongs with the PP-1 fresh-install correctness work (INST-1..INST-8): pick a currently-served default, and consider a boot-time warning when a configured model id is not in the provider's `listModels()` result.
- ~~**Two further truncation-misdiagnosis call sites, found and deliberately not fixed (2026-09-01, Regression Run Unblock)** — The phase fixed three instances of the class "a fixed `maxTokens` budget feeding a structured-output parse reports truncation as malformed output" (Ollama empty output, rubric judge, Food shadow classifier — REQ-LLM-039/040). Two more remain, both out of the phase's blast radius: (a) `core/src/services/conversation-retrieval/recall-classifier.ts:414,443` — `maxTokens: 150`, `responseFormat: 'json'`, uses `complete()` so `finishReason` is invisible; a truncated verdict degrades silently to `RECALL_SAFE_DEFAULT`, i.e. recall quietly stops working rather than reporting why; (b) `core/src/services/router/message-segmenter.ts:237` — `maxTokens: 400`, `responseFormat: 'json'`, also `complete()`; a truncated segmentation silently disables multi-intent splitting for that message. A third, `core/src/gui/services/regression/weakness-summarizer.ts:191-201` (`maxTokens: 1500`, bounded schema, admin-only surface), was judged lower risk. Fix pattern is established: switch to `completeWithMeta` and branch on `finishReason === 'length'` before reporting a parse failure. Trigger: a recall or multi-intent regression that cannot be explained by prompt quality — or fold into the next conversation-retrieval / routing phase.~~ ✓ **Closed (2026-09-02, Truncation Diagnosis + Local-Model Cost Correctness phase, commit `ddb0c3c`)** — Rather than fix the two sites individually, the classification was extracted once into `classifyStructuredOutput` (`core/src/utils/json-strip-fences.ts`) and **ten** sites migrated onto it: both deferred sites (`recall-classifier` parse-first at 150, `message-segmenter` check-length-first at 400), the lower-risk `weakness-summarizer` (1500), plus `title-generator` (60), `session-summarizer` (400), `session-control-classifier` (80), `data-query` file selection (50), `route-verifier`, and the two originally-fixed sites (`rubric.ts`, `shadow-classifier.ts`). The parse/length ordering is a required caller option with no default, because the two original sites deliberately disagree and a silent default would have regressed one of them. REQ-LLM-041; 30 helper tests + 47 per-site tests pinning call counts so retry semantics cannot drift.
- **`pas-classifier`'s `maxTokens: 10` budget is too tight for larger local models (2026-09-01, Regression Run Unblock)** — `muse-glimmer:30b-mlx` cannot answer the PAS-relevance prompt within 10 tokens even with thinking disabled. As of REQ-LLM-039 that now surfaces as a loud `LLMEmptyOutputError` instead of a silent `""` (and the classifier fails open), but the budget itself is unrevisited: it was sized for Gemma-class models emitting a single word. Fix: measure the good-path output length across the local models actually in use and raise the cap to ~3x the longest, the way `SHADOW_MAX_TOKENS` documents its 80. Trigger: an operator adopts a larger local model for the fast tier, or the fail-open rate on `pas-classifier` becomes visible in telemetry.
- **The `apps/food` `parseJsonResponse` cluster — 16 call sites, none passing `maxTokens` (2026-09-02, Truncation Diagnosis phase, needs an owner)** — `apps/food/src/services/recipe-parser.ts:119-142` (`parseJsonResponse`) throws `Could not parse ${context} response: invalid JSON from LLM.` on any parse failure. It has **19 call sites; 17 of them pass no `maxTokens` at all**, so every one of the 17 inherits the 1024-token provider default: `meal-planner.ts:184,251,288`, `hosting-planner.ts:101,157,194`, `health-correlator.ts:115`, `grocery-dedup.ts:143`, `batch-cooking.ts:79`, `cuisine-tracker.ts:52`, `pantry-matcher.ts:113`, `kid-adapter.ts:58`, the three photo parsers (`recipe-photo-parser.ts:56`, `pantry-photo-parser.ts:40`, `grocery-photo-parser.ts:73`), and `recipe-parser.ts:48,112`. (The remaining two, `receipt-parser.ts:130,278`, are already covered — Receipt Parser Robustness PR1 gave them an 8192 cap and a `finishReason` continuation.) A meal plan or hosting plan cut at 1024 tokens is reported to the user as a broken model. **This is the largest single concentration of the defect class in the repo**, and fixing `parseJsonResponse` alone is *insufficient*: it receives only `raw: string`, so it cannot see `finishReason` — every caller must move to `completeWithMeta` first, then hand the meta through. The shared `classifyStructuredOutput` helper is already exported from `@pas/core` (`./utils/json-strip-fences`) for exactly this. `apps/food/src/services/receipt-parser.ts:245-295` is the model to follow: explicit large cap, `finishReason` inspection, single-shot continuation. Trigger: the next Food-app phase, or the first user report of a truncated plan.
- **Pseudo-tool XML tags can be cut mid-tag in chat replies (2026-09-02, Truncation Diagnosis phase)** — `core/src/services/conversation/handle-message.ts:299-304,358-363` and `handle-ask.ts:308,366` cap replies at `maxTokens: 2048`. These are not JSON, so `classifyStructuredOutput` does not apply — but the replies carry pseudo-tool XML tags (`<switch-model>`, `<config-set>`, `<model-journal>`, `<session-search>`) that are extracted by regex afterwards. A reply cut mid-tag means the tag is **silently not extracted** and its raw text leaks into the user-visible reply: the user sees a half-written XML fragment and the requested action never happens. Different family, same root cause (a fixed cap feeding a structured extraction). Fix shape: inspect `finishReason` on those calls and, at `'length'`, either strip trailing partial-tag text or tell the user the reply was cut. Trigger: a user reports a stray `<` fragment in a chatbot reply, or the next conversation-service phase.
- **Test files are excluded from every tsconfig in the repo, so nothing typechecks them (2026-09-02, Truncation Diagnosis phase — significant, needs an owner)** — A throwaway typecheck run during commit `ddb0c3c` surfaced **~1628 pre-existing type errors in test files**. `pnpm build` and every editor typecheck skip them, so test code has no compile-time safety at all. This is not cosmetic: it is why the `as unknown as` LLM stub casts fixed in that commit were invisible. One of them (`regression/src/__tests__/dispatch.test.ts:272`) called a method the stub did not have, threw a `TypeError` **inside** a `try/catch` that swallowed it, returned the safe default, and made the test assert the wrong intent while still passing. Fix shape: a separate `tsconfig.test.json` including `**/__tests__/**` and a `pnpm typecheck:tests` script, then burn the backlog down (almost all of it is expected to be `noImplicitAny` on mock parameters and missing members on hand-rolled stubs) before wiring it into the push gate. Trigger: needs an owner — this is a repo-health item, not a feature.
- **`openai-compatible-provider.ts` sends `max_tokens`, which OpenAI reasoning models reject (2026-09-02, Truncation Diagnosis phase)** — OpenAI's own reasoning models (o-series, GPT-5) reject `max_tokens` and require `max_completion_tokens`. **Not currently reachable:** no `openai-compatible` provider is configured in this install, and every other vendor exposing an OpenAI-shaped endpoint (Ollama, llama.cpp, LM Studio, vLLM, together, groq) still accepts `max_tokens`. The right home for the fix is `core/src/services/llm/model-capabilities.ts` — a `usesMaxCompletionTokens` flag alongside `supportsTemperature`, plus a self-healing single retry mirroring `BaseProvider.completeWithTemperatureFallback`. Trigger: an operator points the `openai-compatible` provider at api.openai.com with an o-series or GPT-5 model.
- **`reasoning_content` detection covers one field name on one path (2026-09-02, Truncation Diagnosis phase)** — `1fcc3e1` reads `message.reasoning_content` on the non-streaming OpenAI-compatible response. Some servers instead use `message.reasoning`, and streaming responses nest the field under `delta`. The consequence of a miss is only a less informative diagnostic — the `LLMEmptyOutputError` still fires on empty + `length`, since the guard is keyed on empty output rather than on a reasoning block being present — but the operator loses the "the model spent N characters thinking" evidence that makes the cause obvious. **Unverified against a live server**: the field names above come from vendor documentation, not from a probe — no OpenAI-compatible local server was available during the 2026-09-02 phase. A second, sharper uncertainty rides along: it is **not established that such a server reports `finish_reason: 'length'`** when the budget burns inside the reasoning block rather than in `content`. Some may report `'stop'`, in which case the guard does not fire at all and the caller receives the silent `''` this phase was meant to eliminate — the failure mode would be identical to the pre-`9f9f2fb` Ollama bug. Trigger: an operator runs a reasoning model on LM Studio / vLLM / SGLang / llama-server. First probe to run: a deliberately tiny `max_tokens` against a reasoning model, then record which of `reasoning_content` / `reasoning` / `delta.reasoning*` is populated and what `finish_reason` comes back.
- **No `num_ctx` is sent anywhere, so the Ollama context window is whatever the server defaults to (2026-09-02, Truncation Diagnosis phase)** — `core/src/services/llm/providers/ollama-provider.ts` sends only `temperature` and `num_predict` in `options`; `num_ctx` is never set, by this provider or any other call path. Ollama therefore applies its own default context window (historically 2048, model-dependent), and a prompt longer than that is truncated **server-side, silently** — no error, no `finishReason` signal, just a model answering from a prompt it only partly received. This is the *input* mirror of the output-truncation class this phase closed, and it is invisible to every guard added here, all of which key on `finishReason === 'length'` (an output-side signal). Most affected: the long-context paths — memory-snapshot prompts, `/ask` with a full context block, receipt photo prompts. Fix shape: send an explicit `num_ctx` derived from the model's advertised context length (available from `ollama show`), and log when the assembled prompt approaches it. Trigger: a local-model answer that ignores the earlier part of its prompt.

---

## Unfinished Corrections

Known issues or cleanup items that should be addressed in a near-term session.

- ~~**`composeRuntime` integration-suite timeout flake (reported as the "I1 flaky test", 2026-05-21)**~~ ✓ Closed (2026-05-21) — Root cause: `composeRuntime`-based integration tests relied on Vitest's default 5000ms test/hook timeout; under full-suite CPU contention (534 files in parallel) a `composeRuntime`-backed test or hook intermittently exceeded 5000ms (`composeRuntime()` alone takes ~250ms–1.6s). Reproduced in a full-suite stress loop — `message-rate-tracker-wiring.integration.test.ts` timed out at 5000ms (it carries no explicit suite timeout, unlike I1's 30s). Fix: `core/vitest.config.ts` now sets `testTimeout`/`hookTimeout: 30_000`; regression guard `core/src/__tests__/vitest-timeout-config.test.ts` fails deterministically if either knob is missing/below threshold. I1 itself was already robust (30s suite timeout) but its assertions were tightened (exact two adjacent ordered `source:'app'` turns + Telegram-send snapshot). 25 post-fix full-suite runs: zero failures.
- ~~**W1/W2 post-merge doc + URS traceability backfill**~~ ✓ Closed (2026-05-21) — all three sub-items resolved. (1) A `## Phase 2026-05-18 — Chatbot Command Awareness (W1)` section was added to `docs/implementation-phases.md`, adjacent to the W2 section and mirroring its structure. (2) Seven `REQ-CHATBOT-CATALOG-001..007` URS entries (each `Status: Implemented (W1, 2026-05-18)`) plus matching Traceability-Matrix rows and an updated Totals row were added to `docs/urs.md`. (3) The W2 namespace was verified: the source spec mandates no `REQ-USER-IDENTITY-*` namespace; W2's shipped `REQ-USER-009..012` are the canonical identity requirements, and a durable note recording this (so no `REQ-USER-IDENTITY-*` IDs are created) was added to `docs/urs.md` adjacent to `REQ-USER-012`. The backfill also added layer-correct catalog-injection test coverage (`prompt-builder.catalog.test.ts` + Layer A augmentations to `command-catalog.test.ts`), which surfaced and fixed two genuine W1 defects (whitespace-only description fallback; truncation marker overflowing the block cap). CL: `w1-traceability-backfill`.
- ~~**GUI test files — inline dynamic-import type casts need `import type` conversion (2026-05-20)**~~ ✓ Closed (2026-05-21) — converted all 50 inline import() type casts across 13 core/src/gui/__tests__ files to top-of-file import type declarations; removed all 46 biome-ignore format: suppressions. AST guard `core/src/gui/__tests__/inline-type-import-guard.test.ts` added.
- ~~**Chunk B follow-up: `app-detail.eta` select-as-text bug**~~ ✓ Closed in Batch 1 (2026-05-06) — added `else if (def.type === 'select')` branch to `app-detail.eta`; coercion at `coerce-user-config.ts:68-84` was already correct; render-side fix only. CL: `batch1-gui-cleanup`.
- ~~**Chunk B follow-up: effective-default resolver for system-config-backed settings**~~ ✓ Closed in Chunk C (2026-05-08) — `SettingsReader.resolveValue` now checks `def.systemConfigBackingKey`; when set and no user override exists, it dot-walks the live `SystemConfig`. `chatbot.log_to_notes` → `chat.logToNotes`. REQ-SETTINGS-029 implemented.
- ~~**Chunk B follow-up: migrate `routes/config.ts` writes to `SettingsWriter`**~~ ✓ Closed in Batch 1 (2026-05-06) — legacy `/gui/config/chatbot/:userId` handler now routes registry-eligible chatbot keys through `SettingsWriter.writeBatch`; post-write hook is the single source of truth. Removed `disableFlushAndCleanup` from `ConfigOptions`, `gui/index.ts`, `compose-runtime.ts`. Mixed-body data-loss bug fixed. CL: `batch1-gui-cleanup`.
- ~~**Energy/mood field removal**~~ ✓ Closed in Batch 4 (2026-05-07) — `energyLevel` and `mood` were already removed from `HealthDailyMetricsPayload.metrics` (see `apps/food/src/events/types.ts:69-70`). There is no SQL correlator table; the open-items entry referenced an LLM-prompt JS object table that was already clean. `isHealthCorrelationIntent` at `apps/food/src/handlers/health.ts:33-34` already excludes these terms. Compile-time regression guard added: `health-payload-shape.test.ts` (`_AssertNoForbiddenMetrics` type assertion + `pnpm build` gate). REQ-FOOD-HEALTH-NEG-001 added to `docs/urs.md`.
- ~~**`isPasRelevant()` deprecation**~~ ✓ Complete (2026-05-06) — function + tests + URS REQ-CHATBOT-006/010 removed. Original location pointer (`apps/chatbot/src/index.ts:81`) was already obsolete after Hermes P1; the function lived in `core/src/services/conversation/pas-classifier.ts` until removal.
- **Stage 3 review follow-up — downstream user cleanup policy** — Decide whether `UserMutationService.removeUser()` should also scrub downstream shared state such as space memberships, active-space records, and household admin references. The smaller rollback/sync fixes from Stage 3 are complete; this broader cross-service cleanup question remains open.
- **Finding 21 — broader Telegram Markdown normalization** — Inline handler messages, budget reporter, household, hosting planner, and cook-mode all send dynamic content without escaping. Phase F9 deferred these to Finding 21. (See `docs/superpowers/specs/2026-04-11-f9-telegram-markdown-escaping-design.md` line 69.)
- **Provider integration tests** — `GoogleProvider`, `OpenAICompatibleProvider`, and `OllamaProvider` lack dedicated integration tests (REQ-LLM-021). Blocked on API keys being available. Once keys exist, add tests alongside `AnthropicProvider`'s test pattern.
- **D5c-review — `revokeLastCheckCommit()` not request-specific under concurrency** — `HouseholdLLMLimiter.revokeLastCheckCommit()` calls `rl.revokeLastCommit(householdId!)` which pops the *last* timestamp. Under concurrent callers at the same `await` point (currently not possible since `reserveEstimated` is sync), rollback could revoke the wrong slot. Low risk today; fix if `reserveEstimated` is ever made async. Consider returning a per-commit handle from `check()` instead of popping last.
- **D5c Chunk E — rewire `multi-household-isolation.integration.test.ts` to composeRuntime.** The existing 717-line integration test hand-wires ~11 services at the data-layer slice with no LLM/Router path. Rewiring to `composeRuntime()` would drag it through the full service graph with no current motivation. Defer until a future food-app phase expands the test's scope to require LLM.
- ~~**D5c Chunk E — `composeRuntime({ config })` configPath footgun.**~~ ✓ Complete (2026-05-07) — Discriminated union (`RuntimeOverrides` two-branch union: `config?: undefined` | `config: SystemConfig; configPath: string`) enforces the pairing at compile time. Runtime guard throws before any service is constructed. Typecheck fixture in `core/src/typecheck/compose-runtime-types.typecheck.ts`; `pnpm typecheck:fixtures` in `core/package.json`. REQ-COMPOSE-001.
- **Stage 6 residual — CLI direct-run heuristic** — `core/src/cli/install-app.ts` and `uninstall-app.ts` still decide whether to auto-run by checking whether `process.argv[1]` contains the script name. The runner-level tests now cover command behavior directly, but the entrypoint heuristic itself remains string-based. Tighten this only if a future packaging/runtime environment makes the current check unreliable.
- ~~**Hermes P1 Chunk A: simplify pass pending**~~ ✓ Complete (2026-05-07, commit 0f44022) — Dead exports removed (gatherContext, MAX_CONTEXT_ENTRIES, ConversationDataScope barrel re-export), stale comments refreshed (manifest.ts, pending-edits.ts, pas-classifier.ts, telegram-format.ts).
- **Hermes P2: collaboration-data wiring** — `SOURCE_POLICY` declares the `collaboration-data` category but `buildContextSnapshot` returns an empty array for it (the underlying cross-household `SpaceService.isMember` query is not implemented). Wire when a concrete collaboration use-case is defined. (Noted in P2 Chunk A; survived through Chunk C.)
- ~~**Hermes P2: ContextStore double-read**~~ ✓ Fixed in P4 (2026-04-28) — `gatherContext` no longer fetches ContextStore entries. Durable memory comes only from the session snapshot; volatile context (apps, system data) is still gathered per-turn.
- ~~**Snapshot interactionContext unused in prompt-builder:** `buildContextSnapshot` fetches `interactionContext` entries but `buildAppAwareSystemPrompt` doesn't render them (the interaction data from `gatherContext`/the classifier path is used instead). This creates redundant I/O. Consider either (a) removing interaction-context from the snapshot fan-out in `free-text` mode until prompt-builder consumes it, or (b) rendering it in a prompt block. Deferred post-P2.~~ ✓ Closed in Batch 5 (2026-05-07) — fetch + snapshot field removed; `chooseSources` updated to 2 cheap readers; public `getRecentInteractions()` path preserved (REQ-CONV-RETRIEVAL-007).
- **Hermes P3: group-chat session isolation** — `buildSessionKey()` accepts `scope: 'group'`; `dispatchConversation` never sets it in P3. Wire when PAS handles Telegram group chats with separate per-group session contexts.
- **Hermes P3: token-counter integration** — `token_counts` in session frontmatter is `{input: 0, output: 0}` in P3. Populate when LLM cost-tracking is threaded through conversation turns (CostTracker → ConversationService). Frontmatter round-trip and preservation are tested (F.2 test).
- ~~**Hermes P5+: session retention / auto-prune**~~ ✓ Addressed in P5 (2026-04-28) — Sessions are kept forever in P3. Add opt-in retention (prune sessions older than N days) in P5 or later, co-designed with the FTS5 index invalidation strategy.
- ~~**Hermes P6: typed `kind:` filter on `buildMemorySnapshot`**~~ ✓ Complete (2026-05-05) — `ContextEntryKind` enum (5 durable + `untyped`), `.kinds.yaml` sidecar, `listDurableForUser` merging system+user entries with user precedence, `strict_durable_kinds` config flag narrowing `buildMemorySnapshot` input. `<memory-kind-set>` LLM tag for runtime kind assignment. REQ-CONV-KIND-001..005.
- ~~**Hermes P6: system durable context inclusion in snapshot**~~ ✓ Complete (2026-05-05) — `listDurableForUser` merges `data/system/context/` + `data/users/<userId>/context/`; user entry wins on key collision; filtered to `DURABLE_KINDS`. REQ-CONV-KIND-004.
- ~~**Hermes P6: sanitizer threat-regex hardening**~~ ✓ Complete (2026-05-05) — `threat-scan.ts` rejects script tags, iframe injection, and prompt-injection patterns; `ContextStoreThreatError` thrown before any write. REQ-CONV-KIND-003.
- **Hermes P4: snapshot character-budget tuning** — 4000-char budget is a starting estimate. Revisit after a month of real use; track `entry_count` and truncation rate via the persisted `memory_snapshot.status` field. Adjust if users with large ContextStore collections see truncation frequently.
- ~~**Hermes P5: `searchSessions` fenced wrapping**~~ ✓ Addressed in P5 (2026-04-28) — P5's transcript search hits should reuse `buildMemoryContextBlock` with `label: 'recalled-session'` and `marker: '... (recalled session truncated)'`. Add the wrapper when implementing `ConversationRetrievalService.searchSessions`.
- ~~**Hermes P6+ carry-forward: mid-session snapshot-rebuild command**~~ ✓ Complete (2026-05-05, Hermes P6.next Chunk B) — `/refreshmemory` and `/refresh-memory` Router built-ins; `rebuildMemorySnapshot` on `ChatSessionStore` with double-CAS safety; `handleRefreshMemory` handler with `buildSnapshot` callback reuse. REQ-CONV-MEMORY-013..022.
- **Hermes P4: snapshot build inside session index lock** — `ensureActiveSession` invokes `buildSnapshot` (which reads ContextStore) while holding the per-user `active-sessions.yaml` file lock. This serializes filesystem reads with any concurrent session operations on the same user. Fix: build snapshot outside the lock, double-check that no session was minted concurrently, then reacquire the lock and mint. Low urgency for single-user deployments.
- ~~**Hermes P4: real end-to-end freeze integration test** — The persona tests use mocked `ChatSessionStore`; they verify prompt injection but not the ContextStore → `buildMemorySnapshot` → `ensureActiveSession` → prompt path with real file I/O. Add one integration test with real temp-backed stores: set a preference, assert the next session's prompt contains it, mutate mid-session, assert the active prompt unchanged, `/newchat` to verify the new snapshot picks up the mutation.~~ ✓ Closed in Batch 5 (2026-05-07) — `composeRuntime` + `RecordingStubProvider`, 9 scenarios F1–F9 in `memory-snapshot-freeze.integration.test.ts`.
- **P4/P6 carry-forward: `<memory-context>` scope — app knowledge not wrapped** — Only durable `MemorySnapshot` (Layer 2) and DataQuery `searchData` results (Layer 4) are wrapped in `<memory-context>` blocks. App knowledge/metadata rendered by `buildAppAwareSystemPrompt` appears in legacy fenced sections outside this wrapper. Extend fenced wrapping in a future phase when there is a concrete motivating use case.
- ~~**Hermes P7 carry-forward — Classifier telemetry for session-control**~~ ✓ Complete (2026-05-07, Batch 3 Item 2) — `SessionControlLogger` (markdown structured log, mirrors `FoodShadowLogger`); classification entries (zone, entryId, latency, sanitized message text) + confirmation entries (linked by entryId, elapsedMs from `createdAtMs`); fail-open; `pnpm analyze-session-control-log` CLI. REQ-CONV-NEWCHAT-009..012.
- **Hermes P7 carry-forward — Group chat session key for sc:yes callback** — `sc:yes` callback currently resolves the session key using the DM session path. When PAS handles Telegram group chats, the callback must carry the group `chatId` so `handleNewChat` mints a group-scoped session. Defer until group-chat session isolation is implemented (see Hermes P3 group-chat entry above).
- ~~**`formatCheapestPriceAnswer` wording stopgap (path a)**~~ ✓ Closed in Batch 4 (2026-05-07) — Answer wording changed from `"… is cheapest for …"` to `"Lowest saved package price for {item}: {name} at {price} at {store}…"` at `apps/food/src/services/receipt-query.ts:377`. REQ-FOOD-PRICE-002 added to `docs/urs.md`. 11 unit tests (U1–U11) + 4 NL persona tests (P1–P4) added.
- ~~**`formatCheapestPriceAnswer` unit-price normalization (path b)**~~ ✓ Closed (2026-05-07, Item 2) — `unit-normalizer.ts` (parseSizeString/calculateUnitPrice/formatUnitPriceToken); `formatCheapestPriceAnswer` switches to unit-price mode when all matched entries parse to the same base (g/ml/ct) and ≥2 entries remain after invalid-price filtering; fallback to existing package-price wording otherwise. `PriceEntry.sizeValue/sizeBase` (transient, not persisted); `ReceiptLineItem.packageSize?` flows through `updatePricesFromReceipt`. Receipt + price-update LLM prompts updated. URS REQ-FOOD-PRICE-003 / 003.1 / 003.2 / 003.3 / 003.4 added. 50 unit-normalizer tests + 14 receipt-query unit-price tests + 5 prompt-content tests + 10 persona tests (P5×5, P6×3, 2 negatives).

- **Regression Run tab's own submit button ignores `activeRunId` (2026-09-01)** — REQ-REG-GUI-V2-028 threads `activeRunId` to all six `/gui/regression` render paths, but only the **compare** tab's summary bar consumes it to disable its submit button during an active run. `core/src/gui/views/partials/regression-tab-run.eta:117`'s `<button type="submit" data-testid="run-submit">` has no `disabled` binding, so an operator on the Run tab can still submit while a run is in flight. The POST is safe (the registry serializes runs and returns 409 with `{activeRunId}` — see the single-active-run accepted risk below), so this is a UX wart, not a correctness bug. Fix: mirror the summary bar's guard in `regression-tab-run.eta` and extend the existing `activeRunId reaches every view` describe with a run-tab case.

---

## Food App Enhancements

Feature work identified in phase specs but deferred.

- **Waste reporting** — `/waste` command with monthly summary of wasted items and estimated cost. (H6 spec)
- **Meal plan awareness** — Suggest meals based on leftovers and freezer inventory. (H6 spec)
- **Nutrition tracking for waste** — Attribute nutritional/cost loss to wasted food entries. (H6 spec)
- **H5b — Cook mode: timer integration** — Extract timer durations from recipe steps, surface "Set timer" Telegram buttons, wire into one-off scheduler, send notification callback on expiry. (Deferred from H5a)
- **H5b — Cook mode: TTS / Chromecast output** — Hands-free cooking via audio output: device selection, non-blocking per-step TTS, `tts_device_name` + `auto_advance_timer` + `cook_mode_timeout_hours` config fields. Voice input for step advancement is out of scope until a voice-input service exists. (Deferred from H5a)
- ~~**H5b — Cook mode: contextual food questions**~~ ✓ Complete — implemented at `apps/food/src/index.ts:2632` (`handleFoodQuestion`): loads `contextStore.searchForUser('food preferences allergies dietary restrictions family', userId)` and injects active-cook-session context (recipe title + current step) into the LLM prompt. The two neighbouring H5b entries (timer integration, TTS/Chromecast) remain deferred.
- **ContextStore writes — household dietary profile** — `apps/food/src/services/family-profiles.ts`, `apps/food/src/services/guest-profiles.ts`, `apps/food/src/handlers/family.ts`, and `apps/food/src/handlers/targets-flow.ts` capture dietary restrictions, allergies, family/guest member profiles, and nutritional targets — but only to data files. The chatbot has no durable cross-session recall of "Matthew is vegetarian, allergic to shellfish, family of 4 with one kid who hates broccoli." Add `contextStore.save()` on profile mutations, with `kind: 'household-policy'` (or `user-preference` for individual prefs). Idempotent key per member so updates overwrite cleanly. Ready to plan; no upstream design question.
- **ContextStore writes — weekly food / nutrition digests (design follow-up)** — `apps/food/src/handlers/nutrition-summary.ts` (Sunday-evening cron `generateWeeklyDigest`), `apps/food/src/handlers/nutrition.ts:387-398` (`generatePersonalSummary`), and `apps/food/src/handlers/health.ts:71-95` (`correlateHealth`) all produce LLM summaries that go out via Telegram and disappear. After each successful digest / personal summary / health correlation, write a distilled summary to ContextStore so the chatbot can recall "last week you were 15% over protein target" in a future session. Needs design: per-user (each household member has their own nutrition targets) vs per-household scope; key strategy for recency-aware overwrite vs append.

---

## Proposals (unconfirmed — implement when triggered)

Ideas that are not yet approved. Each has a stated trigger condition.

- **Member-scoped Files browsing (2026-07-07, live-verification finding)** — The GUI UX Redesign's nav initially showed "Files" under "Your data" for everyone, but `/gui/data` is deliberately admin-only (R3 data-boundary decision, `core/src/gui/routes/data.ts` `denyDataAccess`), so members hit a 403. Tactical fix shipped in the live-verification fix round: the nav item is admin-gated. Strategic option: a member-scoped read-only file browser (own + shared + member-space scopes, same visibility model as the alert wizard's data-source picker) to honor the spec's original "Your data → Files for all" intent. Needs its own design pass (path handling + escaping + tests per `pas-security-posture`). Trigger: a household member asks to browse their files in the GUI.
- **Household-shared-only alert data sources rejected by validator (2026-07-07, live-verification finding)** — `alert-validator.ts` requires `user_id` whenever `space_id` is absent, but the read path (`AlertService.readDataSources` → `resolveScopedDataDir`) fully supports household-shared sources (`households/<hh>/shared/<app>/…`) with neither field. A legitimate use case (alert on household-shared data rather than one member's copy) fails validation today. Fix sketch: accept a source with only `app_id` + `path` when household context resolves, or add an explicit shared-scope marker; then let the alert wizard's picker offer shared files as sources. Trigger: first real alert against household-shared data (the food app's shared pantry/grocery files are the obvious candidates).
- **Per-IP login rate limiting shares one budget across a household (2026-07-07, live-verification finding)** — GUI login rate limiting checks a per-client-IP key before the per-user key, so several household members behind one router share a single attempt budget; a burst of logins can lock everyone out for the window (observed during verification with three users on one IP). Options: per-user-first limiting, higher IP threshold, or household-aware keys — needs a small security-posture review either way. Trigger: a real household reports login lockouts, or before public sharing.
- **Member self-service report/alert creation (2026-07-06)** — The GUI UX Redesign keeps report/alert creation admin-only (operator decision during the Codex plan review); members get read-only views of automations delivered to them. Member self-service would need a dedicated authorization design: creation scoped to the member's own data sources, dangerous action types (`write_data`, `dispatch_message`, `webhook`) restricted or admin-approved, and guard/URS updates. Trigger: a household member asks to create their own report or alert.
- **Fitness/Health app** — Subjective signals (energy, mood, wearables) belong in a dedicated app. Food's `HealthDailyMetricsPayload` intentionally omits these; a future fitness app should emit `health:daily-metrics` events.
- **Full Telegram data access audit** — Each new app phase should verify read + correct NL intents for all stored data. Several food stores still lack this (price store, receipt items, health metrics).
- **App registry/marketplace** — Static JSON index + GUI browse page. Trigger: 10+ apps exist.
- **App signing** — Cryptographic verification for reviewed apps. Trigger: community review process established.
- **Container isolation** — Trigger: ~~community forms and multi-tenant security requirements harden~~ **re-scoped by the 2026-07-06 fourth-pass security review (SEC-4): before `install-app` is publicized as a way to run third-party apps** — the first community app arrives before a community forms, and the static analyzer stops accidents, not adversaries. Until then, SEC-3's install-time trust warning is the mitigation; do not invest in strengthening the regex analyzer. **Expanded into the SR-1 entry under Confirmed Phases (confirmed 2026-07-07, Fable strategic review) — SR-1 is the design space (Tiers A/B/C); this line's Tier C stance is unchanged.**
- **Smart freeze suitability** — Warn when foods that don't freeze well are being frozen. (H6 spec.)
- **Batch expiry estimation** — Estimate multiple pantry items in one LLM call instead of per-item. (H6 spec.)
- **Agentic loops** — 6 agent proposals (Routing-Learning, Data Steward, Receipt/OCR QA, Household Planning, Ops, App Onboarding). See `docs/superpowers/plans/2026-04-15-llm-enhancement-opportunities.md`. **Dependency: chatbot-primary T2 (ToolRegistry + ToolPolicy).** Unblocked once T2 lands.
- **Regression Suite v2 — `judge` oracle** — Reference-answer comparison: commit a human-written "gold" answer; judge LLM scores actual vs. gold. More expensive and harder to maintain than the rubric oracle but more precise for factual recall cases. Trigger: rubric false-fail rate becomes noisy enough to warrant more precision.
- **Regression Suite v2 — cost-regression assertions** — Assert that token counts for a given case stay within a threshold (e.g., ±30%) of the last cached run. Useful for detecting models that drastically increase prompt consumption. Trigger: operator cost-cap tuning reveals unexpected token spikes after a model swap.
- **Regression Suite v2 — CI integration** — Optional GitHub Actions job that runs `pnpm test:regression --no-cache` when `config/pas.yaml` fast/standard/reasoning model IDs change. Requires real API keys in CI secrets. Trigger: team grows beyond solo deployment and model changes become reviewed PRs.
- **Regression Suite v2 — verdict annotation UI** — Allow an admin to mark a rubric verdict as "acceptable" (false-fail override) so the history timeline doesn't accumulate noise from borderline cases across model versions. Trigger: rubric false-fail rate observed in practice.
- **Regression Suite v2 — generic per-app test discovery** — The suite's buckets are currently fixed and core-level (`routing`, `receipt`, `chatbot`, `recall`), each wired to specific production classifiers. Apps cannot register their own model-evaluation cases. The food app's `FOOD_PERSONAS` shadow-classifier pattern is a food-specific shim, not a general API. A generic mechanism — apps declare regression cases (in-manifest or via an app `regression/` directory) and `pnpm test:regression` discovers and runs them through the same model-swap + caching + oracle machinery — would let any app developer measure how model changes affect their app. Needs deeper design (case discovery, per-app budget/cost attribution, oracle reuse, cache-key coverage paths). Trigger: a second app beyond food needs model-behavior regression coverage, or an app developer requests it.
- **Regression Suite — memory-type test coverage requirement on future phase plans** — Chunk C's plan defines a memory-type taxonomy (durable snapshot, recent turns, recalled-data, recalled-session FTS5, photo-memory bridge, daily notes, context-store typed entries, settings catalog, interaction-context summary). Each of the next 7 major phases that extends the regression framework MUST include at least one chatbot case per new memory type and restate coverage for existing types. Tracked here so future plan authors do not skip the checklist. Trigger: each subsequent regression-extension phase plan. See plan `docs/superpowers/plans/2026-05-11-persona-regression-chunk-c.md` "Memory-Type Test Coverage" section for the full taxonomy.
- **Regression Suite Chunk C — `captureHandler` and `endActiveSession` placeholders** — `chatbotEnvFactory` (`build-deps.ts:131-145`) ships placeholder stubs because `chatSessions`/`eventBus` aren't surfaced on `RuntimeServices`. Codex correction C2 (commit `01c8c9f`) unblocked the chatbot bucket by dropping `expectedHandler` from all 10 fixtures so they grade reply quality via the rubric oracle alone. Re-introduce `expectedHandler` per fixture (with IDs aligned to the food app's actual `regexWinner` taxonomy — `receipt_query`, `price_lookup`, `store_spending`, `grocery_view`, etc., per `apps/food/src/index.ts:1000+`) once Router instrumentation lands and surfaces the per-dispatch handler id. Trigger: operator wants routing-correctness assertions on chatbot cases.
- **Food-shadow + recall + PAS classifier prompt hardening for local models** *(partially closed by 2026-05-12 Chunk C Correction; one residual case open)* — Fresh-evidence Batch 5 confirms food-shadow 0/27 → 18/27 pass with 27/27 non-`parse-failed` and 0 errors; PAS 2/6 → 4/6 (prefilter short-circuits). **Recall is 24/25, not zero `parse-failed` as previously stated** — `recall-true-yesterday` still ends in `reason:"parse-failed"` per cache evidence (Codex P1 correction, 2026-05-12). The empty-only retry guard from the post-Batch-2 simplify pass means non-empty unparseable Gemma output now goes straight to safe-default. Resolution: either loosen retry to fire on parse-failure (revisit F9 flake risk), or tighten `tryParseJsonStripFences` to handle additional Gemma quirks. See `docs/superpowers/plans/findings/2026-05-11-chunk-c-local-model-verification.md`.
- **Regression Suite Chunk C — chatbot bucket live verification (judge model was claude-sonnet-4-6 not Gemma 26b)** — *Originally documented as Gemma-judge but Codex review revealed `--judge-model=ollama/gemma4:26b` was silently dropped by `ModelSelector.load()`; cache shows `modelIds.standard: claude-sonnet-4-6` on every run.* Fixed by Chunk C correction phase Batch 0 (`applyTransientOverride`). The original Gemma-judge run is part of Batch 5 mandatory verification. The 3 "judge misclassified fenced reply" failures are now attributable to Claude Sonnet 4.6 (so the rubric prompt defect affects frontier models too, not just local judges). See findings doc Correction #1.
- **Rubric oracle prompt hardening for local judges** *(historical — superseded by "Chatbot bucket — Gemma 26b judge token-repetition loops" entry below)* — Gemma 4 26b originally mis-classified a fenced reply as "no actual reply provided" 3/10 times. After the Codex P1 follow-up landed `responseFormat: 'json'` + `tryParseJsonStripFences` in `regression/src/oracles/rubric.ts`, the failure mode shifted from "judge refuses to grade fenced data" to "judge emits valid opening JSON then degenerates into token-repetition loops." The hardening idea (less-defensive fence wording for cooperative judges) is preserved here for future reference, but the active blocker is the token-repetition entry below, not this one. Findings: `docs/superpowers/plans/findings/2026-05-11-chunk-c-local-model-verification.md` Step 14.3 + Correction #1.
- ~~**Food app — clarifying-question deflection on vague price queries**~~ ✓ Closed by 2026-05-12 Chunk C Correction Phase Batch 3. The `extractPriceItem` regex in `apps/food/src/services/receipt-query.ts` is broadened to match "cheapest X" with optional "place to buy/get/find" prefix; trailing prepositions (`at/from/among/in/near/for`) or end-of-string terminate the capture. Unit test uses the exact cached failing phrase. Fresh-evidence Batch 5 was unable to confirm via the chatbot bucket because the rubric judge hit the new JSON-mode gap (see entry above); the underlying handler/regex change is verified by the unit tests.
- **Settings — per-household settings inheritance UX** — Visualize and edit the system → household → user override chain in the GUI (e.g., "system default: 90 days; your household override: 30 days; your personal override: —"). Trigger: multi-household deployment where household admins want to set household-wide defaults.
- **Settings — Notifications section content** — The Settings page reserves a "Notifications" collapsible section. Content is a placeholder until a per-app digest opt-in feature is designed (e.g., weekly food summary, alert digest, session summary email). Trigger: user requests a notification preference toggle. **Trigger satisfied by the 2026-07-06 UX second pass (findings B1 + N1)** — UX Hardening Batch 5 folds the per-job proactive toggles into this section.
- **Quiet hours + vacation pause for proactive messages (2026-07-06 UX second pass, N1 layers 2–3)** — Core-level per-user quiet-hours window (e.g., `notifications.quiet_hours: "22:00-08:00"`) enforced in `sendProactiveMessage`/`AppOutboundBridge` (drop vs defer needs a design decision), plus a `/pause <days>` command or `pause_until` setting suppressing all proactive output. User-initiated replies never affected. Trigger: Batch 5 per-job toggles ship and notification volume still needs a time-based control, OR a user requests it.
- ~~**GUI conversation-sessions page (2026-07-06 UX second pass, D2)**~~ ✓ Shipped (2026-07-06, GUI UX Redesign Batch 6, commit `df22928`) — Read-only `/gui/sessions` page: per-user session list (title, date, turn count) + transcript view + FTS5-backed search, reusing the existing SQLite transcript index. Delivered **own-sessions-only for everyone, including admins** (a deliberately higher privacy floor than the original "non-admins see only their own" framing — see the "Conversations GUI is own-sessions-only" Accepted Risk below). Edit/delete remain deferred. REQ-GUI-SURFACE-001, tests in `sessions.test.ts`.
- **`/remind` or NL reminder capability (2026-07-06 UX second pass, B2 follow-on)** — Lightweight per-user reminders from Telegram ("remind me at 7 to take out the trash") backed by the one-off scheduler. Prerequisite: "One-off task user scope" entry (Deferred Infrastructure Work). Natural fit as a native tool after chatbot-primary T2/T4. Trigger: one-off task user scope lands, or a user asks for reminders (the onboarding welcome previously advertised this — Batch 4 rewords it until the feature exists).
- **Hermes: tool registry with AST-gated discovery** — Auto-discover tools via AST analysis so runtime registration matches declared exports. (See `docs/hermes-agent-adoption-review.md`) **Unblocked by chatbot-primary T2** — ToolRegistry + static-export check at app-load time is the T2 implementation of this proposal.
- **Hermes: channel adapter ABC + PLATFORM_HINTS** — Clean abstraction for multi-channel messaging (max length, markdown support, reaction support). Trigger: a second messaging channel is added beyond Telegram. (See `docs/hermes-agent-adoption-review.md`)
- **Hermes: secret redaction with import-time flag snapshot** — Scrub known secret-shaped strings from any string sent externally or logged. Trigger: conversation transcripts ever leave the local machine. (See `docs/hermes-agent-adoption-review.md`)
- **Shadow-primary persona sweep (collapsed-bucket overlap)** — *(Repurposed by chatbot-primary T3.)* Originally a trigger-gated pre-flip integration test for food's regex shadow. Now the overlap phrases become part of the labeled persona corpus (`tests/persona/tool-cases.json`) for chatbot-primary T3, which tests tool-call precision on these exact boundary phrasings. No longer a separate proposal — subsumed into T3 persona corpus construction.
- **Chunk C correction phase — Batch 5 fresh-evidence re-run** *(executed 2026-05-12; gates a/b/d/e pass; gate (c) partial; new chatbot-residual open below)* — (a) chatbot fresh-cache `modelIds.standard: "gemma4:26b"` on 10/10 files — Batch 0 override fix verified end-to-end ✓; (b) 27/27 food-shadow cases non-`parse-failed` (18 pass, 9 fail with honest label-mismatch) ✓; (c) recall has **1 of 25 still `parse-failed`** (`recall-true-yesterday`) — gate is mostly green but the original claim "zero parse-failed" was inaccurate per Codex 2026-05-12 review; (d) PAS prefilter behavioral test passes ✓; (e) `session-control-continue` flipped FAIL→PASS ✓. Routing accuracy 0% → 89.62%. Findings: `docs/superpowers/plans/findings/2026-05-11-chunk-c-local-model-verification.md` "Post-Correction Fresh-Evidence Re-Run" section.
- ~~**Rubric oracle JSON-mode plumbing for local judges**~~ ✓ Closed by Codex P1 follow-up (2026-05-12). `regression/src/oracles/rubric.ts` now passes `responseFormat: 'json'`, reuses `tryParseJsonStripFences`, and has 2 new regression-guard tests. Chatbot bucket re-run improved from 0/10 (all `judge JSON parse failed: Unexpected end of JSON input`) to 3/10 pass + 7 errors of a different class (see new chatbot-residual entry below).
- **Chatbot bucket — Gemma 26b judge token-repetition loops (2026-05-12, surfaced by P1 follow-up)** — After the rubric JSON-mode fix, Gemma 26b emits the opening `{"score": N,` correctly but then degenerates into whitespace / word-repetition loops within the `"explanation"` field, producing unparseable JSON even at `maxTokens: 400`. Affects 7/10 chatbot cases. Not a framework bug — the harness correctly refuses to grade a malformed judge response. Possible fixes (no consensus yet, deferred): (a) expose Ollama `top_p` / `repeat_penalty` knobs through `LLMCompletionOptions` and tune for judging; (b) switch to a different local judge model; (c) add a more permissive JSON-fragment parser that extracts `{"score": N}` even when the `explanation` field is truncated. Trigger: operator wants a reliable local-model judge for the chatbot bucket.
- **Chatbot `chatbot-costco-21-items` is a food-handler defect, not a judge error (Codex 2026-05-12)** — Prompt: *"List the items on my Costco receipt from May 1 2026, with each price, and call out which are new entries."* Rubric requires ≥5 items mentioned and `MUST NOT truncate`. Actual reply (cache `data/system/regression-cache/chatbot-costco-21-items/edeebd9c…json`): *"Costco receipt (2026-05-01) price updates: 1 updated, 1 new — Spindrift … Blueberries … Total receipt spend: $306.77"* — a 2-item *diff view*, not the full 21-item list. The "call out which are new entries" phrasing routes through the food app to a `new_receipt_items`-style handler that emphasizes the diff over the full list. The companion case `chatbot-receipt-items-and-total` (simpler prompt) routes to the full-items handler and passes. The Gemma 26b `verdict: 'error'` (token-repetition) masked this real defect. Resolution paths: (a) broaden the receipt-items handler matcher so the "call out which are new" variant returns the full list AND annotates new entries, or (b) widen the rubric to accept either presentation. Fix in food app preferred. Trigger: operator wants chatbot regression bucket to surface food-handler regressions reliably.
- **Remaining 9 food-shadow label-mismatch failures under Gemma 4 e4b (2026-05-12, post-correction)** — After JSON-mode unblocked the bucket, REQ-REG-011 sits at 89.62%. Remaining failures are honest model-strength signals (Gemma picks the wrong label, not the wrong format). Not a framework bug. Production flip of shadow classifier (`routing_primary: shadow` in `pas.yaml`) should gate on a higher-quality model OR additional label-disambiguation few-shots in the food-shadow prompt. Trigger: operator wants the shadow classifier to pass the 95% gate under local Gemma fast tier.
- ~~**REQ-REG-011 gate cleared by stronger routing model**~~ ✓ Closed by stronger-routing-model sweep (2026-05-13). Gemma 4 31B at **0.9811 per-input routing accuracy / 33/36 cases pass** clears the 0.95 gate cleanly. Gemma 4 26B plateaued at 0.9057. Claude Haiku 4.5 underperforms at 0.9151 (over-generalizes cultural recipes to generic recipe search). The 3 residual 31B cases are fixture/semantic edge cases ("just had some leftover chicken", "show me my macros", "what does Hermes do") — not framework or model defects. Findings: `docs/superpowers/plans/findings/2026-05-13-chunk-c-routing-gemma-31b.md` (primary), `2026-05-13-chunk-c-routing-gemma-26b.md`, `2026-05-13-chunk-c-routing-haiku-4-5.md`. Code changes on `regression/stronger-routing-models` branch (also help every other model): paraphrase-fix in food-shadow prompt; `responseFormat: 'json'` in session-control classifier (real bug — was missing from Batch 1 plumbing); split PAS prefilters (NEW `PAS_META_RE` returns `{pasRelated:true}` only for app meta-questions; `SETTINGS_KEYWORDS_RE` widened for auto-detect toggles; `DATA_QUERY_PREFILTER` widened for food data queries). 43 PAS unit tests, 123 shadow-classifier tests, 30 session-control tests all pass.
- ~~**Regression GUI `--model-matrix` / `--judge-model` override surface (2026-05-13)**~~ ✓ Closed by GUI override surface feature (2026-05-13, branch `regression/gui-model-override`). The `/gui/regression` POST handler now accepts `modelMatrix` and `judgeModel` body fields, validated through a shared parser at `core/src/services/regression/model-spec.ts` (REQ-REG-GUI-OV-001..010). The spawn allowlist re-validates via the same parser (defense in depth). Form inputs added to `regression-summary-bar.eta` with `aria-label` accessibility. Tightened parser rejects shell metachars, traversal, control chars, and HTML payloads in both CLI and GUI paths while accepting GUI-compatible provider ids and safe namespaced model ids. Test coverage spans 6 touched test files. See `docs/urs.md` REQ-REG-GUI-OV-001..010 for the full traceability matrix.
- **Cache key should include provider, not just model name (2026-05-13)** — `TierModelSnapshot` in `regression/src/shared/types.ts` stores model strings only (e.g. `"gemma4:31b"`, not `"ollama/gemma4:31b"`). The cache-key composition at `regression/src/shared/cache-key.ts:20-42` reflects this. Consequence: two providers with the same model name (hypothetically `ollama/gemma4:31b` vs `another-provider/gemma4:31b`) would produce the same cache key and silently collide. REQ-REG-GUI-OV-007 was narrowed to "distinct model IDs" for the GUI override surface phase; the schema migration (expand `TierModelSnapshot` + cache-key inputs to include `provider`) is deferred. Trigger: a real second provider ships the same model name OR the cache-collision risk is otherwise evidenced.
- **Cascading-models routing (tier-0 fast model + escalation, 2026-05-13)** — Idea: prepend a tiny, fast first-tier classifier (Cactus Compute's [Needle](https://github.com/cactus-compute/needle) is the operator's preferred candidate but **is not currently Ollama-supported** — provider work required first) that handles the easy 70–80% of routing decisions, then escalate to Gemma 4 31B (or higher) only when the first-tier model's confidence falls below a threshold. Benefits: lower steady-state latency + cost on the easy path; preserves accuracy on the hard path. Open questions before designing: (a) does Needle ship a usable provider integration (HTTP server, llama.cpp loader, vendor SDK)? (b) what confidence signal does it expose (logits, calibrated score, structured "uncertain" output)? (c) how to plumb the escalation hand-off into `LLMService` so apps remain tier-blind? Out of scope for the GUI polish phase; capture only. Findings should land in a future spec. Trigger: operator confirms Needle is reachable from this codebase (or picks a different tier-0 candidate — quantized small Gemma, Phi-mini, etc.) and signs off on the escalation contract.
- **Regression Suite — per-case routing pass-rate gate (dual-gate, 2026-05-14)** — REQ-REG-011 currently gates only on *per-input* routing accuracy (≥0.95 over food-shadow inputs). The operator raised adding a *second*, complementary gate on the per-*case* pass rate (e.g. ≥0.85 of routing-bucket cases pass), so both lenses are enforced, not just displayed. The 2026-05-14 GUI clarity work *displays* both metrics (live banner + Overview leaderboard, REQ-REG-GUI-V2-025/026) but deliberately did **not** change pass/fail semantics. Implementing the second gate is a non-trivial change: it alters the runner's exit-code logic (`regression/src/runner/index.ts:~440`), needs a routing-bucket-scoped per-case count (the current `RunSummary` only carries all-bucket `pass`/`totalCases` — a new field or computation is required), needs a manifest-schema field, a new URS requirement, and has CI exit-code implications. Out of scope for the GUI clarity phase; capture only. Trigger: operator confirms the dual-gate thresholds and signs off on the changed suite pass/fail contract.
- **User identity — self-service display name rename (2026-05-18)** — Allow users to change their own display name post-registration via a GUI page. The 2026-05-18 phase scopes name-setting to admin-time-only via `/invite <name>`; users with an unwanted name must ask an admin to edit YAML. Self-service requires: a `/gui/profile` page (or extension of an existing settings page), uniqueness re-check at rename time using the same locked path as `createInvite`, audit log of historical names, and a decision on whether session cookies / API keys need invalidation on rename. Trigger: a user requests the ability to rename themselves, or a household with rotating guest users finds the admin-edit path too friction-heavy.
- **User identity — per-household name uniqueness (2026-05-18)** — The 2026-05-18 phase enforces globally-unique display names across the PAS instance to make login-by-name unambiguous. A per-household scheme would let two households each have a "Sarah," with login-by-name needing either an additional "which household?" prompt or a household-prefix on the username. Trigger: a multi-household deployment surfaces a real name-collision case OR a household admin requests household-scoped namespaces.
- **User identity — chatbot executes `/invite` on the user's behalf (2026-05-18)** — The 2026-05-18 phase makes the chatbot able to *explain* `/invite` but not run it. A future extension could let the chatbot generate an invite when an admin says "invite Sarah" in natural language, with an admin confirmation step (`tc:yes`/`tc:no` or similar). Belongs after chatbot-primary T2 (ToolRegistry + ToolPolicy + PendingToolConfirmationStore) lands — `invite` becomes a native tool with admin gating and confirmation, not a special-cased chatbot capability. Trigger: chatbot-primary T2 ships AND operator wants invite generation from free text.
- **User identity — per-tier `auto_detect_pas` cost carve-out (2026-05-18)** — The 2026-05-18 phase fixes the `auto_detect_pas` resolver to honor the manifest's `true` default (today the resolver disagrees with the manifest). If the W1 implementation investigation reveals that the original `false` default existed to save tokens on cheap LLM tiers, a per-tier carve-out (e.g., `auto_detect_pas: false` for `fast` tier, `true` for `standard`/`reasoning`) would preserve that savings without leaving the free-text fallback PAS-unaware on capable tiers. Trigger: W1 implementation surfaces a concrete cost driver tied to the historical default AND operator approves a per-tier policy.
- **User identity — broader `user.id` leak sweep (2026-05-18)** — The 2026-05-18 phase enumerates specific operator-GUI templates (`data.eta`, `alert-edit.eta:177`, `report-edit.eta:167`, `config.eta`, `context.eta`, `dashboard.eta`, reset-password) for `user.id` → `user.name` replacement. If implementation reveals additional surfaces (telegram replies that quote ids back to the user, API response envelopes, log lines surfaced via GUI, etc.) outside the enumerated set, they become follow-up items rather than in-scope expansion. Trigger: implementation or operator usage surfaces an unhandled `user.id` display location.
- **Chatbot tool-call to restart apps (2026-05-18)** — Allow the chatbot to issue a tool call that restarts a named app's processes. Blocked on chatbot-primary T2 (ToolRegistry + PendingToolConfirmationStore + tc:yes/tc:no callbacks). Revisit when T2 ships.
- **App-message bridge for group-chat session keys (2026-05-18)** — `AppOutboundBridge` writes to the DM session key only today. When PAS handles Telegram group chats, the bridge must accept a group `chatId` and mint a group-scoped session. Follows Hermes P3 group-chat deferral.
- **Notes app proactive delivery via bridge (2026-05-18)** — Notes is reactive-only today. If proactive output is added (e.g., scheduled daily-notes recap), wire each send through the bridge.
- ~~**Bridge additional food proactive jobs (2026-05-18)**~~ ✓ Resolved 2026-05-22 (Chatbot Context & Routing Fixes, Part 1) — the eight genuine proactive jobs are now bridged via a single `sendProactiveMessage` helper (`apps/food/src/utils/proactive-message.ts`): nightly-rating-prompt, perishable-check, leftover-check, freezer-check, defrost-reminder, cuisine-diversity-nudge, seasonal-nudge, cultural-calendar. The five already-correct bridge sites (weekly-nutrition, weekly-health, weekly-menu, batch-prep, voting) were migrated to the same helper so it is the **only** proactive send path, locked in by a build-failing entrypoint-scoped AST scanner (`apps/food/src/testing/proactive-send-scan.ts`). The other four jobs originally listed (budget alerts, hosting planner, grocery-generator-after-vote, child-tracker) were re-verified as **reactive** in Task 1.1: each replies to a user command/intent/callback and is therefore already in the chatbot transcript via the normal user-turn path — bridging them would double-record. Evidence: budget alerts are appended to other (already-bridged) message bodies; hosting planner's only sends are replies to `/hosting plan` (user-initiated); child-tracker / grocery-generator-after-vote reply to callbacks or commands.

- **Include the `regression` workspace in the root vitest `projects` list (2026-09-01) — operator decision needed** — REQ-REG-001 says the regression suite "MUST be excluded from `pnpm test`", and `regression/README.md` repeats it. That exclusion was written to keep a **cost-incurring, real-LLM** harness out of the default test command. But the exclusion does not actually cover that harness: `pnpm test:regression` is a separate CLI entrypoint (`regression/src/runner/cli-main.ts`) that vitest never invokes under any configuration. What the exclusion suppresses is 660+ fully-mocked unit tests that run in ~5s with zero network access — and that is why 17 of them rotted unnoticed until 2026-09-01 (13 receipt date-window failures that also broke real runs, 4 env-dependent failures that passed only on the maintainer's primary checkout). Proposal: add `'regression'` to the root `vitest.config.ts` `projects` array so the workspace's unit tests run with everything else, leaving `pnpm test:regression` exactly as it is. **This needs a URS amendment to REQ-REG-001** distinguishing the vitest unit tests (cheap, mocked, should run) from the CLI harness (expensive, real-LLM, stays opt-in) — do not make the config change without it. Trigger: operator confirms the distinction is the intended reading of REQ-REG-001.

---

## Accepted Risks (no action needed)

Documented decisions to live with known imperfections.

- **Space membership has two sources of truth in the GUI (2026-07-07, live-verification observation)** — The Household page's space checkboxes render from `RegisteredUser.sharedScopes` while the Spaces pages render from `SpaceDefinition.members`; the production mutation path (`POST /gui/users/:id/groups`) writes both, but any other writer (seeds, future API paths, manual edits) can desynchronize them and the two pages will silently disagree. Pre-existing design (predates the GUI UX Redesign). Accepted for now; revisit if a real drift is observed or when space membership gets its next feature pass.
- **Alerts "Last Fired" and alert-history are separate signals (2026-07-07, live-verification observation)** — The alerts list reads `AlertDefinition.lastFired` (set only by the live `evaluate()` fire path) while the History page and the activity-daily metric count `data/system/alert-history/` files. They normally agree but nothing enforces it. Accepted; only matters if either is ever used for monitoring guarantees.
- **Conversations GUI is own-sessions-only, including for admins (2026-07-06)** — The GUI UX Redesign's `/gui/sessions` page (plan: `docs/superpowers/plans/2026-07-06-gui-ux-redesign.md`, Task 6.1) deliberately scopes transcript browsing to the requesting user for everyone — platform admins get no cross-user transcript read, because chat transcripts are personal. Revisit only on explicit operator request; if revisited, it is a privacy-posture change needing its own design note.
- **Settings resolver AppConfigService instance sharing (2026-05-05):** `compose-runtime.ts` creates a second `AppConfigServiceImpl` instance per installed app for the settings `appConfigResolver`, separate from the instance used by `CoreServices.config`. Both write to the same YAML files (currently correct behavior), but would silently desynchronize if `AppConfigServiceImpl` ever adds in-memory caching. Refactor: populate `appConfigByAppId` from the same instances the serviceFactory hands to each app. Prerequisite: expose app config service instances from the service factory.
- **D40** — `getActiveSpace()` fire-and-forget persist. Acceptable: self-healing on next request.
- **D42** — Conversation history anti-instruction framing removed. Accepted: continuity > theoretical injection risk.
- **Callback space semantics — originating scope** — Inline keyboard callbacks resolve space context at tap-time, not button-generation-time. Encoding the originating scope into every callback data string is a large change with low practical impact (buttons are tapped promptly). Accepted as designed.
- **P8a — Active-work protection is silent** — When `pendingEdits` or `PendingSessionControlStore` blocks an idle reset (`status: 'protected'`), no inactivity notice is sent to the user. The session remains active and the protection window expires naturally. Sending a notice in the protected case would be misleading (the session isn't ending) and adds undesirable noise. Accepted by design.
- **Regression GUI run registry is in-memory only (2026-05-11)** — A `pnpm dev` restart while a regression run is mid-flight kills the subprocess and drops the run state. Cached per-case results on disk survive (REQ-REG-010). Recovery is "rerun the suite from the GUI" — acceptable for an admin-only operator UI where in-flight runs are infrequent. Persistence would require a write-ahead-log of run state to disk; deferred indefinitely.
- **Regression GUI single-active-run serialization (2026-05-11)** — Only one regression run can execute at a time across the GUI surface. The second concurrent `POST /gui/regression/runs` receives 409 with `{activeRunId}` so the UI can attach to the existing SSE stream instead of erroring. Required because the `CostTracker` delta-based metering only produces accurate per-call cost under sequential dispatch. Lifting this would require either (a) per-call usage plumbing through `LLMService.complete()` so cost isn't delta-derived, or (b) a per-run cost-tracker instance. Accepted as designed.
- **Regression GUI estimator constants are approximations (2026-05-11; amended 2026-05-22)** — `core/src/gui/services/regression/estimator.ts` ships hard-coded `$/case` constants per bucket (routing 0.005, receipt 0.06, chatbot 0.04, recall 0.01). These approximate observed costs but do not call into the production `CostTracker.estimateCost` calculator. Real safety is the per-run cap (`regression.maxRunBudgetUsd`, default $5.00). As of 2026-05-22, `RunResult.tokenCounts` carries real observed token data (REQ-REG-018), so numeric recalibration is now possible from a fresh full-suite run; the constants are kept as approximations by design. A follow-up tracks numeric recalibration — see "Numeric recalibration of regression estimator constants" under Deferred Infrastructure Work below.
- ~~**Local Gemma 4 e4b is unreliable on session-control "continue" nuance**~~ — Removed 2026-05-12 (Codex P2 correction). Stale: fresh routing cache shows session-control is now 3/3 pass after the Batch 3 prefilter + few-shot examples landed. If the failure recurs under different inputs, re-open with new cache evidence.
- **Regression GUI v2 — pre-rework cache entries not in leaderboard (2026-05-13)** — Pre-this-phase cache entries lack the new `RunManifest` files (and the `evaluatedTier` field on `RunResult`). They do not appear in the Overview leaderboard or Trends charts. They remain fully accessible in the Compare tab. Operators can repopulate by re-running affected models. Accepted breaking change for the new surface.
- **Regression GUI v2 — Trends `<table>` fallback test is structural only (2026-05-13)** — REQ-REG-GUI-V2-016's accessible `<table>` fallback is asserted via the trends-partial smoke test (`?view=trends` renders without error). A targeted DOM-assertion test for the fallback's contents is deferred — the Trends partial only renders the table when `trendsHasData === true` and the route-test fixture doesn't currently inject a populated `runHistoryStore`. Manual verification covers this surface.
- **Regression GUI v2 — cache key still does not include provider (2026-05-13)** — Carry-forward from the model-override-surface phase. `TierModelSnapshot` stores model strings only; two providers with the same model name would collide in both the cache directory layout AND the new `latestPerTierAndModel` map. Operators using two providers for the same-named model would see one's results overwritten in the leaderboard. Schema migration deferred.
- ~~**Regression GUI v2 — model-aware estimator (2026-05-13; amended 2026-05-22)** — `estimator.ts` uses flat per-bucket constants. Operator-driven model changes in the Run-tab dropdowns don't update the estimate. Acceptable approximation.~~ ✓ **Closed for locality (2026-09-02, Truncation Diagnosis + Local-Model Cost Correctness phase, commit `f4a8767`)** — `estimateRunCostUsd` now takes `localTiers`, charges $0 for any bucket whose serving tier is a local provider, and returns `allLocal` so the Run-tab banner and confirm dialog say "all-local run" instead of a bare $0.00. The risk this entry recorded was real and operator-visible: an all-Ollama 37-case routing sweep displayed ~$0.19 for a run that costs nothing. REQ-LLM-043. **Still open:** *numeric* recalibration of the remote constants — deliberately unchanged here — which remains tracked as "Numeric recalibration of regression estimator constants" under Deferred Infrastructure Work.
- **Regression GUI v2 — cross-run diff view (2026-05-13)** — "Show me the inputs gemma-31b passes but haiku-4-5 fails" requires a new UI surface. High value but deferred to a follow-on phase.
- **Regression GUI v2 — interactive charts (2026-05-13)** — Trends SVGs are static. Tooltips, zoom, click-to-filter all deferred. Adding interactivity means either vendoring a chart library (50–200KB JS) or hand-rolling SVG event handlers. Decide when there's specific user demand.
- **Regression GUI v2 — per-bucket model override (2026-05-13)** — Run launcher applies overrides globally per tier, mirroring the CLI. A future operator might want different models per bucket (e.g., routing under Haiku, chatbot under Sonnet). Deferred.
- **Regression GUI Polish — Pre-fix CLI sweeps not in leaderboard (2026-05-13)** — Pure CLI invocations of `pnpm test:regression` prior to this phase did not pass `--run-id` and therefore never wrote `RunManifest` files. Those historical gemma4 / haiku sweep `RunResult` cache files exist on disk but do not appear in the GUI Overview leaderboard. Operators see them only via the Compare tab if they navigate to the specific case. New CLI sweeps (after this phase) auto-generate a UUID + manifest by default (REQ-REG-CLI-MAN-001) so they appear alongside GUI-launched runs. No back-fill — accepted as a one-time historical gap.
- **Regression GUI Polish — SSE event log + in-flight runs are in-memory (2026-05-13)** — The per-run event log added for SSE reconnect (`MAX_EVENT_LOG_ENTRIES = 1000` ring buffer, REQ-REG-GUI-V2-022) lives in the run registry. Completed runs remain visible after restart via the `RunManifest` read-back (the leaderboard reads from disk). **In-flight runs killed by process restart are lost entirely** — no manifest exists for them, the in-memory event log is gone, and the SSE connection is dropped without a terminal event. Operators must rerun. Accepted: in-flight runs are short-lived and operator-initiated; durability would require a write-ahead log we don't yet need.
- **Regression GUI Polish — client wrapper not unit-tested with DOM (2026-05-13)** — The new EventSource wrapper in `regression-live.eta` (auto-reconnect with `Last-Event-ID`, 3-strike "Lost connection" banner, `gap`-event reload, terminal-state guard) is verified via (a) rendered-page assertions in `regression-routes.test.ts` (REQ-REG-GUI-V2-021/024 wiring is present in the response body), and (b) server-side SSE replay/gap tests. A DOM-level unit test with a fake `EventSource` was scoped but deferred — the `@pas/core` vitest config is node-only (no jsdom), so adding it would require new dependencies. Manual smoke (browser devtools "Offline" toggle) covers the gap.
- **Receipt parser — single-shot continuation cap (2026-05-15, PR1)** — On `finishReason === 'length'`, `parseReceiptFromPhoto` fires exactly one continuation call and never more. Receipts long enough to need a third pass (~160+ items, rare at Sam's Club / large bulk runs) surface `continuation_unresolved + output_truncated` on the final result and the user sees the "couldn't fully verify" Telegram warning. Operator action: verify manually or re-photograph in two halves. Lifting the cap to N-pass continuation would require state-machine plumbing and a budget guard; deferred until real-world need.
- **Chunk A.2 — synthetic expired-90d receipt is pristine OCR text (2026-05-15)** — `regression/fixtures/receipts/expired-90d.jpg` is rendered text on a white background, not a creased thermal print with faded ink and photo glare. A parser that passes this fixture cleanly may still fail on a real >90-day receipt with crumpled paper. Mitigation: the 4 real-receipt fixtures (costco-long, trader-joes-{correction,long,short}) exercise OCR robustness for in-window receipts; the synthetic fixture only proves the `isValidReceiptDate` rejection branch + `rawExtractedDate` preservation. Replacing with a real photographed expired receipt would strengthen coverage but is low priority — the date-rejection path is deterministic and has dedicated unit-test coverage.
- **Chunk A.2 — cropped Trader Joe's fixtures (2026-05-15)** — `trader-joes-long.jpg` and `trader-joes-short.jpg` are cropped above the printed date line in the operator's view of the photo. Sidecars omit `date` so the parser's actual extraction (whatever it reads from the full-resolution JPG) is accepted without assertion. If the parser ever silently falls back to today's date for these (e.g., because of an OCR regression that breaks date reading on full-resolution JPGs), this fixture pair would not catch it. Mitigation: the expired-90d fixture is the canonical date-fallback prober; the cropped fixtures aren't expected to exercise that branch.
- ~~**Receipt parser — cannot detect self-consistent inflation**~~ ✓ Two-oracle defense in regression suite (PR2, 2026-05-15). When the LLM fudges `unitPrice` AND `totalPrice` AND `subtotal` self-consistently, every PARSER-side check still passes — the parser cannot self-detect this failure mode. **The regression suite now defends with TWO independent oracles**: (1) `structural` over `.expected.json` (already catches the bug — see `regression/src/__tests__/structural-catches-inflation.characterization.test.ts`); (2) `transcription` over operator-authored `.transcription.yaml` ground truth (Codex review #1: structural alone is sufficient for the bug shape; PR2's value is drift resistance — preventing silent `.expected.json` regeneration — plus confidence tiers). Coverage: 4 real-receipt fixtures with both sidecars; 15 adversarial-persona integration scenarios. See REQ-FOOD-RECEIPT-TRANSCRIPTION-001..011.
- **LLM stack — Ollama `done_reason` heuristic on older SDKs (2026-05-15, PR1)** — Newer Ollama exposes `response.done_reason` (mapped to `finishReason`). Older versions don't, and the fallback is `eval_count >= options.maxTokens ? 'length' : 'stop'`. This can yield a false `'length'` when the model happened to stop exactly at the cap on its own. The receipt parser's continuation pass transparently resolves this — if there's nothing more to extract, the continuation returns `{lineItems: []}` and the final result is clean. Accepted as a known false-positive that costs one extra LLM call.
- ~~**PAS classifier doesn't yet flag platform-invite questions as `pasRelated:true` (2026-05-24)** — The 2026-05-24 regression run for `pas-invite-platform-positive` showed the PAS classifier returns `pasRelated:false` for all 7 platform-invite phrasings ("Can you tell me about inviting people?", "how do I invite someone to PAS", "how do invite codes work", "add a new user to the platform", "invite my wife to use this", etc.). The case's oracle asserts `pasRelated:true`, hence the per-case `verdict:fail`. **The original chatbot-context-and-routing bug fix IS working** — the deterministic router-level test at `core/src/services/router/__tests__/router-verification.test.ts:'invite-platform routing — deterministic gate (Codex R2 F3)'` (commit `d83a517`) proves the router's always-verify gate + verifier path REDIRECTS the bug-shaped Food-hosting verdict to the chatbot (food.handleMessage is NOT called, conversationService.handleMessage IS). The LLM-classifier-level aspiration that the chatbot should proactively recognize these as platform-feature questions (so it can surface invite-code help unprompted) is a separate, deeper concern — fixing it likely requires PAS-classifier prompt work, few-shot examples for platform-feature phrasings, or both. Accepted for this phase; the case file stays unchanged as an aspirational gate for future PAS-classifier improvements. Trigger: a PAS-classifier prompt-tuning pass OR user complaint that the chatbot doesn't proactively help with platform features when asked.~~ ✓ **Closed (2026-05-24, Classifier + Reply-Collector + Call-Graph Guard phase)** — `PLATFORM_INVITE_RE` deterministic prefilter (10 entries across 8 numbered branches, each anchored on PAS/platform/access/relationship) + 3 new LLM few-shot examples in the systemPrompt + Food shadow-classifier rejection mirror for the 3 new phrasings. Regression case `pas-invite-platform-positive` flips FAIL→PASS deterministically with `verdict:pass`, `source:fresh`, `costUsd:0`, `tokenCounts.{input,output}:0` (no LLM dispatch — all 7 inputs short-circuit on the prefilter). REQ-CONV-PAS-CLASSIFY-001..005.

- **Google and OpenAI models are unprobed for `temperature` rejection (2026-09-01)** — `MODEL_CAPABILITIES` (`core/src/services/llm/model-capabilities.ts`) was seeded from a live probe of the Anthropic catalog only; no Google or OpenAI key is configured on this deployment, so no model from those providers has an entry. They rely on the permissive default (unknown model → `temperature` supported) plus `BaseProvider`'s single strip-and-retry self-heal, which costs one extra round-trip and emits a warn naming the model. Accepted: the self-heal is the designed fallback for exactly this case, and the warn tells an operator which entry to add. Revisit if a Google or OpenAI key is configured and the warn shows up in production logs.
- **Date-bearing receipt photo fixtures have a 90-day shelf life by construction (2026-09-01)** — REQ-REG-020 removes the current failure (expectations now model the parser's `MAX_RECEIPT_AGE_DAYS` acceptance window and assert ground truth on `rawExtractedDate` once a fixture ages out), but the underlying tension is structural: a photo fixture encodes an immutable real-world date while the parser's behaviour is relative to today, so a fixture necessarily crosses a behavioural boundary as it ages. The same tension recurs anywhere a fixture encodes an absolute date. Accepted: the branch is now explicit and both sides are pinned by clock-frozen tests, so the crossing is a modelled transition rather than a silent failure. Revisit if a future parser gains more date-relative branches than the single 90-day one.
- **Regression live-progress client behaviour is verified by inspection + server-side SSE tests only (2026-09-01)** — `setProgress()`, the native `<progress>` element and the terminal-handler behaviour in `regression-live.eta`'s inline script have no jsdom harness (the `@pas/core` vitest config is node-only — same standing gap as the 2026-05-13 "client wrapper not unit-tested with DOM" entry above). This is precisely why REQ-REG-GUI-V2-027's counts are **server-computed**: registry-side counts are testable through SSE frames and rendered-page assertions, whereas client-side counting would be untestable here. Coverage today: rendered-page wiring assertions, CSS-token assertions, run-registry snapshot tests and write-route progress-frame tests. Accepted with that mitigation; a future jsdom harness would close it for both entries at once.

---

## Persona Regression Suite — Chunk B.2 carry-forwards (2026-05-11)

- **LLMService.complete usage plumbing** — *(Partially resolved 2026-05-22.)* `core/src/services/llm/index.ts:56–75` `LLMServiceImpl.complete()` returns only the response `string`, dropping the `usage` object even though `provider.completeWithUsage()` returns `LLMCompletionResult` with `{inputTokens, outputTokens}`. `LLMService.complete()` was deliberately NOT changed in the Regression Token Metering phase. **Regression consequences are now closed:** the harness meters closures around production classifiers via `CostTracker.getTokenUsageTotals()` deltas (REQ-REG-018); REQ-REG-013 now displays real per-case token counts; REQ-REG-017 estimator note references real token data. **Still open:** per-*step* token and cost metering for the chatbot-primary T1 tool-calling substrate — threading usage metadata through the tool-calling step boundaries. `LLMService.completeWithMeta()` already exists and returns `{text, finishReason, usage}`, so T1 should reuse that existing API rather than change `complete()`'s contract, unless the tool-loop genuinely needs a new public surface. This is a separate T1 work item.

---

## Per-Model Tool-Call Support Matrix

*(Populated during T1a dependency spike — all entries TBV as of 2026-05-08)*

When T1a spike runs, update this table with confirmed results from the `experiments/tool-call-spike/` driver:

| Provider | Model | supportsTools | supportsParallelToolCalls | supportsJsonSchema | Notes |
|---|---|---|---|---|---|
| Anthropic | claude-sonnet-4-6 | TBV | TBV | TBV | |
| Anthropic | claude-haiku-4-5 | TBV | TBV | TBV | |
| Google | gemini-2.0-flash | TBV | TBV | TBV | |
| OpenAI-compat | gpt-4o | TBV | TBV | TBV | |
| Ollama | llama3.1 | TBV | TBV | TBV | |
| Ollama | qwen2.5 | TBV | TBV | TBV | |
| Ollama | mistral | TBV | TBV | TBV | |
| Ollama | gemma2 | TBV | TBV | TBV | Expected: no tool support |

Tool-name pattern (confirmed in spike): `[a-zA-Z0-9_-]{1,64}` (Anthropic/OpenAI, no dots); verify Google's variant.
Ollama provider pick: `ai-sdk-ollama@^3.8.x` OR `ollama-ai-provider-v2` — T1a chooses based on tool-call fidelity per model.

---

## Credits and Influences

Resources that informed PAS design, used with attribution:

| Resource | License | What we borrowed |
|---|---|---|
| [Vercel AI SDK (`ai`)](https://github.com/vercel/ai) | Apache 2.0 | Tool-call substrate: `ToolLoopAgent`, per-step cost reservation via `prepareStep`, `activeTools` preselection, `tool approval` callback (chatbot-primary T1+) |
| [asg017/sqlite-vec](https://github.com/asg017/sqlite-vec) | MIT | Local vector index virtual table for semantic recall alongside FTS5 (T7.1+) |
| [Mert Cobanov — memory.cobanov.dev](https://memory.cobanov.dev/) | (cited as influence) | Working/episodic/semantic/procedural memory taxonomy; RRF hybrid retrieval; HyDE; supersession governance patterns |
| [Letta (formerly MemGPT)](https://github.com/letta-ai/letta) | Apache 2.0 | Self-editing memory + hierarchical context paging (informs snapshot rebuild design and `<memory-kind-set>`) |
| [LangGraph](https://github.com/langchain-ai/langgraph) | MIT | Tool-loop checkpoint design concepts |
| [Anthropic MCP](https://modelcontextprotocol.io/) | MIT | Future: expose PAS apps as MCP servers or consume external MCP tools |
| [Xenova/Transformers.js](https://github.com/xenova/transformers.js) | Apache 2.0 | Fallback in-process embedding (T7.1, if Ollama + OpenAI-compat both unavailable) |
