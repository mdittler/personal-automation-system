{
	"name": "@pas/notes",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"description": "Quick notes via Telegram — save, list, and summarize daily notes (example app)",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "tsc --build",
		"clean": "rm -rf dist tsconfig.tsbuildinfo"
	},
	"dependencies": {
		"@pas/core": "workspace:*"
	}
}
